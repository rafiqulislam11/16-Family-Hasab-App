/**
 * খাতা — Main Application Controller (App.js)
 * Master logic, event routing, PWA lifecycle, accounting views, modal orchestration
 */

/**
 * ========================================================
 * BUSINESS CATEGORIES CONFIGURATION & PRODUCT CATEGORIES
 * ========================================================
 */
const BUSINESS_CATEGORIES = [
  {
    id: 'grocery',
    name: 'মুদি ও গ্রোসারি',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🛒',
    badgeClass: 'grocery',
    desc: 'চাল, ডাল, তেল, লবণ, মসলা, চিনি ও নিত্যপ্রয়োজনীয় মুদি সামগ্রী',
    productCategories: ["চাল-ডাল ও আটা", "ভোজ্যতেল ও ঘি", "চিনি ও লবণ", "মসলা ও রান্নার উপকরণ", "বিস্কুট ও চানাচুর", "চা, কফি ও পানীয়", "সাবান ও টয়লেট্রিজ", "প্যাকেটজাত খাবার"]
  },
  {
    id: 'super-shop',
    name: 'সুপার শপ',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🏬',
    badgeClass: 'grocery',
    desc: 'এক ছাদের নিচে খাদ্য, প্রসাধন, ফ্রোজেন ফুড ও গৃহস্থালি পণ্য',
    productCategories: ["গ্রোসারি ও খাদ্যশস্য", "ফ্রোজেন ফুড ও মিট", "বেকারি ও স্ন্যাক্স", "টয়লেট্রিজ ও ক্লিনিং", "ডেইরি ও দুগ্ধজাত", "পানীয় ও জুস", "গৃহস্থালি সামগ্রী"]
  },
  {
    id: 'rice-warehouse',
    name: 'চালের আড়ত',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🌾',
    badgeClass: 'wholesale',
    desc: 'মিনিকেট, নাজিরশাইল, বাসমতী, পাইজাম, চিনিগুড়া ও পাইকারি চাল',
    productCategories: ["মিনিকেট চাল", "নাজিরশাইল চাল", "পাইজাম চাল", "আমন ও বোরো চাল", "চিনিগুড়া ও পোলাও চাল", "বাসমতী চাল", "লাল চাল ও আতপ চাল"]
  },
  {
    id: 'spices-dry-food',
    name: 'মসলা ও শুকনা খাবার',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🌶️',
    badgeClass: 'grocery',
    desc: 'এলাচ, দারুচিনি, লবঙ্গ, হলুদ, মরিচ, বাদাম ও কিশমিশ',
    productCategories: ["গরম মসলা (এলাচ, দারুচিনি)", "হলুদ, মরিচ ও ধনে গুঁড়া", "জিরা ও তেজপাতা", "কাজু, পেস্তা ও কাঠবাদাম", "কিশমিশ ও খেজুর", "শুকনা মরিচ ও গোটা মসলা"]
  },
  {
    id: 'fruit-shop',
    name: 'ফলের দোকান',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍎',
    badgeClass: 'grocery',
    desc: 'দেশি ও বিদেশি তাজা ফলমূল, আপেল, কমলা, আঙ্গুর ও মাল্টা',
    productCategories: ["আপেল ও নাশপাতি", "কমলা ও মাল্টা", "আঙ্গুর ও ডালিম", "কলা ও পেঁপে", "আম ও লিচু", "মৌসুমি ফল ও খেজুর"]
  },
  {
    id: 'vegetable-shop',
    name: 'সবজির দোকান',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🥦',
    badgeClass: 'grocery',
    desc: 'তাজা কাঁচা শাকসবজি, আলু, পেঁয়াজ, রসুন, আদা ও মরিচ',
    productCategories: ["আলু ও পেঁয়াজ", "রসুন ও আদা", "কাঁচা মরিচ ও লেবু", "টমেটো ও শসা", "সবুজ শাকসবজি", "লাউ, মিষ্টি কুমড়া ও বেগুন"]
  },
  {
    id: 'fish-market',
    name: 'মাছের দোকান',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🐟',
    badgeClass: 'grocery',
    desc: 'রুই, কাতলা, ইলিশ, চিংড়ি, পাঙ্গাশ ও দেশি জ্যান্ত মাছ',
    productCategories: ["ইলিশ মাছ", "রুই ও কাতলা", "চিংড়ি মাছ", "পাঙ্গাশ ও তেলাপিয়া", "দেশি ছোট মাছ", "সামুদ্রিক মাছ ও শুঁটকি"]
  },
  {
    id: 'meat-shop',
    name: 'মাংসের দোকান',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🥩',
    badgeClass: 'grocery',
    desc: 'গরুর মাংস, খাসির মাংস, ব্রয়লার, সোনালী ও দেশি মুরগি',
    productCategories: ["গরুর মাংস", "খাসির মাংস", "ব্রয়লার মুরগি", "সোনালী ও দেশি মুরগি", "হাঁসের মাংস", "কলিজা ও গিজার্ড"]
  },
  {
    id: 'bakery',
    name: 'বেকারি',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍞',
    badgeClass: 'restaurant',
    desc: 'তাজা পাউরুটি, বান, কেক, পেস্ট্রি, কুকিজ ও বিস্কুট',
    productCategories: ["পাউরুটি ও স্লাইস রুটি", "কেক ও পেস্ট্রি", "কুকিজ ও বিস্কুট", "টোস্ট ও বাটার বান", "পেটিস ও রোল", "চকোলেট ও ডোনাট"]
  },
  {
    id: 'sweet-shop',
    name: 'মিষ্টির দোকান',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍬',
    badgeClass: 'restaurant',
    desc: 'রসগোল্লা, চমচম, ছানামুখী, মিষ্টি দই, লাড্ডু ও সন্দেশ',
    productCategories: ["রসগোল্লা ও গোলাপজামুন", "চমচম ও রসমালাই", "সন্দেশ ও কাঁচাগোল্লা", "মিষ্টি দই ও টক দই", "লাড্ডু ও জিলাপি", "ঘি ও খাঁটি ছানা"]
  },
  {
    id: 'restaurant-dining',
    name: 'রেস্টুরেন্ট',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍽️',
    badgeClass: 'restaurant',
    desc: 'ভাত, পোলাও, বিরিয়ানি, তরকারি, মাছ-মাংস ও চায়নিজ খাবার',
    productCategories: ["বিরিয়ানি ও তেহারি", "পোলাও ও ফ্রাইড রাইস", "চিকেন ও মাটন কারি", "মাছ ও ভর্তা-ভাজি", "নান, পরোটা ও চাপ", "স্যুপ ও ফ্রাই", "পানীয় ও ডেজার্ট"]
  },
  {
    id: 'fast-food',
    name: 'ফাস্ট ফুড',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍔',
    badgeClass: 'restaurant',
    desc: 'বার্গার, পিজ্জা, ফ্রেঞ্চ ফ্রাই, ফ্রাইড চিকেন, স্যান্ডউইচ ও জুস',
    productCategories: ["বার্গার ও স্যান্ডউইচ", "পিজ্জা ও পাস্তা", "ক্রিস্পি ফ্রাইড চিকেন", "ফ্রেঞ্চ ফ্রাই ও নাগেটস", "শাওয়ারমা ও রোল", "কোল্ড ড্রিংকস ও শেক"]
  },
  {
    id: 'tea-coffee-shop',
    name: 'চা ও কফি শপ',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '☕',
    badgeClass: 'restaurant',
    desc: 'দুধ চা, রং চা, স্পেশাল মালাই চা, এসপ্রেসো কফি ও হালকা নাশতা',
    productCategories: ["দুধ চা ও মালাই চা", "ব্ল্যাক টি ও লেমন টি", "এসপ্রেসো ও কোল্ড কফি", "ক্যাপুচিনো ও লাতে", "টোস্ট, সমুচা ও সিংগাড়া", "বিস্কুট ও স্ন্যাক্স"]
  },
  {
    id: 'catering-service',
    name: 'ক্যাটারিং সার্ভিস',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍱',
    badgeClass: 'service',
    desc: 'বিয়ে, জন্মদিন ও সামাজিক অনুষ্ঠানের খাবার রান্না ও ক্যাটারিং',
    productCategories: ["কাচ্চি বিরিয়ানি প্যাকেজ", "মোরগ পোলাও প্যাকেজ", "রেগুলার খাবার প্যাকেজ", "সালাদ, বোরহানি ও রায়তা", "মিষ্টি ও জর্দা", "ক্যাটারিং সরঞ্জাম ভাড়া"]
  },
  {
    id: 'street-food',
    name: 'ফুচকা ও স্ট্রিট ফুড',
    group: '🛒 খাদ্য, মুদি ও খাবার',
    icon: '🍲',
    badgeClass: 'restaurant',
    desc: 'ফুচকা, চটপটি, ভেলপুরি, ঝালমুড়ি, দোসা ও স্ট্রিট স্ন্যাক্স',
    productCategories: ["স্পেশাল ফুচকা ও দই ফুচকা", "চটপটি ও ডিম চটপটি", "ভেলপুরি ও পাপড়ি চাট", "ঝালমুড়ি ও মসলা বুট", "স্পেশাল টক ও মসলা"]
  },
  {
    id: 'cloth-store',
    name: 'কাপড়ের দোকান',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '🧵',
    badgeClass: 'clothing',
    desc: 'গজ কাপড়, থান কাপড়, শার্ট-প্যান্টের কাপড় ও ফেব্রিক',
    productCategories: ["সুতি গজ কাপড়", "ভয়েল ও লন থান", "শার্ট ও প্যান্টের কাপড়", "সিল্ক ও জর্জেট ফেব্রিক", "লাইলিং ও ফলস কাপড়", "চাদর ও বেডশিট কাপড়"]
  },
  {
    id: 'saree-shop',
    name: 'শাড়ির দোকান',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '🥻',
    badgeClass: 'clothing',
    desc: 'জামদানি, কাতান, বেনারসি, সিল্ক, সুতি ও পার্টি শাড়ি',
    productCategories: ["জামদানি শাড়ি", "কাতান ও বেনারসি শাড়ি", "সুতি ও তাঁতের শাড়ি", "সিল্ক ও হাফসিল্ক শাড়ি", "জর্জেট ও পার্টি শাড়ি", "ব্রাইডাল শাড়ি"]
  },
  {
    id: 'three-piece-dress',
    name: 'থ্রি-পিস ও ড্রেস',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '👗',
    badgeClass: 'clothing',
    desc: 'রেডিমেড ও আনস্টিচড থ্রি-পিস, কুর্তি, গাউন ও লেডিস ড্রেস',
    productCategories: ["সুতি থ্রি-পিস", "পার্টি থ্রি-পিস", "আনস্টিচড ড্রেস মেটেরিয়াল", "কুর্তি ও টিউনিক", "গাউন ও ম্যাক্সি ড্রেস", "লেগিংস ও প্লাজো"]
  },
  {
    id: 'mens-clothing',
    name: 'পাঞ্জাবি ও পুরুষদের পোশাক',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '🥼',
    badgeClass: 'clothing',
    desc: 'পাঞ্জাবি, পায়জামা, শার্ট, প্যান্ট, টি-শার্ট ও জিন্স',
    productCategories: ["সেমি-লং পাঞ্জাবি", "প্রিমিয়াম কার্পাস পাঞ্জাবি", "শার্ট (ফরমাল ও ক্যাজুয়াল)", "টি-শার্ট ও পোলো শার্ট", "জিন্স ও গ্যাবার্ডিন প্যান্ট", "পায়জামা ও ট্রাউজার"]
  },
  {
    id: 'kids-clothing',
    name: 'শিশুদের পোশাক',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '👶',
    badgeClass: 'clothing',
    desc: 'নবজাতক ও বাচ্চাদের ফ্রক, সেট, টি-শার্ট, প্যান্ট ও শীতবস্ত্র',
    productCategories: ["নবজাতকের পোশাক ও ন্যাপকিন", "বাচ্চাদের ফ্রক ও পার্টি ড্রেস", "ছেলেদের টি-শার্ট ও শর্টস", "বেবি স্যুট ও সেট", "বাচ্চাদের শীতবস্ত্র", "বেবি ক্যাপ ও মোজা"]
  },
  {
    id: 'shoes-sandals',
    name: 'জুতা ও স্যান্ডেল',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '👟',
    badgeClass: 'clothing',
    desc: 'লেদার জুতা, স্নিকার্স, ক্যাজুয়াল শু, হিল ও স্যান্ডেল',
    productCategories: ["পুরুষদের ফরমাল জুতা", "স্নিকার্স ও স্পোর্টস শু", "লেডিস ফ্ল্যাট ও হিল স্যান্ডেল", "বাচ্চাদের জুতা", "স্লিপার ও ক্যাজুয়াল স্যান্ডেল", "মোজা ও শু-কেয়ার সামগ্রী"]
  },
  {
    id: 'bags-luggage',
    name: 'ব্যাগ ও লাগেজ',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '👜',
    badgeClass: 'clothing',
    desc: 'স্কুল ব্যাগ, ট্রাভেল লাগেজ, ব্যাকপ্যাক, লেডিস ভ্যানিটি ব্যাগ ও পার্স',
    productCategories: ["লেডিস হ্যান্ডব্যাগ ও পার্স", "স্কুল ও কলেজ ব্যাকপ্যাক", "ট্রাভেল ট্রলি লাগেজ", "ল্যাপটপ ব্যাগ ও অফিস ব্যাগ", "ওয়ালেট ও মানিব্যাগ", "ডাফেল ব্যাগ"]
  },
  {
    id: 'fashion-accessories',
    name: 'ফ্যাশন অ্যাক্সেসরিজ',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '👓',
    badgeClass: 'clothing',
    desc: 'সানগ্লাস, বেল্ট, ক্যাপ, টাই, হেয়ারব্যান্ড ও অর্নামেন্টস',
    productCategories: ["লেদার বেল্ট", "সানগ্লাস ও আইওয়্যার", "ক্যাপ ও হ্যাট", "টাই ও কাফলিংক", "হেয়ার ব্যান্ড ও ক্লিপ", "ফ্যাশন গয়না ও অর্নামেন্টস"]
  },
  {
    id: 'cosmetics-shop',
    name: 'কসমেটিকস',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '💄',
    badgeClass: 'cosmetics',
    desc: 'ফেসওয়াশ, লিপস্টিক, ময়েশ্চারাইজার, শ্যাম্পু, মেকআপ ও স্কিন কেয়ার',
    productCategories: ["ফেসওয়াশ ও স্ক্রাব", "লিপস্টিক ও লিপ কেয়ার", "ময়েশ্চারাইজার ও লোশন", "শ্যাম্পু ও হেয়ার অয়েল", "ফাউন্ডেশন ও কমপ্যাক্ট", "আইলাইনার ও মাশকারা"]
  },
  {
    id: 'perfume-attar',
    name: 'পারফিউম ও আতর',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '🧴',
    badgeClass: 'cosmetics',
    desc: 'আরবি আতর, ফ্রেঞ্চ পারফিউম, বডি স্প্রে, উদ ও সুগন্ধি',
    productCategories: ["অ্যালকোহলমুক্ত খাঁটি আতর", "ব্র্যান্ডেড পারফিউম", "বডি স্প্রে ও ডিওডোরেন্ট", "উদ ও বাকহুর", "রোল-অন আতর", "পারফিউম গিফট সেট"]
  },
  {
    id: 'jewelry-shop',
    name: 'জুয়েলারি',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '💍',
    badgeClass: 'clothing',
    desc: 'স্বর্ণ, রূপা, ডায়মন্ড কাট ও প্রিমিয়াম সিটি গোল্ড গহনা',
    productCategories: ["নেকলেস ও হার", "কানের দুল ও ঝুমকা", "আংটি ও রিং", "চুড়ি ও ব্রেসলেট", "পায়েল ও নূপুর", "ব্রাইডাল জুয়েলারি সেট"]
  },
  {
    id: 'watch-shop',
    name: 'ঘড়ির দোকান',
    group: '👗 পোশাক, ফ্যাশন ও ব্যক্তিগত যত্ন',
    icon: '⌚',
    badgeClass: 'electronics',
    desc: 'স্মার্টওয়াচ, অ্যানালগ ঘড়ি, দেয়াল ঘড়ি ও লেদার ওয়াচ',
    productCategories: ["স্মার্টওয়াচ ও ফিটনেস ব্যান্ড", "পুরুষদের রিস্ট ওয়াচ", "মহিলাদের ফ্যাশন ওয়াচ", "দেয়াল ঘড়ি ও টেবিল ক্লক", "ঘড়ির বেল্ট ও ব্যাটারি", "ঘড়ি সার্ভিসিং পার্টস"]
  },
  {
    id: 'mobile-phone',
    name: 'মোবাইল ফোন',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '📱',
    badgeClass: 'electronics',
    desc: 'স্মার্টফোন, বাটন ফোন, ট্যাব ও নতুন-পুরাতন হ্যান্ডসেট',
    productCategories: ["অ্যান্ড্রয়েড স্মার্টফোন", "আইফোন (iPhone)", "বাটন/ফিচার ফোন", "ট্যাবলেট (Tablet)", "সেকেন্ডহ্যান্ড/ব্যবহৃত ফোন"]
  },
  {
    id: 'mobile-accessories',
    name: 'মোবাইল অ্যাক্সেসরিজ',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '🔌',
    badgeClass: 'electronics',
    desc: 'চার্জার, ডাটা ক্যাবল, হেডফোন, গ্লাস প্রটেক্টর ও ব্যাক কভার',
    productCategories: ["ফাস্ট চার্জার ও এডাপ্টার", "ইউএসবি ও টাইপ-সি ক্যাবল", "হেডফোন ও ইয়ারবাডস", "ব্যাক কভার ও কেসিং", "ডিসপ্লে গ্লাস প্রটেক্টর", "পাওয়ার ব্যাংক"]
  },
  {
    id: 'mobile-servicing',
    name: 'মোবাইল সার্ভিসিং',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '🔧',
    badgeClass: 'service',
    desc: 'ডিসপ্লে চেঞ্জ, ব্যাটারি রিপ্লেস, ফ্ল্যাশিং ও সার্ভিসিং',
    productCategories: ["ডিসপ্লে/কম্বো রিপ্লেসমেন্ট", "ব্যাটারি পরিবর্তন", "চার্জিং পোর্ট ও স্পিকার মেরামত", "আইসি ও মাদারবোর্ড কাজ", "সফটওয়্যার ও আনলকিং সার্ভিস", "অন্যান্য মোবাইল পার্টস"]
  },
  {
    id: 'computer-laptop',
    name: 'কম্পিউটার ও ল্যাপটপ',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '💻',
    badgeClass: 'electronics',
    desc: 'ব্র্যান্ডেড পিসি, কাস্টম ডেস্কটপ, গেমিং পিসি ও ল্যাপটপ',
    productCategories: ["নতুন ল্যাপটপ", "ব্যবহৃত/রিবিল্ট ল্যাপটপ", "ডেস্কটপ সিপিইউ", "গেমিং পিসি বিল্ড", "মনিটর ও ডিসপ্লে", "অল-ইন-ওয়ান পিসি"]
  },
  {
    id: 'computer-accessories',
    name: 'কম্পিউটার অ্যাক্সেসরিজ',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '🖱️',
    badgeClass: 'electronics',
    desc: 'কীবোর্ড, মাউস, পেনড্রাইভ, এসএসডি, র‍্যাম ও রাউটার',
    productCategories: ["কীবোর্ড ও মাউস", "এসএসডি ও হার্ডডিস্ক", "র‍্যাম ও প্রসেসর", "ওয়াইফাই রাউটার ও সুইচ", "পেনড্রাইভ ও মেমোরি কার্ড", "পাওয়ার সাপ্লাই ও ক্যাসিং"]
  },
  {
    id: 'computer-servicing',
    name: 'কম্পিউটার সার্ভিসিং',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '🛠️',
    badgeClass: 'service',
    desc: 'উইন্ডোজ সেটআপ, হার্ডওয়্যার রিপেয়ার ও ল্যাপটপ সার্ভিসিং',
    productCategories: ["উইন্ডোজ ও ওএস সেটআপ", "ল্যাপটপ হার্ডওয়্যার মেরামত", "চিপ-লেভেল মাদারবোর্ড সার্ভিস", "থার্মাল পেস্ট ও সার্ভিসিং", "ডাটা রিকভারি সার্ভিস"]
  },
  {
    id: 'electronics-general',
    name: 'ইলেকট্রনিক্স',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '⚡',
    badgeClass: 'electronics',
    desc: 'সাউন্ড সিস্টেম, স্পিকার, ব্লুটুথ ডিভাইস ও সাধারণ ইলেকট্রনিক্স',
    productCategories: ["ব্লুটুথ স্পিকার ও সাউন্ডবার", "মাইক্রোফোন ও অডিও ডিভাইস", "ভোল্টেজ স্ট্যাবিলাইজার", "মাল্টিপ্লাগ ও এক্সটেনশন", "ইলেকট্রনিক কনভার্টার ও পার্টস"]
  },
  {
    id: 'tv-home-appliances',
    name: 'টিভি ও হোম অ্যাপ্লায়েন্স',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '📺',
    badgeClass: 'electronics',
    desc: 'স্মার্ট এলইডি টিভি, ব্লেন্ডার, ওভেন, রাইস কুকার ও আয়রন',
    productCategories: ["স্মার্ট এলইডি টিভি", "ব্লেন্ডার ও মিক্সার গ্রাইন্ডার", "মাইক্রোওয়েভ ওভেন", "রাইস কুকার ও প্রেশার কুকার", "ইলেকট্রিক আয়রন ও ইস্ত্রি", "ইন্ডাকশন ও ইনফ্রারেড চুলা"]
  },
  {
    id: 'fridge-ac',
    name: 'ফ্রিজ ও এসি',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '❄️',
    badgeClass: 'electronics',
    desc: 'রেফ্রিজারেটর, ডিপ ফ্রিজ, ইনভার্টার এসি ও এয়ার কুলার',
    productCategories: ["রেগুলার রেফ্রিজারেটর", "ডিপ ফ্রিজার", "ইনভার্টার স্প্লিট এসি", "নন-ইনভার্টার এসি", "এয়ার কুলার", "ফ্রিজ-এসি পার্টস ও গ্যাস"]
  },
  {
    id: 'electric-goods',
    name: 'ইলেকট্রিক পণ্য',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '💡',
    badgeClass: 'electronics',
    desc: 'বৈদ্যুতিক তার, সুইচ, সকেট, সার্কিট ব্রেকার ও হোল্ডার',
    productCategories: ["হাউজ ওয়্যারিং তার (বিআরবি/অন্যান্য)", "সুইচ ও সকেট বোর্ড", "এমসিবি ও সার্কিট ব্রেকার", "প্লাগ ও টপ", "হোল্ডার ও ইনসুলেশন টেপ", "কনসিল বক্স ও পাইপ"]
  },
  {
    id: 'lighting-led',
    name: 'লাইটিং ও LED',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '💡',
    badgeClass: 'electronics',
    desc: 'এলইডি বাল্ব, টিউবলাইট, স্পটলাইট, সিলিং লাইট ও স্ট্রিপ',
    productCategories: ["এলইডি বাল্ব ও এনার্জি বাল্ব", "টিউবলাইট ও বাতেন লাইট", "সিলিং স্পট ও প্যানেল লাইট", "রোড লাইট ও ফ্লাড লাইট", "এলইডি স্ট্রিপ ও ডেকোরেশন লাইট", "নাইট লাইট"]
  },
  {
    id: 'fan-appliances',
    name: 'ফ্যান ও বৈদ্যুতিক সরঞ্জাম',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '🪭',
    badgeClass: 'electronics',
    desc: 'সিলিং ফ্যান, টেবিল ফ্যান, স্ট্যান্ড ফ্যান ও এক্সহস্ট ফ্যান',
    productCategories: ["৫৬\" সিলিং ফ্যান", "স্ট্যান্ড ফ্যান ও প্যাডেস্টাল", "টেবিল ও হাই স্পিড ফ্যান", "এক্সহস্ট ফ্যান", "চার্জার ফ্যান", "ফ্যান রেগুলেটর ও ক্যাপাসিটর"]
  },
  {
    id: 'camera-cctv',
    name: 'ক্যামেরা ও CCTV',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '📹',
    badgeClass: 'electronics',
    desc: 'সিসিটিভি ক্যামেরা, আইপি ক্যামেরা, ডিভিআর ও ক্যামেরা অ্যাক্সেসরিজ',
    productCategories: ["এইচডি সিসিটিভি ক্যামেরা", "আইপি ও ওয়াইফাই ক্যামেরা", "ডিভিআর ও এনভিআর মেশিন", "সার্ভিল্যান্স হার্ডডিস্ক", "সিসিটিভি পাওয়ার সাপ্লাই ও ক্যাবল", "ক্যামেরা লেন্স ও ট্রাইপড"]
  },
  {
    id: 'printer-photocopy',
    name: 'প্রিন্টার ও ফটোকপি',
    group: '📱 প্রযুক্তি, ইলেকট্রনিক্স ও গ্যাজেট',
    icon: '🖨️',
    badgeClass: 'electronics',
    desc: 'লেজার ও ইঙ্কজেট প্রিন্টার, ফটোকপিয়ার, টোনার ও রিফিলিং',
    productCategories: ["লেজারজেট প্রিন্টার", "ইঙ্কট্যাংক প্রিন্টার", "হেভি ফটোকপি মেশিন", "প্রিন্টার টোনার ও কার্তুজ", "রিফিল ইঙ্ক ও পাউডার", "প্রিন্টার স্পেয়ার পার্টস"]
  },
  {
    id: 'photo-studio',
    name: 'ফটো স্টুডিও',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '📷',
    badgeClass: 'service',
    desc: 'পাসপোর্ট ছবি, ফটো ফ্রেম, পোর্ট্রেট ও ফটো প্রিন্ট',
    productCategories: ["পাসপোর্ট ও স্ট্যাম্প ফটো", "ফটো ফ্রেম ও গ্লাস", "বড় প্রিন্ট ও অ্যালবাম", "ডিজিটাল ফটো এডিটিং", "কাস্টম মগ ও টি-শার্ট প্রিন্ট"]
  },
  {
    id: 'graphic-design',
    name: 'গ্রাফিক ডিজাইন',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '🎨',
    badgeClass: 'service',
    desc: 'লোগো, ভিজিটিং কার্ড, ব্যানার, পোস্টার ও ডিজাইন সেবা',
    productCategories: ["লোগো ও ব্র্যান্ডিং ডিজাইন", "ভিজিটিং কার্ড ডিজাইন", "ব্যানার ও ফেস্টুন ডিজাইন", "সোশ্যাল মিডিয়া পোস্ট ডিজাইন", "ক্যাটালগ ও ফ্লায়ার ডিজাইন"]
  },
  {
    id: 'printing-press',
    name: 'প্রিন্টিং প্রেস',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '🖨️',
    badgeClass: 'service',
    desc: 'বই, লিফলেট, রসিদ বই, ক্যালেন্ডার ও অফসেট প্রিন্টিং',
    productCategories: ["ভিজিটিং কার্ড প্রিন্ট", "ক্যাশ মেমো ও চালান বই", "লিফলেট ও হ্যান্ডবিল", "পোস্টার ও ব্রোশিওর", "ক্যালেন্ডার ও ডায়েরি প্রিন্ট", "খাম ও লেটারহেড"]
  },
  {
    id: 'signboard-led-sign',
    name: 'সাইনবোর্ড ও LED সাইন',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '🪧',
    badgeClass: 'service',
    desc: 'অ্যাক্রিলিক ৩ডি লেটার, এলইডি নিয়ন সাইন ও বিলবোর্ড',
    productCategories: ["এলইডি নিয়ন সাইন", "অ্যাক্রিলিক ৩ডি লেটার", "পিভিসি ও ভিনাইল সাইনবোর্ড", "রোড বিলবোর্ড ও ফ্রেম", "লাইট বক্স সাইন"]
  },
  {
    id: 'stationery-shop',
    name: 'স্টেশনারি',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '📚',
    badgeClass: 'stationery',
    desc: 'খাতা, কলম, পেন্সিল, ফাইল, স্ট্যাপলার ও অফিস স্টেশনারি',
    productCategories: ["খাতা ও এক্সারসাইজ বুক", "বলপেন, জেলপেন ও সাইনপেন", "পেন্সিল, ইরেজার ও কাটার", "ফাইল ও ফোল্ডার", "কাগজ ও রিম (A4/Legal)", "অফিস স্টেশনারি"]
  },
  {
    id: 'book-library',
    name: 'বই ও লাইব্রেরি',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '📖',
    badgeClass: 'stationery',
    desc: 'পাঠ্যবই, ইসলামিক বই, উপন্যাস, সাহিত্য ও সাধারণ জ্ঞান',
    productCategories: ["স্কুল ও মাদ্রাসার পাঠ্যবই", "গাইড বই ও টেস্ট পেপার", "ইসলামিক ও ধর্মীয় বই", "উপন্যাস ও সাহিত্য", "সাধারণ জ্ঞান ও জব প্রস্তুতি", "শিশুতোষ গল্প ও ছড়া"]
  },
  {
    id: 'toys-shop',
    name: 'খেলনা',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '🧸',
    badgeClass: 'general',
    desc: 'বাচ্চাদের রিমোট গাড়ি, পুতুল, লেগো, ব্লক ও খেলনা সামগ্রী',
    productCategories: ["রিমোট কন্ট্রোল গাড়ি ও ড্রোন", "পুতুল ও ডল হাউস", "বিল্ডিং ব্লক ও পাজল", "বেবি ওয়াকার ও ট্রাইসাইকেল", "এডুকেশনাল টয় ও বোর্ড গেম"]
  },
  {
    id: 'gift-shop',
    name: 'গিফট শপ',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '🎁',
    badgeClass: 'general',
    desc: 'গিফট আইটেম, শো-পিস, ওয়ালমেট, ডল ও আকর্ষণীয় উপহার',
    productCategories: ["শো-পিস ও স্ট্যাচু", "ওয়ালমেট ও সিনারি", "ঘড়ি ও ফটো ফ্রেম গিফট", "চকলেট গিফট বক্স", "গিফট র‍্যাপিং সামগ্রী"]
  },
  {
    id: 'flower-decoration',
    name: 'ফুল ও ডেকোরেশন',
    group: '📚 মিডিয়া, প্রকাশনা, স্টেশনারি ও উপহার',
    icon: '💐',
    badgeClass: 'general',
    desc: 'প্রাকৃতিক ও কৃত্রিম ফুল, ফুলের তোড়া ও ইভেন্ট ডেকোরেশন',
    productCategories: ["তাজা গোলাপ ও রজনীগন্ধা", "কৃত্রিম ফুলের তোড়া", "স্টেজ ও গাড়ি সাজানো সেবা", "ফুলের বাস্কেট ও বোকে", "ডেকোরেটিভ প্ল্যান্ট"]
  },
  {
    id: 'furniture-shop',
    name: 'ফার্নিচার',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🛋️',
    badgeClass: 'construction',
    desc: 'খাট, আলমারি, ড্রেসিং টেবিল, সোফা, ডাইনিং টেবিল ও আসবাবপত্র',
    productCategories: ["কাঠের খাট ও বেড", "আলমারি ও ওয়ারড্রব", "সোফা সেট ও ডিভান", "ডাইনিং টেবিল ও চেয়ার", "ড্রেসিং টেবিল ও শোকেস", "অফিস টেবিল ও এক্সিকিউটিভ চেয়ার"]
  },
  {
    id: 'interior-design',
    name: 'ইন্টেরিয়র ডিজাইন',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '📐',
    badgeClass: 'construction',
    desc: 'হোম ইন্টেরিয়র, ফলস সিলিং, ক্যাবিনেট ও আর্কিটেকচারাল ডিজাইন',
    productCategories: ["ফলস সিলিং ও জিপসাম কাজ", "মডুলার কিচেন ক্যাবিনেট", "ওয়াল প্যানেলিং ও উডেন ডেকোর", "অফিস ইন্টেরিয়র ফিটিংস", "থ্রিডি ডিজাইন ও কনসাল্টেশন"]
  },
  {
    id: 'hardware-store',
    name: 'হার্ডওয়্যার',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🔩',
    badgeClass: 'construction',
    desc: 'রেঞ্জ, ড্রিল মেশিন, হাতুড়ি, তালা, স্ক্রু ও নাট-বোল্ট',
    productCategories: ["হাতুড়ি, প্লায়ার্স ও স্ক্রু ড্রাইভার", "ড্রিল মেশিন ও কাটার", "তালা, লক ও সিকিউরিটি হ্যান্ডেল", "স্ক্রু, নাট-বোল্ট ও কব্জা", "আঠা, গ্লু ও মেটাল সিলেন্ট", "রশি, তার ও চেইন"]
  },
  {
    id: 'steel-cement',
    name: 'রড ও সিমেন্ট',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🏗️',
    badgeClass: 'construction',
    desc: '৬০ ও ৭২ গ্রেডের এমএস রড, পোর্টল্যান্ড সিমেন্ট ও নির্মাণ সামগ্রী',
    productCategories: ["৬০ গ্রেড টিএমটি রড", "৭২ গ্রেড বিশেষ রড", "পোর্টল্যান্ড কম্পোজিট সিমেন্ট (PCC)", "অর্ডিনারি পোর্টল্যান্ড সিমেন্ট (OPC)", "বাইন্ডিং ওয়্যার ও রিং"]
  },
  {
    id: 'sand-stone',
    name: 'বালু ও পাথর',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🧱',
    badgeClass: 'construction',
    desc: 'সিলেট বালু, ভিটি বালু, লাল বালু, বোল্ডার পাথর ও ইটের খোয়া',
    productCategories: ["সিলেট বালু (মোটা বালু)", "লোকাল ভিটি বালু", "লাল বালু", "ভাঙ্গা পাথর ও চিপস", "ইটের খোয়া ও ইট"]
  },
  {
    id: 'tiles-shop',
    name: 'টাইলস',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🔲',
    badgeClass: 'construction',
    desc: 'ফ্লোর টাইলস, ওয়াল টাইলস, সিরামিক, গ্রানাইট ও মার্বেল পাথর',
    productCategories: ["ফ্লোর টাইলস (১৬x১৬, ২৪x২৪)", "বাথরুম ও কিচেন ওয়াল টাইলস", "সিঁড়ি ও পার্কিং টাইলস", "মার্বেল ও গ্রানাইট স্ল্যাব", "টাইলস আঠা ও গ্রাউট"]
  },
  {
    id: 'sanitary-bath',
    name: 'স্যানিটারি ও বাথরুম পণ্য',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🚿',
    badgeClass: 'construction',
    desc: 'কমোড, বেসিন, পানির কল, শাওয়ার, পিভিসি পাইপ ও বাথ ফিটিংস',
    productCategories: ["কমোড ও প্যান", "ওয়াশ বেসিন ও পেডেস্টাল", "পানির ট্যাপ ও মিক্সার কল", "শাওয়ার ও বাথ এক্সেসরিজ", "পিভিসি পাইপ ও ফিটিংস", "পানির রিজার্ভার ট্যাংক"]
  },
  {
    id: 'paint-shop',
    name: 'রং ও পেইন্ট',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🎨',
    badgeClass: 'construction',
    desc: 'প্লাস্টিক পেইন্ট, ডিসটেম্পার, ওয়েদার কোট, বার্নিশ ও ব্রাশ',
    productCategories: ["ইন্টেরিয়র প্লাস্টিক পেইন্ট", "এক্সটেরিয়র ওয়েদারকোট", "ডিসটেম্পার ও পুটি", "কাঠ ও মেটালের এনামেল পেইন্ট", "রং রোলার ও ব্রাশ", "থিনার ও তারপিন"]
  },
  {
    id: 'doors-windows',
    name: 'দরজা ও জানালা',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🚪',
    badgeClass: 'construction',
    desc: 'মেহগনি-সেগুন কাঠের দরজা, পিভিসি ডোর ও থাই জানালা',
    productCategories: ["কাঠের সলিড মেইন ডোর", "ফ্লাশ ডোর ও ভিনিয়ার ডোর", "পিভিসি বাথরুম ডোর", "থাই অ্যালুমিনিয়াম উইন্ডো", "দরজার লক ও কব্জা"]
  },
  {
    id: 'glass-aluminum',
    name: 'কাঁচ ও অ্যালুমিনিয়াম',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🪟',
    badgeClass: 'construction',
    desc: '৫মিমি/১০মিমি ফ্লোট গ্লাস, মিরর, অ্যালুমিনিয়াম সেকশন ও থাই গ্লাস',
    productCategories: ["স্বচ্ছ ফ্লোট গ্লাস", "কালার ও ফ্রস্টেড গ্লাস", "লুকিং গ্লাস ও আয়না", "অ্যালুমিনিয়াম চ্যানেল ও প্রোফাইল", "সিলিকন গাম ও রাবার"]
  },
  {
    id: 'timber-wood',
    name: 'কাঠের ব্যবসা',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🪵',
    badgeClass: 'construction',
    desc: 'সেগুন, মেহগনি, শাল, গামারি ও কাঠ চেরাই এবং কাঠের তক্তা',
    productCategories: ["সেগুন কাঠ ও সাইজ", "মেহগনি গোল কাঠ ও তক্তা", "শাল ও গর্জন কাঠ", "গামারি ও আকাশি কাঠ", "প্লাইউড ও হার্ডবোর্ড"]
  },
  {
    id: 'plastic-goods',
    name: 'প্লাস্টিক পণ্য',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🥣',
    badgeClass: 'general',
    desc: 'প্লাস্টিকের বালতি, মগ, চেয়ার, টুল, ড্রয়ার ও গৃহস্থালি প্লাস্টিক',
    productCategories: ["বালতি, বাউল ও মগ", "প্লাস্টিক চেয়ার ও টুল", "স্টোরেজ ড্রয়ার ও র‍্যাক", "পানির বোতল ও জগ", "ডাস্টবিন ও লন্ড্রি বাস্কেট"]
  },
  {
    id: 'kitchen-ware',
    name: 'কিচেন সামগ্রী',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🍳',
    badgeClass: 'general',
    desc: 'কড়াই, পাতিল, প্রেসার কুকার, ছুরি, চামচ, হটপট ও কিচেন আইটেম',
    productCategories: ["নন-স্টিক ফ্রাইপ্যান ও কড়াই", "প্রেসার কুকার", "অ্যালুমিনিয়াম পাতিল ও ডেগ", "ছুরি, বঁটি ও কাটিং বোর্ড", "হটপট ও ক্যাসেরোল", "চামচ ও সার্ভিং সেট"]
  },
  {
    id: 'household-items',
    name: 'গৃহস্থালি পণ্য',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🏠',
    badgeClass: 'general',
    desc: 'ঝাড়ু, মপ, হ্যাঙ্গার, আয়না, লন্ড্রি ঝুড়ি ও গৃহস্থালি সামগ্রী',
    productCategories: ["ঝাড়ু ও ফ্লোর ওয়াইপার", "স্পিন মপ ও ক্লিনার", "কাপড় শুকানোর র‍্যাক ও ক্লিপ", "হেঙ্গার ও ক্লথ অর্গানাইজার", "আয়রন টেবিল ও সিঁড়ি"]
  },
  {
    id: 'curtains-carpets',
    name: 'পর্দা ও কার্পেট',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🪟',
    badgeClass: 'clothing',
    desc: 'জানালা-দরজার পর্দা, ফ্লোর কার্পেট, ডোরম্যাট ও ভেলভেট ফেব্রিক',
    productCategories: ["রেডিমেড জানালার পর্দা", "পর্দার থান কাপড়", "পর্দার পাইপ ও রিং ব্র্যাকেট", "ফ্লোর কার্পেট ও রাগস", "ওয়েলকাম ডোরম্যাট ও ম্যাট"]
  },
  {
    id: 'mattress-bedding',
    name: 'ম্যাট্রেস ও বেডিং',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🛏️',
    badgeClass: 'general',
    desc: 'অর্থোপেডিক ম্যাট্রেস, ফোম, তোশক, বালিশ, বেডশিট ও কম্বল',
    productCategories: ["অর্থোপেডিক ম্যাট্রেস", "হাই ডেনসিটি ফোম ও তোশক", "তুলা ও ফাইবার বালিশ", "কটন বেডশিট ও কভার", "শীতের কম্বল ও কমফোর্টার"]
  },
  {
    id: 'home-decoration',
    name: 'হোম ডেকোরেশন',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🪴',
    badgeClass: 'general',
    desc: 'ইনডোর প্ল্যান্ট, ওয়াল ক্লক, ডেকোরেটিভ লাইট ও ফটো ফ্রেম',
    productCategories: ["ইনডোর প্ল্যান্ট ও টব", "ওয়াল আর্ট ও ক্যালোগ্রাফি", "ডেকোরেটিভ ল্যাম্প ও লাইট", "অ্যারোমা ডিফিউজার ও ক্যান্ডেল", "ফ্লোর ফুলদানি"]
  },
  {
    id: 'kitchen-appliances',
    name: 'রান্নাঘরের সরঞ্জাম',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🍳',
    badgeClass: 'electronics',
    desc: 'গ্যাস স্টোভ, এলপিজি সিলিন্ডার, চিমনি, কিচেন হুড ও রেগুলেটর',
    productCategories: ["ডাবল বার্নার গ্যাস স্টোভ", "গ্লাস টপ আধুনিক চুলা", "এলপিজি গ্যাস সিলিন্ডার", "কিচেন চিমনি ও হুড", "গ্যাস রেগুলেটর ও সেফটি পাইপ"]
  },
  {
    id: 'water-filter',
    name: 'পানির ফিল্টার',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '🚰',
    badgeClass: 'general',
    desc: 'আরও (RO) ওয়াটার পিউরিফায়ার, নরমাল ফিল্টার, কার্ট্রিজ ও স্পেয়ার',
    productCategories: ["আরও (RO) পিউরিফায়ার মেশিন", "ইউভি ও নরমাল ফিল্টার", "পিপি ও কার্বন কার্ট্রিজ", "আরও মেমব্রেন ফিল্টার", "মিনারেল ও অ্যালকালাইন ফিল্টার"]
  },
  {
    id: 'solar-ips',
    name: 'সোলার ও IPS',
    group: '🏠 নির্মাণ ও গৃহস্থালি',
    icon: '☀️',
    badgeClass: 'electronics',
    desc: 'আইপিএস মেশিন, টিউবুলার ব্যাটারি, সোলার প্যানেল ও ইনভার্টার',
    productCategories: ["আইপিএস ইউনিট ও ইনভার্টার", "টিউবুলার ও সোলার ব্যাটারি", "সোলার প্যানেল প্লেট", "সোলার কন্ট্রোলার ও তার", "ইউপিএস (কম্পিউটারের জন্য)"]
  },
  {
    id: 'motorcycle-showroom',
    name: 'মোটরসাইকেল শোরুম',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🏍️',
    badgeClass: 'vehicle',
    desc: 'নতুন ও ব্র্যান্ডেড মোটরসাইকেল, স্কুটার ও ই-বাইক শোরুম',
    productCategories: ["১০০-১২৫ সিসি কমিউটার বাইক", "১৫০-১৬৫ সিসি স্পোর্টস বাইক", "লেডিস স্কুটার", "ইলেকট্রিক বাইক (E-Bike)", "রেজিস্ট্রেশন ফি ও হেলমেট"]
  },
  {
    id: 'motorcycle-parts',
    name: 'মোটরসাইকেল পার্টস',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '⚙️',
    badgeClass: 'vehicle',
    desc: 'ইঞ্জিন অয়েল, ব্রেক শু, চেইন স্প্রকেট, স্পার্ক প্লাগ ও পার্টস',
    productCategories: ["ইঞ্জিন অয়েল ও মবিল (4T)", "টায়ার ও টিউব", "ব্রেক প্যাড ও ব্রেক শু", "ড্রাইভ চেইন ও স্প্রকেট কিট", "স্পার্ক প্লাগ ও এয়ার ফিল্টার", "ক্লাচ ক্যাবল ও লিভার"]
  },
  {
    id: 'motorcycle-servicing',
    name: 'মোটরসাইকেল সার্ভিসিং',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🔧',
    badgeClass: 'service',
    desc: 'বাইক টিউনিং, ইঞ্জিন মেরামত, ওয়াশ ও সার্ভিসিং সেবা',
    productCategories: ["মাস্টার সার্ভিসিং প্যাকেজ", "ইঞ্জিন ওভারহলিং ও কাজ", "কার্বুরেটর/এফআই টিউনিং", "বাইক ওয়াশ ও পলিশ", "ওয়্যারিং মেরামত"]
  },
  {
    id: 'bicycle-parts',
    name: 'সাইকেল ও পার্টস',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🚲',
    badgeClass: 'vehicle',
    desc: 'মাউন্টেন বাইক, গিয়ার সাইকেল, বাচ্চাদের সাইকেল ও পার্টস',
    productCategories: ["এমটিবি ও গিয়ার সাইকেল", "নন-গিয়ার সাধারণ সাইকেল", "বাচ্চাদের সাইকেল", "সাইকেল টায়ার ও টিউব", "প্যাডেল, রিং ও চেইন", "হেলমেট ও সাইকেল লক"]
  },
  {
    id: 'auto-parts',
    name: 'অটো পার্টস',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🚗',
    badgeClass: 'vehicle',
    desc: 'সিএনজি, অটোরিকশা ও তিন চাকার যানবাহনের স্পেয়ার পার্টস',
    productCategories: ["সিএনজি ইঞ্জিন পার্টস", "অটো ব্যাটারি ও মোটর", "ব্রেক শু ও মাস্টার সিলিন্ডার", "শক এবজর্বার ও স্প্রিং", "ওয়্যারিং ও লাইটিং"]
  },
  {
    id: 'car-parts',
    name: 'গাড়ির পার্টস',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🚙',
    badgeClass: 'vehicle',
    desc: 'প্রাইভেট কার ও মাইক্রোবাসের স্পেয়ার পার্টস, ফিল্টার ও ব্রেক',
    productCategories: ["ইঞ্জিন অয়েল ও ফ্লুইড", "এয়ার ফিল্টার ও অয়েল ফিল্টার", "ব্রেক প্যাড ও রোটর", "সাসপেনশন পার্টস ও বুশ", "ওয়াইপার ব্লেড ও হর্ন"]
  },
  {
    id: 'car-servicing',
    name: 'গাড়ি সার্ভিসিং',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🛠️',
    badgeClass: 'service',
    desc: 'কার ডেন্টিং, পেইন্টিং, ইঞ্জিন মেরামত, এসি সার্ভিস ও ওয়াশ',
    productCategories: ["ইঞ্জিন টিউনআপ ও সার্ভিস", "ডেন্টিং ও বডি রিপেয়ার", "পেইন্টিং ও কালার ম্যাচিং", "কার এসি গ্যাস ও রিপেয়ার", "কার ওয়াশ ও ফোম ক্লিনিং"]
  },
  {
    id: 'tyre-battery',
    name: 'টায়ার ও ব্যাটারি',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🛞',
    badgeClass: 'vehicle',
    desc: 'গাড়ি, বাইক ও অটোর টায়ার, টিউব ও অটোমোবাইল ব্যাটারি',
    productCategories: ["কার ও জিপ টায়ার", "বাইক টায়ার ও টিউব", "হেভি ডিউটি ট্রাক টায়ার", "কার ড্রাই ব্যাটারি (MF)", "ইউপিএস/আইপিএস ব্যাটারি"]
  },
  {
    id: 'rickshaw-van-parts',
    name: 'রিকশা ও ভ্যান পার্টস',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🛺',
    badgeClass: 'vehicle',
    desc: 'রিকশার রিং, টায়ার, স্পোক, এক্সেল, প্যাডেল ও পার্টস',
    productCategories: ["রিকশার চাকা ও রিং", "রিকশা ও ভ্যান টায়ার", "এক্সেল ও বিয়ারিং", "প্যাডেল ও ক্র্যাঙ্ক", "রিকশার বডি ফিটিংস ও হুড"]
  },
  {
    id: 'vehicle-dealer',
    name: 'গাড়ি/বাইক ডিলার',
    group: '🚗 যানবাহন ও যন্ত্রপাতি',
    icon: '🚘',
    badgeClass: 'vehicle',
    desc: 'রিকন্ডিশন্ড গাড়ি, প্রি-ওনড বাইক কেনাবেচা ও এজেন্সি',
    productCategories: ["রিকন্ডিশন্ড কার বিক্রয়", "ব্যবহৃত মোটরসাইকেল ক্রয়-বিক্রয়", "ডকুমেন্টেশন ও নাম পরিবর্তন চার্জ", "গাড়ি বুকিং টোকেন"]
  },
  {
    id: 'agri-machinery',
    name: 'কৃষি যন্ত্রপাতি',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🚜',
    badgeClass: 'agri',
    desc: 'পাওয়ার টিলার, সেচ পাম্প, স্প্রে মেশিন, ধান মাড়াই কল ও পার্টস',
    productCategories: ["সেচ পাম্প ও ডিজেল ইঞ্জিন", "পাওয়ার টিলার ও পার্টস", "স্প্রে মেশিন (ম্যানুয়াল ও ব্যাটারি)", "ধান কাটার ও মাড়াই মেশিন", "পানির পাইপ ও ডেলিভারি পাইপ"]
  },
  {
    id: 'seeds-shop',
    name: 'বীজের দোকান',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🌱',
    badgeClass: 'agri',
    desc: 'উন্নত জাতের ধান বীজ, শাকসবজির হাইব্রিড বীজ ও ভুট্টা বীজ',
    productCategories: ["হাইব্রিড ধান বীজ", "শাকসবজির হাইব্রিড বীজ", "ভুট্টা ও ডাল বীজ", "আলু বীজ", "ফুলের বীজ"]
  },
  {
    id: 'fertilizer-pesticide',
    name: 'সার ও কীটনাশক',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🌾',
    badgeClass: 'agri',
    desc: 'ইউরিয়া, টিএসপি, ডিএপি, পটাশ, বালাইনাশক ও ভিটামিন স্প্রে',
    productCategories: ["ইউরিয়া ও ডিএপি সার", "টিএসপি ও পটাশ সার", "কীটনাশক ও ছত্রাকনাশক", "আগাছানাশক", "জৈব সার ও অনুখাদ্য (জিঙ্ক/বোরন)"]
  },
  {
    id: 'cattle-feed',
    name: 'পশুখাদ্য',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🐄',
    badgeClass: 'agri',
    desc: 'গরু, ছাগল ও মহিষের ফিড, ভুসি, খৈল, খড় ও মিনারেল',
    productCategories: ["গরুর দানাদার ফিড", "গমের ভুসি ও চালের কুঁড়া", "সরিষার খৈল", "ক্যালসিয়াম ও মিনারেল মিক্স", "সাইলেজ ও ঘাসের বীজ"]
  },
  {
    id: 'poultry-farm',
    name: 'পোল্ট্রি ফার্ম',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🐔',
    badgeClass: 'agri',
    desc: 'মুরগির বাচ্চা, পোল্ট্রি ফিড, ফার্মের ওষুধ ও সরঞ্জাম',
    productCategories: ["ব্রয়লার স্টার্টার ও গ্রোয়ার ফিড", "লেয়ার লেয়ার ফিড", "একদিনের মুরগির বাচ্চা (DOC)", "পোল্ট্রি অ্যান্টিবায়োটিক ও ভিটামিন", "ফিডার ও ড্রিংকার পাত্র"]
  },
  {
    id: 'livestock-cattle',
    name: 'গবাদিপশুর ব্যবসা',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🐂',
    badgeClass: 'agri',
    desc: 'দুগ্ধবতী গাভী, মোটাতাজাকরণ ষাঁড়, ছাগল ও কোরবানি পশু',
    productCategories: ["উন্নত জাতের গাভী ও বকনা", "মোটাতাজাকরণ ষাঁড়", "ছাগল ও ভেড়া", "কোরবানির পশু বুকিং", "দুধ বিক্রি (পাইকারি)"]
  },
  {
    id: 'nursery-plants',
    name: 'নার্সারি ও গাছের দোকান',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🌿',
    badgeClass: 'agri',
    desc: 'ফলদ, বনজ, ঔষধি ও শোভাবর্ধনকারী চারাগাছ, সার ও টব',
    productCategories: ["কলমের ফলদ গাছের চারা", "বনজ ও কাঠের চারা", "ঔষধি গাছের চারা", "ইনডোর ও ক্যাকটাস ভ্যারাইটি", "মাটির ও প্লাস্টিকের টব", "ভার্মিকম্পোস্ট ও কোকোপিট"]
  },
  {
    id: 'farming-agribusiness',
    name: 'ফার্মিং ও কৃষি ব্যবসা',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🚜',
    badgeClass: 'agri',
    desc: 'মাছ চাষ, সমন্বিত খামার, এগ্রো প্রজেক্ট ও কৃষি ফসল উৎপাদন',
    productCategories: ["মাছের পোনা ও রেণু", "মাছের ভাসমান ফিড", "মাটির স্বাস্থ্য পরীক্ষার সেবা", "কৃষি ফসল সরাসরি বিক্রয়"]
  },
  {
    id: 'pharmacy-drugstore',
    name: 'ফার্মেসি',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '💊',
    badgeClass: 'pharmacy',
    desc: 'অ্যালোপ্যাথিক ওষুধ, প্রেসক্রিপশন ড্রাগ, সিরাপ, ইনজেকশন ও ফার্স্ট এইড',
    productCategories: ["ট্যাবলেট ও ক্যাপসুল", "সিরাপ ও সাসপেনশন", "ইনজেকশন ও স্যালাইন", "আই/ইয়ার ড্রপস ও অয়েন্টমেন্ট", "সার্জিক্যাল ও ব্যান্ডেজ", "ডায়াবেটিক ও প্রেশার কেয়ার", "শিশুখাদ্য ও পুষ্টি"]
  },
  {
    id: 'medical-equipment',
    name: 'মেডিকেল সরঞ্জাম',
    group: '🌾 কৃষি ও অন্যান্য',
    icon: '🩺',
    badgeClass: 'pharmacy',
    desc: 'বিপি মেশিন, গ্লুকোমিটার, নেবুলাইজার, থার্মোমিটার ও হুইলচেয়ার',
    productCategories: ["ডিজিটাল ও ম্যানুয়াল বিপি মেশিন", "ডায়াবেটিস গ্লুকোমিটার ও স্ট্রিপ", "নেবুলাইজার মেশিন ও কিট", "থার্মোমিটার ও অক্সিমিটার", "হুইলচেয়ার ও ক্র্যাচ", "সার্জিক্যাল গ্লাভস ও মাস্ক"]
  },
  {
    id: 'tailoring-shop',
    name: 'টেইলারিং',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '✂️',
    badgeClass: 'service',
    desc: 'শার্ট, প্যান্ট, স্যুট, পাঞ্জাবি ও পোশাক কাটিং-সেলাই',
    productCategories: ["শার্ট তৈরি মজুরি", "প্যান্ট ও ট্রাউজার মজুরি", "পাঞ্জাবি ও পায়জামা মজুরি", "ব্লাউজ ও পেটিকোট মজুরি", "স্যুট ও ব্লেজার মেকিং", "অল্টারেশন ও ফিটিং চার্জ"]
  },
  {
    id: 'boutique-house',
    name: 'বুটিক',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '👗',
    badgeClass: 'clothing',
    desc: 'ডিজাইনার ড্রেস, হ্যান্ডলুম, এমব্রয়ডারি ও বুটিক কালেকশন',
    productCategories: ["এক্সক্লুসিভ ডিজাইনার কুর্তি", "হ্যান্ড পেইন্ট ও এমব্রয়ডারি শাড়ি", "কাস্টম ব্রাইডাল গাউন", "বুটিক থ্রি-পিস কালেকশন", "হ্যান্ডমেড এক্সেসরিজ"]
  },
  {
    id: 'salon-mens',
    name: 'সেলুন',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '💈',
    badgeClass: 'service',
    desc: 'পুরুষদের হেয়ার কাটিং, শেভিং, হেয়ার স্পা ও ফেসিয়াল',
    productCategories: ["হেয়ার কাট ও স্টাইলিং", "শেভিং ও বিয়ার্ড ট্রিম", "হেয়ার কালার ও ডাই", "ফেসিয়াল ও ডি-ট্যান", "হেয়ার স্পা ও ম্যাসাজ", "হেয়ার ওয়াক্স ও জেল"]
  },
  {
    id: 'beauty-parlor',
    name: 'বিউটি পার্লার',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '💇‍♀️',
    badgeClass: 'cosmetics',
    desc: 'মহিলাদের ভ্রু প্লাক, ফেসিয়াল, হেয়ার রিবন্ডিং ও মেকআপ',
    productCategories: ["ভ্রু প্লাক ও ওয়াক্সিং", "ব্রাইডাল মেকআপ প্যাকেজ", "পার্টি মেকআপ", "ফেসিয়াল (গোল্ড/ডায়মন্ড/হার্বাল)", "হেয়ার রিবন্ডিং ও কেরাটিন", "ম্যানিকিউর ও পেডিকিউর"]
  },
  {
    id: 'laundry-dryclean',
    name: 'লন্ড্রি ও ড্রাই ক্লিনিং',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '🧺',
    badgeClass: 'service',
    desc: 'পোশাক ওয়াশ, স্টিম ইস্ত্রি, ড্রাই ওয়াশ ও কম্বল ক্লিনিং',
    productCategories: ["শার্ট-প্যান্ট স্টিম ইস্ত্রি", "স্যুট ও ব্লেজার ড্রাই ওয়াশ", "শাড়ি ও লেহেঙ্গা স্পেশাল ওয়াশ", "কম্বল ও লেপ ক্লিনিং", "পর্দা ও বেডশিট ওয়াশ"]
  },
  {
    id: 'courier-delivery',
    name: 'কুরিয়ার ও ডেলিভারি',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '📦',
    badgeClass: 'service',
    desc: 'পার্সেল বুকিং, হোম ডেলিভারি, কন্ডিশন পার্সেল ও ডেলিভারি',
    productCategories: ["সাধারণ ডকুমেন্ট কুরিয়ার চার্জ", "পার্সেল বুকিং চার্জ (প্রতি কেজি)", "ক্যাশ অন ডেলিভারি (COD) কমিশন", "জরুরি/সেম-ডে ডেলিভারি ফি", "প্যাকিং কার্টন ও বাবল র‍্যাপ"]
  },
  {
    id: 'travel-agency',
    name: 'ট্রাভেল এজেন্সি',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '✈️',
    badgeClass: 'service',
    desc: 'এয়ার টিকিট, ভিসা প্রসেসিং, ওমরাহ ও ট্যুর প্যাকেজ সার্ভিস',
    productCategories: ["ঘরোয়া ও আন্তর্জাতিক এয়ার টিকিট", "ওমরাহ ও হজ্ব প্যাকেজ", "ভিসা প্রসেসিং কনসাল্টেন্সি", "হলিডে ট্যুর প্যাকেজ", "হোটেল বুকিং সার্ভিস", "ট্রাভেল ইন্স্যুরেন্স"]
  },
  {
    id: 'real-estate',
    name: 'রিয়েল এস্টেট',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '🏢',
    badgeClass: 'service',
    desc: 'ফ্ল্যাট, প্লট ও জমি কেনাবেচা, ভাড়া এবং প্রপার্টি কনসাল্টেন্সি',
    productCategories: ["আবাসিক ফ্ল্যাট বিক্রয়", "বাণিজ্যিক স্পেস বিক্রয় ও ভাড়া", "রেডি প্লট ও জমি কেনাবেচা", "ফ্ল্যাট ও বাড়ি ভাড়া সার্ভিস", "প্রপার্টি ভ্যালুয়েশন ও কনসালটেন্সি"]
  },
  {
    id: 'digital-marketing-it',
    name: 'ডিজিটাল মার্কেটিং ও IT সার্ভিস',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '💻',
    badgeClass: 'service',
    desc: 'ফেসবুক বুস্টিং, এসইও, ওয়েব ডেভেলপমেন্ট ও সফটওয়্যার সেবা',
    productCategories: ["ফেসবুক ও গুগল অ্যাড বুস্টিং", "ওয়েবসাইট তৈরি ও ডোমেন-হোস্টিং", "সার্চ ইঞ্জিন অপটিমাইজেশন (SEO)", "কন্টেন্ট রাইটিং ও কপিরাইটিং", "সফটওয়্যার ও ই-কমার্স সলিউশন"]
  },
  {
    id: 'online-ecommerce',
    name: 'অনলাইন শপ / ই-কমার্স',
    group: '💼 সার্ভিস ও প্রফেশনাল ব্যবসা',
    icon: '🌐',
    badgeClass: 'general',
    desc: 'অনলাইন পণ্য বিক্রয়, ড্রপশিপিং ও ফেসবুক ভিত্তিক ই-কমার্স',
    productCategories: ["অনলাইন কালেকশন আইটেম", "ভাইরাল ও ট্রেন্ডিং প্রোডাক্টস", "কম্বো ও ডিসকাউন্ট অফার প্যাকেজ", "ডেলিভারি চার্জ ও প্যাকিং", "গিফট বক্স ও স্পেশাল হ্যাম্পার"]
  }
];

/**
 * Helper to populate all category select dropdowns (<optgroup> supported)
 */
function populateBusinessCategorySelects() {
  const profSelect = document.getElementById('profBusinessCategory');
  const regSelect = document.getElementById('regBusinessCategory');
  if (!profSelect && !regSelect) return;

  // Build grouped options HTML
  const groups = {};
  BUSINESS_CATEGORIES.forEach(cat => {
    const grp = cat.group || 'অন্যান্য ব্যবসা';
    if (!groups[grp]) groups[grp] = [];
    groups[grp].push(cat);
  });

  let optionsHtml = '';
  for (const [grpName, list] of Object.entries(groups)) {
    optionsHtml += `<optgroup label="${grpName}">`;
    list.forEach(c => {
      optionsHtml += `<option value="${c.name}">${c.icon} ${c.name}</option>`;
    });
    optionsHtml += `</optgroup>`;
  }

  if (profSelect) {
    const curVal = profSelect.value;
    profSelect.innerHTML = optionsHtml;
    if (curVal) profSelect.value = curVal;
  }
  if (regSelect) {
    regSelect.innerHTML = optionsHtml;
  }
}

function getBusinessCategoryMeta(nameOrId) {
  if (!nameOrId) return BUSINESS_CATEGORIES[0];
  const clean = String(nameOrId).trim().toLowerCase();

  // 1. Direct match on name or id
  let matched = BUSINESS_CATEGORIES.find(c => 
    c.name === String(nameOrId).trim() || 
    c.id.toLowerCase() === clean
  );
  if (matched) return matched;

  // 2. Legacy / Alias Matching for backward compatibility
  if (clean.includes('মুদি') || clean.includes('সুপারশপ') || clean.includes('গ্রোসারি')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'grocery') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('ফার্মেসি') || clean.includes('ড্রাগ') || clean.includes('ওষুধ')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'pharmacy-drugstore') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('বস্ত্র') || clean.includes('কাপড়') || clean.includes('গার্মেন্টস')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'cloth-store') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('হার্ডওয়্যার')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'hardware-store') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('ইলেকট্রনিক্স') || clean.includes('ইলেকট্রিক')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'electronics-general') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('রেস্তোরাঁ') || clean.includes('রেস্টুরেন্ট') || clean.includes('হোটেল') || clean.includes('ক্যাফে')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'restaurant-dining') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('কসমেটিকস') || clean.includes('পার্লার')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'cosmetics-shop') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('বই') || clean.includes('লাইব্রেরি')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'book-library') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('স্টেশনারি')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'stationery-shop') || BUSINESS_CATEGORIES[0];
  }
  if (clean.includes('পাইকারি') || clean.includes('ট্রেডার্স')) {
    return BUSINESS_CATEGORIES.find(c => c.id === 'rice-warehouse') || BUSINESS_CATEGORIES[0];
  }

  // 3. Substring match
  matched = BUSINESS_CATEGORIES.find(c => 
    clean.includes(c.name.toLowerCase()) || 
    c.name.toLowerCase().includes(clean)
  );
  return matched || BUSINESS_CATEGORIES[0];
}

const App = {
  state: {
    currentView: 'dashboard',
    activeContext: 'ALL', // 'ALL' | 'BUSINESS' | 'FAMILY'
    sales: [],
    saleItems: [],
    purchases: [],
    purchaseItems: [],
    customers: [],
    suppliers: [],
    financialAccounts: [],
    stockMovements: [],
    auditLogs: [],
    bannerPeriod: 'thisMonth',
    txPeriod: 'thisMonth',
    txTypeFilter: 'ALL',
    txPaymentFilter: 'ALL',
    txSearchQuery: '',
    debtTab: 'RECEIVABLE', // RECEIVABLE (আমি পাব) or PAYABLE (আমি দেব)
    reportPeriod: 'thisMonth',
    transactions: [],
    categories: [],
    debts: [],
    budgets: [],
    savingGoals: [],
    products: [],
    productSearch: '',
    productCategoryFilter: 'ALL',
    productStockFilter: 'ALL',
    productViewMode: 'TABLE', // 'TABLE' or 'CATEGORY'
    profile: {},
    settings: {},
    accounts: [],
    activeAccount: null,
    deferredInstallPrompt: null
  },

  /**
   * Application Initialization
   */
  async init() {
    console.log('[Khata App] Starting initialization...');
    populateBusinessCategorySelects();

    // 1. Setup Service Worker for offline capability
    this.initServiceWorker();

    // 2. Setup Network listeners
    this.initNetworkListeners();

    // 3. Setup PWA Install Prompt Listener
    this.initPWAInstallPrompt();

    // 4. Initialize IndexedDB
    try {
      await khataDB.init();
      console.log('[Khata App] IndexedDB ready.');
    } catch (e) {
      console.error('[Khata App] IndexedDB failed to init:', e);
      UI.toast('ডাটাবেজ চালু হতে সমস্যা হয়েছে', 'error');
      return;
    }

    // 5. Initialize Multi-Business Accounts
    await this.initAccountsSystem();

    // 6. Load Settings, Theme, PIN Lock
    await this.loadSettings();

    // 7. Load App Data
    await this.refreshAllData();

    // 8. Update UI Shell (Header dates, owner greeting, profile)
    this.updateHeaderAndProfile();

    // 9. Render Initial View
    this.renderCurrentView();

    console.log('[Khata App] Initialized successfully.');
  },

  /**
   * Initialize Multi-Business Accounts & Active Session
   */
  async initAccountsSystem() {
    let accounts = await khataDB.getAll('accounts');
    if (!accounts || accounts.length === 0) {
      const founderAcc = {
        id: 'acc_rafiqul_main',
        businessName: 'মেসার্স রফিক স্টোর',
        ownerName: 'রফিকুল ইসলাম',
        tagline: 'দোকান, ব্যবসা ও সংসারের সহজ হিসাব',
        phone: '0131082-4987',
        email: 'rafiqulislam.globalwork@gmail.com',
        address: 'ঢাকা, বাংলাদেশ',
        pin: '1234',
        role: 'FOUNDER',
        photoUrl: 'icons/founder_rafiqul_islam.jpg',
        isFounder: true,
        businessCategory: 'মুদি দোকান ও সুপারশপ',
        createdAt: new Date().toISOString()
      };
      await khataDB.put('accounts', founderAcc);
      accounts = [founderAcc];
    } else {
      // Ensure all existing accounts have businessCategory and photoUrl
      for (const acc of accounts) {
        let changed = false;
        if (!acc.businessCategory) {
          acc.businessCategory = (acc.businessName && acc.businessName.includes('ট্রেডার্স')) ? 'ফার্মেসি' : 'মুদি ও গ্রোসারি';
          changed = true;
        }
        if (!acc.photoUrl) {
          acc.photoUrl = 'icons/founder_rafiqul_islam.jpg';
          changed = true;
        }
        if (changed) {
          await khataDB.put('accounts', acc);
        }
      }
    }
    this.state.accounts = accounts;

    let activeId = localStorage.getItem('khata_active_account_id');
    let activeAcc = accounts.find(a => a.id === activeId);

    if (!activeAcc) {
      const prof = await khataDB.get('appProfile', 'main');
      if (prof && prof.activeAccountId) {
        activeAcc = accounts.find(a => a.id === prof.activeAccountId);
      }
    }

    if (!activeAcc) {
      activeAcc = accounts.find(a => a.id === 'acc_rafiqul_main') || accounts[0];
    }

    this.state.activeAccount = activeAcc;
    localStorage.setItem('khata_active_account_id', activeAcc.id);
  },

  /**
   * Service Worker Registration
   */
  initServiceWorker() {
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('sw.js')
          .then((reg) => console.log('[ServiceWorker] Registered successfully:', reg.scope))
          .catch((err) => console.log('[ServiceWorker] Registration failed:', err));
      });
    }
  },

  /**
   * Offline / Online Status Indicator
   */
  initNetworkListeners() {
    const updateOnlineStatus = () => {
      const offlineBadge = document.getElementById('offlineBadge');
      if (offlineBadge) {
        if (!navigator.onLine) {
          offlineBadge.classList.add('show');
          UI.toast('আপনি বর্তমানে অফলাইনে আছেন', 'warning');
        } else {
          offlineBadge.classList.remove('show');
        }
      }
    };

    window.addEventListener('online', updateOnlineStatus);
    window.addEventListener('offline', updateOnlineStatus);
    updateOnlineStatus();
  },

  /**
   * PWA Install Prompt listener
   */
  initPWAInstallPrompt() {
    window.addEventListener('beforeinstallprompt', (e) => {
      e.preventDefault();
      this.state.deferredInstallPrompt = e;
      const pwaCard = document.getElementById('pwaInstallCard');
      if (pwaCard) pwaCard.style.display = 'block';
    });
  },

  promptPWAInstall() {
    if (this.state.deferredInstallPrompt) {
      this.state.deferredInstallPrompt.prompt();
      this.state.deferredInstallPrompt.userChoice.then((choiceResult) => {
        if (choiceResult.outcome === 'accepted') {
          UI.toast('অ্যাপটি সফলভাবে ইনস্টল করা হচ্ছে', 'success');
        }
        this.state.deferredInstallPrompt = null;
        const pwaCard = document.getElementById('pwaInstallCard');
        if (pwaCard) pwaCard.style.display = 'none';
      });
    } else {
      UI.toast('ব্রাউজারের মেনু থেকে "Add to Home screen" নির্বাচন করুন', 'info', 4000);
    }
  },

  /**
   * Load Settings & Security Check
   */
  async loadSettings() {
    const themeObj = await khataDB.get('settings', 'theme');
    const theme = themeObj ? themeObj.value : 'light';
    this.applyTheme(theme);

    const pinObj = await khataDB.get('settings', 'pinLock');
    if (pinObj && pinObj.enabled && pinObj.hash) {
      // Show PIN Lock Screen
      UI.pinPad.init(pinObj.hash);
      const overlay = document.getElementById('pinLockOverlay');
      if (overlay) overlay.classList.add('active');
    }

    this.updatePinLockSettingsUI(pinObj);
  },

  applyTheme(theme) {
    const sunIcon = document.getElementById('themeIconSun');
    const moonIcon = document.getElementById('themeIconMoon');

    if (theme === 'dark') {
      document.body.classList.add('dark-theme');
      if (sunIcon) sunIcon.style.display = 'none';
      if (moonIcon) moonIcon.style.display = 'block';
    } else {
      document.body.classList.remove('dark-theme');
      if (sunIcon) sunIcon.style.display = 'block';
      if (moonIcon) moonIcon.style.display = 'none';
    }
  },

  async toggleTheme() {
    const isDark = document.body.classList.contains('dark-theme');
    const newTheme = isDark ? 'light' : 'dark';
    this.applyTheme(newTheme);
    await khataDB.put('settings', { key: 'theme', value: newTheme });
    UI.toast(newTheme === 'dark' ? 'ডার্ক মোড সক্রিয় হয়েছে' : 'লাইট মোড সক্রিয় হয়েছে', 'info', 1500);

    // Redraw charts with new theme colors
    this.renderActiveCharts();
  },

  /**
   * Lock App Manually with PIN
   */
  async lockAppWithPin() {
    const pinObj = await khataDB.get('settings', 'pinLock');
    if (pinObj && pinObj.enabled && pinObj.hash) {
      UI.pinPad.init(pinObj.hash);
      const overlay = document.getElementById('pinLockOverlay');
      if (overlay) overlay.classList.add('active');
    } else {
      UI.toast('প্রথমে সেটিংস থেকে পিন কোড সেট করুন', 'info');
      this.navigateView('settings');
    }
  },

  async handlePinSetupClick() {
    const pinObj = await khataDB.get('settings', 'pinLock');
    const isEnabled = pinObj && pinObj.enabled;

    if (isEnabled) {
      // Disable PIN
      UI.confirm({
        title: 'পিন লক বন্ধ করবেন?',
        message: 'আপনি কি নিশ্চিত যে আপনি পিন কোড সুরক্ষা নিষ্ক্রিয় করতে চান?',
        confirmText: 'বন্ধ করুন',
        isDanger: true,
        onConfirm: async () => {
          await khataDB.put('settings', { key: 'pinLock', enabled: false, hash: '' });
          this.updatePinLockSettingsUI({ enabled: false });
          UI.toast('পিন লক বন্ধ করা হয়েছে', 'info');
        }
      });
    } else {
      // Set New PIN prompt
      const newPin = prompt('আপনার নতুন ৪ ডিজিটের পিন কোড লিখুন (যেমন: 1234):');
      if (!newPin) return;
      if (!/^\d{4}$/.test(newPin.trim())) {
        UI.toast('পিন কোড অবশ্যই ৪ ডিজিটের হতে হবে!', 'error');
        return;
      }
      const confirmPin = prompt('পিন কোডটি পুনরায় নিশ্চিত করুন:');
      if (newPin.trim() !== confirmPin.trim()) {
        UI.toast('পিন কোড দুটি মেলেনি!', 'error');
        return;
      }

      const hash = await Utils.hashPin(newPin.trim());
      await khataDB.put('settings', { key: 'pinLock', enabled: true, hash });
      this.updatePinLockSettingsUI({ enabled: true });
      UI.toast('পিন কোড সফলভাবে সক্রিয় হয়েছে ✓', 'success');
    }
  },

  updatePinLockSettingsUI(pinObj) {
    const statusText = document.getElementById('pinLockStatusText');
    const toggleBtn = document.getElementById('pinToggleBtn');
    if (!statusText || !toggleBtn) return;

    if (pinObj && pinObj.enabled) {
      statusText.textContent = 'বর্তমানে সক্রিয় আছে ✓';
      statusText.style.color = '#059669';
      toggleBtn.textContent = 'পিন বন্ধ করুন';
      toggleBtn.className = 'btn btn-danger btn-sm';
    } else {
      statusText.textContent = 'বর্তমানে নিষ্ক্রিয়';
      statusText.style.color = 'var(--text-muted)';
      toggleBtn.textContent = 'পিন চালু করুন';
      toggleBtn.className = 'btn btn-secondary btn-sm';
    }
  },

  /**
   * Refresh all data from IndexedDB
   */
  async refreshAllData() {
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const allTx = await khataDB.getAll('transactions');
    // Account filtering with backward compatibility
    this.state.transactions = allTx.filter(t => t.accountId === activeAccId || (!t.accountId && activeAccId === 'acc_rafiqul_main'));

    this.state.categories = await khataDB.getAll('categories');

    const allDebts = await khataDB.getAll('debts');
    this.state.debts = allDebts.filter(d => d.accountId === activeAccId || (!d.accountId && activeAccId === 'acc_rafiqul_main'));

    const allBudgets = await khataDB.getAll('budgets');
    this.state.budgets = allBudgets.filter(b => b.accountId === activeAccId || (!b.accountId && activeAccId === 'acc_rafiqul_main'));

    const allGoals = await khataDB.getAll('savingGoals');
    this.state.savingGoals = allGoals.filter(g => g.accountId === activeAccId || (!g.accountId && activeAccId === 'acc_rafiqul_main'));

    this.state.profile = (await khataDB.get('appProfile', 'main')) || {};

    const allProducts = await khataDB.getAll('products');
    this.state.products = allProducts.filter(p => p.accountId === activeAccId || (!p.accountId && activeAccId === 'acc_rafiqul_main'));

    const allSales = (await khataDB.getAll('sales')) || [];
    this.state.sales = allSales.filter(s => s.accountId === activeAccId || (!s.accountId && activeAccId === 'acc_rafiqul_main'));

    this.state.saleItems = (await khataDB.getAll('saleItems')) || [];

    const allPurchases = (await khataDB.getAll('purchases')) || [];
    this.state.purchases = allPurchases.filter(p => p.accountId === activeAccId || (!p.accountId && activeAccId === 'acc_rafiqul_main'));

    this.state.purchaseItems = (await khataDB.getAll('purchaseItems')) || [];

    const allCustomers = (await khataDB.getAll('customers')) || [];
    this.state.customers = allCustomers.filter(c => c.accountId === activeAccId || (!c.accountId && activeAccId === 'acc_rafiqul_main'));

    const allSuppliers = (await khataDB.getAll('suppliers')) || [];
    this.state.suppliers = allSuppliers.filter(s => s.accountId === activeAccId || (!s.accountId && activeAccId === 'acc_rafiqul_main'));

    this.state.financialAccounts = (await khataDB.getAll('financialAccounts')) || [];
    this.state.stockMovements = (await khataDB.getAll('stockMovements')) || [];
    this.state.auditLogs = (await khataDB.getAll('auditLogs')) || [];

    // Sort transactions by date descending
    this.state.transactions.sort((a, b) => new Date(b.date + ' ' + (b.createdAt || '')) - new Date(a.date + ' ' + (a.createdAt || '')));

    // Update sidebar counters
    const sidebarCount = document.getElementById('sidebarTxCount');
    if (sidebarCount) sidebarCount.textContent = Utils.toBanglaNumber(this.state.transactions.length, false);

    const prodCountBadge = document.getElementById('sidebarProductCount');
    if (prodCountBadge) prodCountBadge.textContent = Utils.toBanglaNumber(this.state.products.length, false);

    // Refresh customers datalist for autocomplete and old-due lookup
    this.updateCustomersDatalist();

    // Refresh products datalist for cash memo autocomplete
    this.updateProductsDatalist();
  },

  /**
   * Update Topbar, Date, Profile
   */
  updateHeaderAndProfile() {
    const today = Utils.getTodayDateString();
    const dayName = Utils.getDayNameBengali(today);
    const dateFormatted = Utils.formatDateBengali(today);

    const topbarDate = document.getElementById('topbarDate');
    if (topbarDate) topbarDate.textContent = `${dayName}, ${dateFormatted}`;

    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};

    const currentBizName = acc.businessName || prof.businessName || 'সহজ খাতা';
    const currentOwnerName = acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';

    const catMeta = getBusinessCategoryMeta(acc.businessCategory || prof.businessCategory);

    // Topbar active account widget
    const topbarAccountName = document.getElementById('topbarAccountName');
    if (topbarAccountName) topbarAccountName.textContent = currentBizName;

    const topbarAccountCategory = document.getElementById('topbarAccountCategory');
    if (topbarAccountCategory) {
      topbarAccountCategory.textContent = `${catMeta.icon} ${catMeta.name}`;
      topbarAccountCategory.className = `biz-cat-badge biz-cat-badge-sm ${catMeta.badgeClass}`;
    }

    const ownerPhoto = acc.photoUrl || prof.photoUrl || 'icons/founder_rafiqul_islam.jpg';

    const topbarAccountImg = document.getElementById('topbarAccountImg');
    if (topbarAccountImg) {
      topbarAccountImg.src = ownerPhoto;
      topbarAccountImg.style.width = '100%';
      topbarAccountImg.style.height = '100%';
      topbarAccountImg.style.objectFit = 'cover';
      topbarAccountImg.style.borderRadius = '50%';
    }

    const topbarOwner = document.getElementById('topbarOwner');
    if (topbarOwner) topbarOwner.textContent = currentOwnerName;

    const sidebarShopName = document.getElementById('sidebarShopName');
    if (sidebarShopName) sidebarShopName.textContent = currentBizName;

    const sidebarOwnerName = document.getElementById('sidebarOwnerName');
    if (sidebarOwnerName) sidebarOwnerName.textContent = currentOwnerName;

    const sidebarAvatar = document.getElementById('sidebarAvatar');
    if (sidebarAvatar) {
      sidebarAvatar.innerHTML = `<img src="${ownerPhoto}" alt="${currentOwnerName}" id="sidebarAvatarImg" style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;">`;
    }

    // Update Settings Profile Form owner photo preview and hidden input
    const profOwnerPreview = document.getElementById('profOwnerPhotoPreview');
    if (profOwnerPreview) {
      profOwnerPreview.src = ownerPhoto;
    }
    const profOwnerHidden = document.getElementById('profOwnerPhotoHidden');
    if (profOwnerHidden) {
      profOwnerHidden.value = ownerPhoto;
    }

    // Profile form inputs in Settings
    const catSelect = document.getElementById('profBusinessCategory');
    if (catSelect) {
      const targetCat = acc.businessCategory || prof.businessCategory;
      const catMeta = getBusinessCategoryMeta(targetCat);
      catSelect.value = catMeta.name;
    }

    const bNameInput = document.getElementById('profBusinessName');
    if (bNameInput) bNameInput.value = currentBizName;
    const oNameInput = document.getElementById('profOwnerName');
    if (oNameInput) oNameInput.value = currentOwnerName;
    const phoneInput = document.getElementById('profPhone');
    if (phoneInput) phoneInput.value = acc.phone || prof.phone || '';
    const addrInput = document.getElementById('profAddress');
    if (addrInput) addrInput.value = acc.address || prof.address || '';

    // Update Settings Category Showcase
    this.updateMerchantCategoryShowcase(acc.businessCategory || prof.businessCategory);
  },

  /**
   * Update Settings Merchant Category Profile Showcase Card
   */
  updateMerchantCategoryShowcase(categoryName) {
    const catMeta = getBusinessCategoryMeta(categoryName);

    const badgeHeader = document.getElementById('profileCategoryBadgeHeader');
    if (badgeHeader) {
      badgeHeader.textContent = `${catMeta.icon} ${catMeta.name}`;
      badgeHeader.className = `biz-cat-badge ${catMeta.badgeClass}`;
    }

    const iconEl = document.getElementById('merchantCatIcon');
    if (iconEl) iconEl.textContent = catMeta.icon;

    const titleEl = document.getElementById('merchantCatTitle');
    if (titleEl) titleEl.textContent = catMeta.name;

    const descEl = document.getElementById('merchantCatDesc');
    if (descEl) descEl.textContent = catMeta.desc;

    const badgeEl = document.getElementById('merchantCatBadge');
    if (badgeEl) {
      badgeEl.textContent = `${catMeta.icon} সক্রিয় ক্যাটাগরি`;
      badgeEl.className = `biz-cat-badge ${catMeta.badgeClass}`;
    }

    const prodCountEl = document.getElementById('merchantCatProdCount');
    if (prodCountEl) {
      prodCountEl.textContent = `${Utils.toBanglaNumber(this.state.products.length, false)} টি`;
    }

    const groupCountEl = document.getElementById('merchantCatGroupCount');
    if (groupCountEl) {
      const distinctCats = new Set(this.state.products.map(p => (p.category || '').trim()).filter(Boolean));
      groupCountEl.textContent = `${Utils.toBanglaNumber(distinctCats.size, false)} টি`;
    }

    const stockValEl = document.getElementById('merchantCatStockVal');
    if (stockValEl) {
      const totalVal = this.state.products.reduce((acc, p) => {
        const qty = parseFloat(p.currentStock !== undefined ? p.currentStock : p.stockQty) || 0;
        const buy = parseFloat(p.buyingPrice !== undefined ? p.buyingPrice : p.buyPrice) || 0;
        return acc + (qty * buy);
      }, 0);
      stockValEl.textContent = Utils.formatCurrency(Math.round(totalVal));
    }
  },

  /**
   * Live preview when user changes business category in profile settings
   */
  onProfileCategoryChange(catName) {
    this.updateMerchantCategoryShowcase(catName);
  },

  async saveProfile(e) {
    e.preventDefault();
    const businessCategory = document.getElementById('profBusinessCategory')?.value || 'মুদি ও গ্রোসারি';
    const businessName = document.getElementById('profBusinessName').value.trim();
    const ownerName = document.getElementById('profOwnerName').value.trim();
    const phone = document.getElementById('profPhone').value.trim();
    const address = document.getElementById('profAddress').value.trim();
    const photoUrl = document.getElementById('profOwnerPhotoHidden')?.value || this.state.activeAccount?.photoUrl || 'icons/founder_rafiqul_islam.jpg';

    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const updated = {
      key: 'main',
      businessCategory,
      businessName,
      ownerName,
      phone,
      address,
      photoUrl,
      activeAccountId: activeAccId,
      updatedAt: new Date().toISOString()
    };
    await khataDB.put('appProfile', updated);
    this.state.profile = updated;

    // Also update current active account in accounts store
    if (this.state.activeAccount) {
      this.state.activeAccount.businessCategory = businessCategory;
      this.state.activeAccount.businessName = businessName;
      this.state.activeAccount.ownerName = ownerName;
      this.state.activeAccount.phone = phone;
      this.state.activeAccount.address = address;
      this.state.activeAccount.photoUrl = photoUrl;
      await khataDB.put('accounts', this.state.activeAccount);

      const idx = this.state.accounts.findIndex(a => a.id === this.state.activeAccount.id);
      if (idx !== -1) {
        this.state.accounts[idx] = { ...this.state.activeAccount };
      }
    }

    this.updateHeaderAndProfile();
    this.renderCurrentView();
    UI.toast('মালিকের ছবি ও প্রোফাইল সফলভাবে আপডেট হয়েছে ✓', 'success');
  },

  /**
   * Handle Owner Profile Photo Upload & Compression
   */
  handleOwnerPhotoUpload(event) {
    const file = event.target.files && event.target.files[0];
    if (!file) return;

    if (!file.type.startsWith('image/')) {
      UI.toast('অনুগ্রহ করে শুধুমাত্র ছবি ফাইল (JPG, PNG) নির্বাচন করুন', 'error');
      return;
    }

    const reader = new FileReader();
    reader.onload = (e) => {
      const img = new Image();
      img.onload = () => {
        const canvas = document.createElement('canvas');
        const maxDim = 360;
        let width = img.width;
        let height = img.height;

        if (width > height) {
          if (width > maxDim) {
            height = Math.round((height * maxDim) / width);
            width = maxDim;
          }
        } else {
          if (height > maxDim) {
            width = Math.round((width * maxDim) / height);
            height = maxDim;
          }
        }

        canvas.width = width;
        canvas.height = height;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0, width, height);
        const dataUrl = canvas.toDataURL('image/jpeg', 0.88);

        // Update preview and hidden input
        const preview = document.getElementById('profOwnerPhotoPreview');
        if (preview) preview.src = dataUrl;

        const hiddenInput = document.getElementById('profOwnerPhotoHidden');
        if (hiddenInput) hiddenInput.value = dataUrl;

        // Instantly reflect in topbar and sidebar
        const topbarImg = document.getElementById('topbarAccountImg');
        if (topbarImg) topbarImg.src = dataUrl;

        const sidebarAvatar = document.getElementById('sidebarAvatar');
        if (sidebarAvatar) {
          sidebarAvatar.innerHTML = `<img src="${dataUrl}" alt="মালিকের ছবি" id="sidebarAvatarImg" style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;">`;
        }

        UI.toast('মালিকের নতুন ছবি নির্বাচন করা হয়েছে। স্থায়ী করতে "প্রোফাইল সংরক্ষণ করুন" চাপুন।', 'info');
      };
      img.src = e.target.result;
    };
    reader.readAsDataURL(file);
  },

  /**
   * Reset Owner Photo to Default (Founder Photo)
   */
  resetOwnerPhoto() {
    const defaultPhoto = 'icons/founder_rafiqul_islam.jpg';
    const preview = document.getElementById('profOwnerPhotoPreview');
    if (preview) preview.src = defaultPhoto;

    const hiddenInput = document.getElementById('profOwnerPhotoHidden');
    if (hiddenInput) hiddenInput.value = defaultPhoto;

    const fileInput = document.getElementById('profOwnerPhotoInput');
    if (fileInput) fileInput.value = '';

    const topbarImg = document.getElementById('topbarAccountImg');
    if (topbarImg) topbarImg.src = defaultPhoto;

    const sidebarAvatar = document.getElementById('sidebarAvatar');
    if (sidebarAvatar) {
      sidebarAvatar.innerHTML = `<img src="${defaultPhoto}" alt="মালিকের ছবি" id="sidebarAvatarImg" style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;">`;
    }

    UI.toast('মূল ছবি রিস্টোর করা হয়েছে। স্থায়ী করতে "প্রোফাইল সংরক্ষণ করুন" চাপুন।', 'info');
  },

  /**
   * View Routing
   */
  navigateView(viewId) {
    this.state.currentView = viewId;

    // Switch active view container
    document.querySelectorAll('.app-view').forEach(view => {
      view.classList.remove('active');
    });
    const targetView = document.getElementById(`view-${viewId}`);
    if (targetView) targetView.classList.add('active');

    // Update Desktop Sidebar active nav link
    document.querySelectorAll('.sidebar-nav .nav-item').forEach(item => {
      if (item.getAttribute('data-view') === viewId) item.classList.add('active');
      else item.classList.remove('active');
    });

    // Update Mobile Bottom Nav active item
    document.querySelectorAll('.mobile-bottom-nav .bottom-nav-item').forEach(item => {
      if (item.getAttribute('data-view') === viewId) item.classList.add('active');
      else item.classList.remove('active');
    });

    // Update Topbar Active View Chip
    const viewNameMap = {
      dashboard: 'ড্যাশবোর্ড',
      transactions: 'লেনদেন খতিয়ান',
      debts: 'ধার-বাকি খাতা',
      products: 'পণ্য ও স্টক খাতা',
      reports: 'রিপোর্ট ও এনালাইটিক্স',
      budget: 'মাসিক বাজেট',
      savings: 'সঞ্চয় লক্ষ্য',
      settings: 'সেটিংস ও প্রোফাইল',
      sales: 'পিওএস বিক্রি ও চালান',
      purchases: 'ক্রয় ও সরবরাহকারী হিসাব',
      customers: 'কাস্টমার খতিয়ান',
      suppliers: 'মহাজন / সাপ্লায়ার খতিয়ান',
      accounts: 'ক্যাশ ও ব্যাংক একাউন্ট',
      family: 'পারিবারিক হিসাব ও খরচ'
    };
    const topbarViewName = document.getElementById('topbarViewName');
    if (topbarViewName && viewNameMap[viewId]) {
      topbarViewName.textContent = viewNameMap[viewId];
    }

    // Close mobile sidebar if open
    window.toggleSidebar(false);

    // Scroll to top
    window.scrollTo({ top: 0, behavior: 'smooth' });

    // Render View Content
    this.renderCurrentView();
  },

  renderCurrentView() {
    switch (this.state.currentView) {
      case 'dashboard':
        this.renderDashboard();
        break;
      case 'transactions':
        this.renderTransactionsView();
        break;
      case 'debts':
        this.renderDebtsView();
        break;
      case 'reports':
        this.renderReportsView();
        break;
      case 'budget':
        this.renderBudgetView();
        break;
      case 'savings':
        this.renderSavingGoalsView();
        break;
      case 'products':
        this.renderProductsView();
        break;
      case 'settings':
        this.updateHeaderAndProfile();
        break;
      case 'sales':
        this.renderSalesView();
        break;
      case 'purchases':
        this.renderPurchasesView();
        break;
      case 'customers':
        this.renderCustomersView();
        break;
      case 'suppliers':
        this.renderSuppliersView();
        break;
      case 'accounts':
        this.renderAccountsView();
        break;
      case 'family':
        this.renderFamilyView();
        break;
    }
  },

  /**
   * ==========================================
   * 1. DASHBOARD VIEW CONTROLLER
   * ==========================================
   */
  renderDashboard() {
    const today = Utils.getTodayDateString();

    // 1. Calculate Today's Metrics with context
    const todayTxs = this.state.transactions.filter(t => t.date === today);
    const todaySales = (this.state.sales || []).filter(s => s.date === today);
    const todayMetrics = Accounting.calculateMetrics(
      todayTxs,
      this.state.debts,
      todaySales,
      this.state.saleItems,
      this.state.products,
      this.state.activeContext
    );

    const overallMetrics = Accounting.calculateMetrics(
      this.state.transactions,
      this.state.debts,
      this.state.sales,
      this.state.saleItems,
      this.state.products,
      this.state.activeContext
    );

    // Update Dashboard Accounts Strip
    const elCash = document.getElementById('dashAccCash');
    const elBank = document.getElementById('dashAccBank');
    const elMobile = document.getElementById('dashAccMobile');
    const elTotal = document.getElementById('dashAccTotal');

    if (elCash) elCash.textContent = Utils.formatCurrency(overallMetrics.accountBalances?.Cash || 0);
    if (elBank) elBank.textContent = Utils.formatCurrency(overallMetrics.accountBalances?.Bank || 0);
    const mfsTotal = (overallMetrics.accountBalances?.bKash || 0) + (overallMetrics.accountBalances?.Nagad || 0) + (overallMetrics.accountBalances?.Rocket || 0);
    if (elMobile) elMobile.textContent = Utils.formatCurrency(mfsTotal);
    if (elTotal) elTotal.textContent = Utils.formatCurrency(overallMetrics.totalLiquidBalance || 0);

    // Context-dependent dashboard labels
    const salesCardLabel = document.querySelector('.card-sales .metric-label');
    const expenseCardLabel = document.querySelector('.card-shop-expense .metric-label');
    const profitCardLabel = document.querySelector('.card-profit .metric-label');

    if (this.state.activeContext === 'FAMILY') {
      if (salesCardLabel) salesCardLabel.textContent = 'আজকের পারিবারিক আয়';
      if (expenseCardLabel) expenseCardLabel.textContent = 'আজকের সংসার খরচ';
      if (profitCardLabel) profitCardLabel.textContent = 'আজকের পারিবারিক সঞ্চয়';

      const elSales = document.getElementById('todaySalesVal');
      const elExp = document.getElementById('todayShopExpenseVal');
      const elProfit = document.getElementById('todayProfitVal');
      if (elSales) elSales.textContent = Utils.formatCurrency(todayMetrics.familyIncome);
      if (elExp) elExp.textContent = Utils.formatCurrency(todayMetrics.familyExpenses);
      if (elProfit) elProfit.textContent = Utils.formatCurrency(todayMetrics.netFamilySavings);
    } else {
      if (salesCardLabel) salesCardLabel.textContent = 'আজকের মোট বিক্রি';
      if (expenseCardLabel) expenseCardLabel.textContent = 'আজকের দোকান খরচ';
      if (profitCardLabel) profitCardLabel.textContent = 'আজকের নিট লাভ';

      const elSales = document.getElementById('todaySalesVal');
      const elInc = document.getElementById('todayIncomeVal');
      const elShopExp = document.getElementById('todayShopExpenseVal');
      const elProfit = document.getElementById('todayProfitVal');
      const elHouseExp = document.getElementById('todayHouseExpenseVal');
      const elCashVal = document.getElementById('todayCashVal');

      if (elSales) elSales.textContent = Utils.formatCurrency(todayMetrics.totalSales);
      if (elInc) elInc.textContent = Utils.formatCurrency(todayMetrics.totalIncome);
      if (elShopExp) elShopExp.textContent = Utils.formatCurrency(todayMetrics.shopExpenses);
      if (elProfit) elProfit.textContent = Utils.formatCurrency(todayMetrics.businessProfit);
      if (elHouseExp) elHouseExp.textContent = Utils.formatCurrency(todayMetrics.householdExpenses);
      if (elCashVal) elCashVal.textContent = Utils.formatCurrency(todayMetrics.netCashInHand);
    }

    // 2. Banner Period Summary (Section 4)
    this.renderBannerSummary();

    // 3. Render Dashboard Recent Transactions
    this.renderDashboardRecentTransactions();

    // 4. Render Dashboard Trend Chart
    setTimeout(() => {
      this.renderDashboardTrendChart();
    }, 100);
  },

  changeBannerPeriod(period) {
    this.state.bannerPeriod = period;
    document.querySelectorAll('.banner-filter-pills .banner-pill').forEach(pill => {
      if (pill.getAttribute('data-period') === period) pill.classList.add('active');
      else pill.classList.remove('active');
    });
    this.renderBannerSummary();
  },

  renderBannerSummary() {
    const range = Accounting.getDateRangeForPeriod(this.state.bannerPeriod);
    const filteredTxs = Accounting.filterTransactions(this.state.transactions, range.start, range.end);
    const metrics = Accounting.calculateMetrics(filteredTxs, this.state.debts);

    document.getElementById('bannerPeriodTitle').textContent = `${range.label}-এর সারসংক্ষেপ`;
    document.getElementById('bannerSalesVal').textContent = Utils.formatCurrency(metrics.totalSales);
    document.getElementById('bannerIncomeVal').textContent = Utils.formatCurrency(metrics.totalIncome);
    document.getElementById('bannerShopExpVal').textContent = Utils.formatCurrency(metrics.shopExpenses);
    document.getElementById('bannerHouseExpVal').textContent = Utils.formatCurrency(metrics.householdExpenses);
    document.getElementById('bannerProfitVal').textContent = Utils.formatCurrency(metrics.businessProfit);
    document.getElementById('bannerSavingsVal').textContent = Utils.formatCurrency(metrics.netSavings);
  },

  renderDashboardRecentTransactions() {
    const container = document.getElementById('dashboardRecentTxList');
    if (!container) return;

    const recent = this.state.transactions.slice(0, 6);
    if (recent.length === 0) {
      container.innerHTML = `
        <div class="empty-state" style="padding: 1.5rem;">
          <div class="empty-icon">📝</div>
          <div class="empty-title">আজ কোনো লেনদেন হয়নি</div>
          <p class="empty-desc">নিচের বাটনে চাপ দিয়ে আজকের প্রথম হিসাব এন্ট্রি করুন</p>
          <button class="btn btn-primary btn-sm" onclick="App.openAddTransactionModal()">+ প্রথম হিসাব যোগ করুন</button>
        </div>
      `;
      return;
    }

    container.innerHTML = recent.map(tx => this.renderTransactionRowHTML(tx)).join('');
  },

  renderDashboardTrendChart() {
    // Generate daily points for the last 7 days
    const points = [];
    const today = new Date();

    for (let i = 6; i >= 0; i--) {
      const d = new Date(today);
      d.setDate(d.getDate() - i);
      const dateStr = Utils.toInputDate(d);
      const dayTxs = this.state.transactions.filter(t => t.date === dateStr);
      const m = Accounting.calculateMetrics(dayTxs);

      const dayLabel = i === 0 ? 'আজ' : (i === 1 ? 'গতকাল' : Utils.getDayNameBengali(dateStr).slice(0, 3));
      points.push({
        label: dayLabel,
        value: m.totalSales || m.totalIncome
      });
    }

    KhataCharts.drawLineTrendChart('dashboardTrendCanvas', points);
  },

  /**
   * ==========================================
   * 2. TRANSACTIONS VIEW CONTROLLER
   * ==========================================
   */
  renderTransactionsView() {
    const container = document.getElementById('fullTransactionList');
    if (!container) return;

    let list = [...this.state.transactions];

    // 1. Period filter
    if (this.state.txPeriod !== 'all') {
      const range = Accounting.getDateRangeForPeriod(this.state.txPeriod);
      list = Accounting.filterTransactions(list, range.start, range.end);
    }

    // 2. Type filter
    if (this.state.txTypeFilter !== 'ALL') {
      if (this.state.txTypeFilter === 'INCOME') {
        list = list.filter(t => t.type === 'INCOME');
      } else if (this.state.txTypeFilter === 'EXPENSE_SHOP') {
        list = list.filter(t => t.type === 'EXPENSE' && (t.expenseType === 'EXPENSE_SHOP' || (t.category && t.category.includes('shop'))));
      } else if (this.state.txTypeFilter === 'EXPENSE_HOUSE') {
        list = list.filter(t => t.type === 'EXPENSE' && (t.expenseType === 'EXPENSE_HOUSE' || (t.category && t.category.includes('house'))));
      } else if (this.state.txTypeFilter === 'SAVING') {
        list = list.filter(t => t.type === 'SAVING');
      }
    }

    // 3. Payment Method filter
    if (this.state.txPaymentFilter !== 'ALL') {
      list = list.filter(t => t.paymentMethod === this.state.txPaymentFilter);
    }

    // 4. Global Search query
    if (this.state.txSearchQuery) {
      const q = this.state.txSearchQuery.toLowerCase();
      list = list.filter(t => {
        const cat = (t.categoryName || t.category || '').toLowerCase();
        const desc = (t.description || '').toLowerCase();
        const person = (t.person || '').toLowerCase();
        const amt = String(t.amount);
        const ref = (t.refNo || '').toLowerCase();
        return cat.includes(q) || desc.includes(q) || person.includes(q) || amt.includes(q) || ref.includes(q);
      });
    }

    if (list.length === 0) {
      container.innerHTML = `
        <div class="card">
          <div class="empty-state">
            <div class="empty-icon">🔍</div>
            <div class="empty-title">কোনো লেনদেন পাওয়া যায়নি</div>
            <p class="empty-desc">ফিল্টার পরিবর্তন করুন অথবা নতুন হিসাব যুক্ত করুন</p>
            <button class="btn btn-primary" onclick="App.openAddTransactionModal()">+ নতুন হিসাব যোগ করুন</button>
          </div>
        </div>
      `;
      return;
    }

    container.innerHTML = list.map(tx => this.renderTransactionRowHTML(tx, true)).join('');
  },

  handleTxSearch(query) {
    this.state.txSearchQuery = query.trim();
    this.renderTransactionsView();
  },

  handleTxPeriodChange(period) {
    this.state.txPeriod = period;
    this.renderTransactionsView();
  },

  handleTxTypeFilterChange(type) {
    this.state.txTypeFilter = type;
    this.renderTransactionsView();
  },

  handleTxPaymentFilterChange(method) {
    this.state.txPaymentFilter = method;
    this.renderTransactionsView();
  },

  renderTransactionRowHTML(tx, showActions = false) {
    const isInc = tx.type === 'INCOME' || tx.type === 'CUSTOMER_PAYMENT' || tx.type === 'SALE';
    const isSaving = tx.type === 'SAVING';
    const isTransfer = tx.type === 'TRANSFER';
    const isShopExp = tx.expenseType === 'EXPENSE_SHOP' || tx.type === 'SUPPLIER_PAYMENT' || tx.type === 'PURCHASE' || (tx.category && tx.category.includes('shop'));
    
    let typeClass = isTransfer ? 'tx-transfer' : (isInc ? 'tx-income' : (isSaving ? 'tx-saving' : 'tx-expense'));
    let typeLabel = 'খরচ';
    let icon = '↓';
    let sign = '-';

    if (isTransfer) {
      typeLabel = 'ট্রান্সফার';
      icon = '🔁';
      sign = '↔';
    } else if (tx.type === 'CUSTOMER_PAYMENT') {
      typeLabel = 'বাকি আদায়';
      icon = '📥';
      sign = '+';
    } else if (tx.type === 'SUPPLIER_PAYMENT') {
      typeLabel = 'মহাজন পরিশোধ';
      icon = '📤';
      sign = '-';
    } else if (tx.type === 'SALE') {
      typeLabel = 'পিওএস বিক্রি';
      icon = '🛒';
      sign = '+';
    } else if (tx.type === 'PURCHASE') {
      typeLabel = 'মাল ক্রয়';
      icon = '📦';
      sign = '-';
    } else if (isInc) {
      typeLabel = tx.isSales ? 'বিক্রি' : 'আয়';
      icon = '↑';
      sign = '+';
    } else if (isSaving) {
      typeLabel = 'সঞ্চয়';
      icon = '🪙';
      sign = '-';
    } else {
      typeLabel = isShopExp ? 'দোকান খরচ' : 'সংসার খরচ';
      icon = '↓';
      sign = '-';
    }

    const paymentLabel = {
      'Cash': 'নগদ',
      'bKash': 'বিকাশ',
      'Nagad': 'নগদ ডিজিটাল',
      'Bank': 'ব্যাংক',
      'Card': 'কার্ড',
      'Other': 'অন্যান্য'
    }[tx.paymentMethod] || tx.paymentMethod || 'নগদ';

    return `
      <div class="transaction-row ${typeClass}">
        <div class="tx-left">
          <div class="tx-icon-circle">${icon}</div>
          <div class="tx-details">
            <span class="tx-category">${tx.categoryName || tx.category || 'হিসাব'}</span>
            <div class="tx-meta">
              <span>${Utils.formatDateBengali(tx.date)}</span>
              <span class="tx-badge">${typeLabel}</span>
              <span class="tx-badge">${paymentLabel}</span>
              ${tx.person ? `<span style="color: var(--text-main); font-weight: 600;">• ${tx.person}</span>` : ''}
              ${tx.description ? `<span style="color: var(--text-light);">(${tx.description})</span>` : ''}
            </div>
          </div>
        </div>

        <div class="tx-right">
          <div class="tx-amount">
            ${sign} ${Utils.formatCurrency(tx.amount)}
          </div>
          ${showActions ? `
            <div class="tx-actions">
              <button class="btn-action-icon" title="A4 ক্যাশ মেমো ও PDF" onclick="event.stopPropagation(); App.openSingleTxMemoModal('${tx.id}')">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
              </button>
              <button class="btn-action-icon" title="সম্পাদনা" onclick="event.stopPropagation(); App.editTransaction('${tx.id}')">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
              </button>
              <button class="btn-action-icon btn-delete" title="মুছে ফেলুন" onclick="event.stopPropagation(); App.deleteTransaction('${tx.id}')">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
              </button>
            </div>
          ` : ''}
        </div>
      </div>
    `;
  },

  /**
   * ==========================================
   * 3. UNIVERSAL TRANSACTION MODAL CONTROLLER
   * ==========================================
   */
  openAddTransactionModal(defaultType = 'INCOME', isSales = false) {
    document.getElementById('txId').value = '';
    document.getElementById('txModalTitle').textContent = 'হিসাব যোগ করুন';
    document.getElementById('txSaveBtn').textContent = 'সংরক্ষণ করুন ✓';
    document.getElementById('txDateInput').value = Utils.getTodayDateString();
    document.getElementById('txAmountInput').value = '';
    document.getElementById('txDescInput').value = '';
    document.getElementById('txPersonInput').value = '';
    document.getElementById('txRefInput').value = '';
    document.getElementById('txCalcBadge').classList.remove('show');

    this.selectPaymentMethod('Cash');
    this.setTxFormType(defaultType, isSales);

    UI.openModal('txModal');
  },

  setTxFormType(type, isSales = false) {
    // Update active pill
    document.querySelectorAll('.type-selector-pills .type-pill-btn').forEach(btn => {
      if (btn.getAttribute('data-txtype') === type) btn.classList.add('active');
      else btn.classList.remove('active');
    });

    // Populate categories select & quick chips
    const catSelect = document.getElementById('txCategorySelect');
    const chipsContainer = document.getElementById('txCategoryChips');
    if (catSelect) catSelect.innerHTML = '';
    if (chipsContainer) chipsContainer.innerHTML = '';

    let filterTypes = [];
    if (type === 'INCOME') filterTypes = ['INCOME_BUSINESS', 'INCOME_PERSONAL'];
    else if (type === 'EXPENSE_SHOP') filterTypes = ['EXPENSE_SHOP'];
    else if (type === 'EXPENSE_HOUSE') filterTypes = ['EXPENSE_HOUSE'];
    else if (type === 'SAVING') filterTypes = ['SAVING'];

    const filteredCats = this.state.categories.filter(c => filterTypes.includes(c.type));

    const catIcons = {
      'cat_inc_sales': '🛒',
      'cat_inc_service': '🛠️',
      'cat_inc_online': '🌐',
      'cat_inc_freelance': '💻',
      'cat_inc_commission': '🏷️',
      'cat_inc_other_biz': '💰',
      'cat_inc_personal': '👤',
      'cat_exp_stock': '📦',
      'cat_exp_salary': '👨‍💼',
      'cat_exp_rent_shop': '🏪',
      'cat_exp_elec_shop': '💡',
      'cat_exp_transport_shop': '🚚',
      'cat_exp_net_shop': '📶',
      'cat_exp_tea_shop': '☕',
      'cat_exp_repair_shop': '🔧',
      'cat_exp_other_shop': '📁',
      'cat_exp_bazar': '🥬',
      'cat_exp_rent_house': '🏠',
      'cat_exp_elec_house': '⚡',
      'cat_exp_gas': '🔥',
      'cat_exp_water': '💧',
      'cat_exp_med': '💊',
      'cat_exp_edu': '📚',
      'cat_exp_trans_house': '🚌',
      'cat_exp_food': '🍲',
      'cat_exp_mobile': '📱',
      'cat_exp_net_house': '📶',
      'cat_exp_family': '👨‍👩‍👧',
      'cat_exp_other_house': '🏡',
      'cat_saving_general': '🪙'
    };

    let selectedCatId = null;

    filteredCats.forEach((c, idx) => {
      // Add to hidden select for form submission
      const opt = document.createElement('option');
      opt.value = c.id;
      opt.textContent = c.name;
      if ((isSales && c.isSales) || (!isSales && idx === 0)) {
        opt.selected = true;
        selectedCatId = c.id;
      }
      catSelect.appendChild(opt);

      // Add visual clickable touch chip
      if (chipsContainer) {
        const chip = document.createElement('div');
        const icon = catIcons[c.id] || '🏷️';
        chip.className = `cat-chip ${c.id === selectedCatId ? 'active' : ''}`;
        chip.setAttribute('data-catid', c.id);
        chip.innerHTML = `<span>${icon}</span> <span>${c.name}</span>`;
        chip.onclick = () => this.selectCategory(c.id);
        chipsContainer.appendChild(chip);
      }
    });

    // If empty (e.g. saving), add generic
    if (filteredCats.length === 0) {
      const opt = document.createElement('option');
      opt.value = 'cat_saving_general';
      opt.textContent = 'সাধারণ সঞ্চয়';
      opt.selected = true;
      catSelect.appendChild(opt);

      if (chipsContainer) {
        const chip = document.createElement('div');
        chip.className = 'cat-chip active';
        chip.setAttribute('data-catid', 'cat_saving_general');
        chip.innerHTML = `<span>🪙</span> <span>সাধারণ সঞ্চয়</span>`;
        chipsContainer.appendChild(chip);
      }
    }
  },

  selectCategory(catId) {
    const catSelect = document.getElementById('txCategorySelect');
    if (catSelect) catSelect.value = catId;

    document.querySelectorAll('#txCategoryChips .cat-chip').forEach(chip => {
      if (chip.getAttribute('data-catid') === catId) chip.classList.add('active');
      else chip.classList.remove('active');
    });
  },

  addPresetAmount(val) {
    const input = document.getElementById('txAmountInput');
    if (!input) return;

    const current = input.value.trim();
    const parsed = Utils.parseSmartAmount(current);

    if (!parsed.valid || parsed.value === 0) {
      input.value = String(val);
    } else {
      input.value = String(parsed.value + val);
    }

    this.handleAmountCalcInput(input.value);
    input.focus();
  },

  clearAmount() {
    const input = document.getElementById('txAmountInput');
    if (input) {
      input.value = '';
      this.handleAmountCalcInput('');
      input.focus();
    }
  },

  handleCalculatorKey(key, inputId = 'txAmountInput', badgeId = 'txCalcBadge') {
    const input = document.getElementById(inputId);
    if (!input) return;

    let cur = input.value ? String(input.value) : '';

    if (key === 'C') {
      input.value = '';
      this.handleAmountCalcInput('', badgeId);
      input.focus();
      return;
    }

    if (key === 'BACK' || key === 'BACKSPACE') {
      input.value = cur.slice(0, -1);
      this.handleAmountCalcInput(input.value, badgeId);
      input.focus();
      return;
    }

    if (key === '=') {
      if (cur.trim().length > 0) {
        const parsed = Utils.parseSmartAmount(cur);
        if (parsed.valid && parsed.value >= 0) {
          input.value = String(parsed.value);
        }
      }
      this.handleAmountCalcInput(input.value, badgeId);
      input.focus();
      return;
    }

    // Operators: +, -, *, /
    if (['+', '-', '*', '/'].includes(key)) {
      if (!cur) {
        if (key === '-') {
          input.value = '-';
        }
        return;
      }
      const lastChar = cur.slice(-1);
      if (['+', '-', '*', '/'].includes(lastChar)) {
        input.value = cur.slice(0, -1) + key;
      } else {
        input.value = cur + key;
      }
      this.handleAmountCalcInput(input.value, badgeId);
      input.focus();
      return;
    }

    // Number or dot
    if (key === '.') {
      const parts = cur.split(/[\+\-\*\/]/);
      const lastPart = parts[parts.length - 1];
      if (lastPart && lastPart.includes('.')) return;
      input.value = cur + '.';
    } else {
      input.value = cur + key;
    }

    this.handleAmountCalcInput(input.value, badgeId);
    input.focus();
  },

  selectPaymentMethod(method) {
    document.getElementById('txPaymentMethodInput').value = method;
    document.querySelectorAll('.payment-methods-grid .payment-method-tile').forEach(tile => {
      if (tile.getAttribute('data-method') === method) tile.classList.add('active');
      else tile.classList.remove('active');
    });
  },

  handleAmountCalcInput(val, badgeId = 'txCalcBadge') {
    const badge = document.getElementById(badgeId);
    if (!badge) return;

    if (!val || val.trim().length === 0) {
      badge.classList.remove('show');
      return;
    }

    const parsed = Utils.parseSmartAmount(val);
    if (parsed.valid && parsed.expression && /[\+\-\*\/]/.test(parsed.expression)) {
      badge.textContent = `= ৳ ${Utils.toBanglaNumber(parsed.value)}`;
      badge.classList.add('show');
    } else {
      badge.classList.remove('show');
    }
  },

  async handleSaveTransaction(e) {
    e.preventDefault();

    const rawAmount = document.getElementById('txAmountInput').value;
    const parsedAmount = Utils.parseSmartAmount(rawAmount);

    if (!parsedAmount.valid || parsedAmount.value <= 0) {
      UI.toast('সঠিক টাকার পরিমাণ লিখুন!', 'error');
      document.getElementById('txAmountInput').focus();
      return;
    }

    const activeTypeBtn = document.querySelector('.type-selector-pills .type-pill-btn.active');
    const selectedFormType = activeTypeBtn ? activeTypeBtn.getAttribute('data-txtype') : 'INCOME';

    let txType = 'INCOME';
    let expenseType = null;

    if (selectedFormType === 'INCOME') {
      txType = 'INCOME';
    } else if (selectedFormType === 'EXPENSE_SHOP') {
      txType = 'EXPENSE';
      expenseType = 'EXPENSE_SHOP';
    } else if (selectedFormType === 'EXPENSE_HOUSE') {
      txType = 'EXPENSE';
      expenseType = 'EXPENSE_HOUSE';
    } else if (selectedFormType === 'SAVING') {
      txType = 'SAVING';
    }

    const catSelect = document.getElementById('txCategorySelect');
    const catId = catSelect.value;
    const catName = catSelect.options[catSelect.selectedIndex] ? catSelect.options[catSelect.selectedIndex].text : '';

    const isSales = catId === 'cat_inc_sales' || catId === 'cat_inc_online';

    const txId = document.getElementById('txId').value || Utils.generateId('tx');
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const txRecord = {
      id: txId,
      accountId: activeAccId,
      type: txType,
      expenseType: expenseType,
      amount: parsedAmount.value,
      category: catId,
      categoryName: catName,
      isSales: isSales,
      date: document.getElementById('txDateInput').value,
      paymentMethod: document.getElementById('txPaymentMethodInput').value || 'Cash',
      person: document.getElementById('txPersonInput').value.trim(),
      description: document.getElementById('txDescInput').value.trim(),
      refNo: document.getElementById('txRefInput').value.trim()
    };

    try {
      await khataDB.put('transactions', txRecord);
      UI.toast('হিসাব সফলভাবে সংরক্ষণ হয়েছে ✓', 'success');
      UI.closeModal('txModal');
      await this.refreshAllData();
      this.renderCurrentView();
    } catch (err) {
      console.error('Save tx error:', err);
      UI.toast('ডাটা সংরক্ষণ করা যায়নি। আবার চেষ্টা করুন।', 'error');
    }
  },

  async editTransaction(id) {
    const tx = await khataDB.get('transactions', id);
    if (!tx) return;

    document.getElementById('txId').value = tx.id;
    document.getElementById('txModalTitle').textContent = 'হিসাব সম্পাদনা করুন';
    document.getElementById('txSaveBtn').textContent = 'আপডেট করুন ✓';
    document.getElementById('txDateInput').value = tx.date;
    document.getElementById('txAmountInput').value = tx.amount;
    document.getElementById('txDescInput').value = tx.description || '';
    document.getElementById('txPersonInput').value = tx.person || '';
    document.getElementById('txRefInput').value = tx.refNo || '';

    let formType = 'INCOME';
    if (tx.type === 'EXPENSE') {
      formType = tx.expenseType === 'EXPENSE_SHOP' ? 'EXPENSE_SHOP' : 'EXPENSE_HOUSE';
    } else if (tx.type === 'SAVING') {
      formType = 'SAVING';
    }

    this.setTxFormType(formType, tx.isSales);
    if (tx.category) document.getElementById('txCategorySelect').value = tx.category;
    this.selectPaymentMethod(tx.paymentMethod || 'Cash');

    UI.openModal('txModal');
  },

  deleteTransaction(id) {
    const txToDelete = (this.state.transactions || []).find(t => t.id === id);
    if (!txToDelete) return;

    UI.confirm({
      title: 'হিসাব মুছে ফেলবেন?',
      message: 'আপনি কি নিশ্চিত যে এই লেনদেনটি মুছে ফেলতে চান?',
      confirmText: 'মুছে ফেলুন',
      onConfirm: async () => {
        await khataDB.delete('transactions', id);
        await this.refreshAllData();
        this.renderCurrentView();

        UI.undoToast('লেনদেনটি মুছে ফেলা হয়েছে', async () => {
          await khataDB.put('transactions', txToDelete);
          await this.refreshAllData();
          this.renderCurrentView();
          UI.toast('লেনদেনটি সফলভাবে পুনরুদ্ধার (Undo) করা হয়েছে ✓', 'success');
        }, 6000);
      }
    });
  },

  /**
   * ==========================================
   * 4. DEBT MANAGEMENT VIEW CONTROLLER (ধার-বাকি)
   * ==========================================
   */
  renderDebtsView() {
    const metrics = Accounting.calculateMetrics(this.state.transactions, this.state.debts);

    document.getElementById('debtTotalReceivable').textContent = Utils.formatCurrency(metrics.totalReceivable);
    document.getElementById('debtTotalPayable').textContent = Utils.formatCurrency(metrics.totalPayable);
    document.getElementById('debtNetPosition').textContent = Utils.formatCurrency(metrics.netDebtPosition);

    const recCount = this.state.debts.filter(d => d.type === 'RECEIVABLE').length;
    const payCount = this.state.debts.filter(d => d.type === 'PAYABLE').length;

    document.getElementById('receivableCountBadge').textContent = Utils.toBanglaNumber(recCount, false);
    document.getElementById('payableCountBadge').textContent = Utils.toBanglaNumber(payCount, false);

    // Filter by tab
    const filteredDebts = this.state.debts.filter(d => d.type === this.state.debtTab);
    const container = document.getElementById('debtCardsContainer');
    if (!container) return;

    if (filteredDebts.length === 0) {
      container.innerHTML = `
        <div class="card" style="grid-column: 1 / -1;">
          <div class="empty-state">
            <div class="empty-icon">🤝</div>
            <div class="empty-title">কোনো ধার-বাকি হিসাব নেই</div>
            <p class="empty-desc">কাস্টমার বা মহাজনের নতুন বাকি হিসাব যুক্ত করুন</p>
            <button class="btn btn-primary" onclick="App.openNewDebtModal()">+ নতুন ধার যুক্ত করুন</button>
          </div>
        </div>
      `;
      return;
    }

    container.innerHTML = filteredDebts.map(d => this.renderDebtCardHTML(d)).join('');
  },

  switchDebtTab(tabType) {
    this.state.debtTab = tabType;
    const recBtn = document.getElementById('tabReceivableBtn');
    const payBtn = document.getElementById('tabPayableBtn');

    if (tabType === 'RECEIVABLE') {
      recBtn.className = 'btn btn-outline active';
      payBtn.className = 'btn btn-secondary';
    } else {
      recBtn.className = 'btn btn-secondary';
      payBtn.className = 'btn btn-outline active';
    }

    this.renderDebtsView();
  },

  renderDebtCardHTML(d) {
    const remaining = d.remainingAmount !== undefined ? d.remainingAmount : (d.amount - (d.paidAmount || 0));
    const paid = d.paidAmount || 0;
    const total = d.amount || 0;
    const pct = total > 0 ? Math.min(100, Math.round((paid / total) * 100)) : 0;
    const isSupplier = d.type === 'PAYABLE';

    let statusTag = `<span class="debt-status-tag status-pending">বাকি রয়েছে</span>`;
    if (remaining <= 0) {
      statusTag = `<span class="debt-status-tag status-paid">পরিশোধিত ✓</span>`;
    } else if (paid > 0) {
      statusTag = `<span class="debt-status-tag status-partial">আংশিক জমা (${Utils.toBanglaNumber(pct)}%)</span>`;
    }

    const cleanPhone = (d.phone || '').replace(/[^0-9]/g, '');

    return `
      <div class="debt-person-card">
        <div class="debt-person-header">
          <div>
            <div style="display: flex; align-items: center; gap: 6px; flex-wrap: wrap;">
              <div class="debt-person-name">${d.person}</div>
              ${isSupplier ? `<span class="supplier-badge">🏪 মহাজন / সাপ্লায়ার</span>` : ''}
              ${d.promiseDate ? `<span class="promise-date-tag" title="টাকা দেওয়ার ওয়াদা তারিখ">📅 ওয়াদা: ${Utils.formatDateBengali(d.promiseDate)}</span>` : ''}
            </div>
            <div class="debt-person-phone">${d.phone || 'মোবাইল নম্বর নেই'}</div>
            ${cleanPhone ? `
              <div style="display: flex; gap: 0.35rem; margin-top: 0.35rem; flex-wrap: wrap;">
                <a href="tel:${cleanPhone}" class="debt-contact-btn" title="সরাসরি কল দিন">📞 কল</a>
                <button type="button" class="debt-contact-btn" onclick="App.openPoliteReminderModal('${d.id}')" title="মিষ্টি ভাষার তাগাদা নোটিশ">💌 তাগাদা</button>
                <button type="button" class="debt-contact-btn" onclick="App.openCustomerPassbookModal('${d.id}')" title="ডিজিটাল পাসবুক দেখুন">📱 পাসবুক</button>
              </div>
            ` : ''}
          </div>
          ${statusTag}
        </div>

        <div class="debt-amounts-grid">
          <div class="debt-amt-box">
            <span>${isSupplier ? 'মোট কেনা চালান' : 'মোট বিক্রি/ধার'}</span>
            <strong>${Utils.formatCurrency(total)}</strong>
          </div>
          <div class="debt-amt-box">
            <span>${isSupplier ? 'পরিশোধ করেছি' : 'জমা পেয়েছি'}</span>
            <strong style="color: var(--color-income);">${Utils.formatCurrency(paid)}</strong>
          </div>
          <div class="debt-amt-box debt-remaining">
            <span>${isSupplier ? 'মহাজন পাবে' : 'বাকি পাওনা'}</span>
            <strong style="${isSupplier ? 'color: #7C3AED;' : ''}">${Utils.formatCurrency(remaining)}</strong>
          </div>
        </div>

        <div class="debt-progress-bar-wrap" title="পরিশোধ ${pct}%">
          <div class="debt-progress-bar-fill" style="width: ${pct}%; ${isSupplier ? 'background: linear-gradient(90deg, #8B5CF6, #6D28D9);' : ''}"></div>
        </div>

        ${d.dueDate ? `
          <div style="font-size: 0.76rem; color: var(--text-muted); display: flex; align-items: center; gap: 0.35rem;">
            <span>তারিখ: ${Utils.formatDateBengali(d.date)}</span> • 
            <span style="color: #D97706;">মেয়াদ: ${Utils.formatDateBengali(d.dueDate)}</span>
          </div>
        ` : `<div style="font-size: 0.76rem; color: var(--text-muted);">তারিখ: ${Utils.formatDateBengali(d.date)}</div>`}

        <div class="debt-card-actions">
          <button class="btn btn-primary btn-sm" style="flex: 1;" onclick="App.openPartialPaymentModal('${d.id}')">
            ${isSupplier ? 'টাকা দিন' : 'টাকা জমা নিন'}
          </button>
          <button class="btn btn-secondary btn-sm" onclick="App.openPersonLedger('${d.id}')" title="খতিয়ান দেখুন">
            খতিয়ান
          </button>
          <button class="btn btn-accent btn-sm" onclick="App.openCustomerMemoModal('${d.id}')" title="A4 মেমো প্রিন্ট ও PDF ডাউনলোড" style="padding: 4px 8px;">
            📄 মেমো
          </button>
          <button class="btn btn-outline btn-sm" onclick="App.openCustomerPassbookModal('${d.id}')" title="ডিজিটাল পাসবুক" style="padding: 4px 8px;">
            📱 পাসবুক
          </button>
        </div>
      </div>
    `;
  },

  openNewDebtModal(debtId = null) {
    document.getElementById('editDebtId').value = debtId || '';
    document.getElementById('newDebtPersonInput').value = '';
    document.getElementById('newDebtPhoneInput').value = '';
    document.getElementById('newDebtAmountInput').value = '';
    document.getElementById('newDebtPaidInput').value = '0';
    document.getElementById('newDebtDateInput').value = Utils.getTodayDateString();
    document.getElementById('newDebtDueDateInput').value = '';
    document.getElementById('newDebtNoteInput').value = '';

    this.setNewDebtType(this.state.debtTab || 'RECEIVABLE');
    UI.openModal('newDebtModal');
  },

  setNewDebtType(type) {
    document.getElementById('newDebtTypeInput').value = type;
    const recBtn = document.getElementById('debtTypeRecBtn');
    const payBtn = document.getElementById('debtTypePayBtn');
    if (type === 'RECEIVABLE') {
      recBtn.classList.add('active');
      payBtn.classList.remove('active');
    } else {
      payBtn.classList.add('active');
      recBtn.classList.remove('active');
    }
  },

  async handleSaveNewDebt(e) {
    e.preventDefault();

    const person = document.getElementById('newDebtPersonInput').value.trim();
    const rawAmount = document.getElementById('newDebtAmountInput').value;
    const parsedAmount = Utils.parseSmartAmount(rawAmount);

    if (!parsedAmount.valid || parsedAmount.value <= 0) {
      UI.toast('সঠিক টাকার পরিমাণ দিন!', 'error');
      return;
    }

    const paid = Number(document.getElementById('newDebtPaidInput').value) || 0;
    const remaining = parsedAmount.value - paid;
    const type = document.getElementById('newDebtTypeInput').value;

    const id = document.getElementById('editDebtId').value || Utils.generateId('dbt');
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const debtObj = {
      id,
      accountId: activeAccId,
      type,
      person,
      phone: document.getElementById('newDebtPhoneInput').value.trim(),
      amount: parsedAmount.value,
      paidAmount: paid,
      remainingAmount: Math.max(0, remaining),
      date: document.getElementById('newDebtDateInput').value,
      dueDate: document.getElementById('newDebtDueDateInput').value,
      note: document.getElementById('newDebtNoteInput').value.trim(),
      status: remaining <= 0 ? 'PAID' : (paid > 0 ? 'PARTIAL' : 'PENDING')
    };

    await khataDB.put('debts', debtObj);

    // If initial paid > 0, log a payment
    if (paid > 0) {
      await khataDB.put('debtPayments', {
        id: Utils.generateId('pmt'),
        debtId: id,
        accountId: activeAccId,
        person,
        amount: paid,
        date: debtObj.date,
        paymentMethod: 'Cash',
        note: 'প্রারম্ভিক নগদ জমা'
      });
    }

    UI.toast('ধার-বাকি হিসাব সংরক্ষিত হয়েছে ✓', 'success');
    UI.closeModal('newDebtModal');
    await this.refreshAllData();
    this.renderDebtsView();
  },

  openPartialPaymentModal(debtId) {
    const debt = this.state.debts.find(d => d.id === debtId);
    if (!debt) return;

    document.getElementById('pmtDebtId').value = debt.id;
    document.getElementById('pmtPersonName').textContent = debt.person;
    document.getElementById('pmtRemainingBalance').textContent = Utils.formatCurrency(debt.remainingAmount);
    document.getElementById('pmtAmountInput').value = '';
    document.getElementById('pmtDateInput').value = Utils.getTodayDateString();
    document.getElementById('pmtNoteInput').value = '';

    UI.openModal('debtPaymentModal');
  },

  async handleSaveDebtPayment(e) {
    e.preventDefault();

    const debtId = document.getElementById('pmtDebtId').value;
    const debt = this.state.debts.find(d => d.id === debtId);
    if (!debt) return;

    const rawAmt = document.getElementById('pmtAmountInput').value;
    const parsedAmt = Utils.parseSmartAmount(rawAmt);

    if (!parsedAmt.valid || parsedAmt.value <= 0) {
      UI.toast('সঠিক পরিশোধের পরিমাণ লিখুন!', 'error');
      return;
    }

    const payAmount = parsedAmt.value;
    const newPaid = (debt.paidAmount || 0) + payAmount;
    const newRemaining = Math.max(0, debt.amount - newPaid);

    debt.paidAmount = newPaid;
    debt.remainingAmount = newRemaining;
    debt.status = newRemaining <= 0 ? 'PAID' : 'PARTIAL';

    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : (debt.accountId || 'acc_rafiqul_main');

    // Update debt
    await khataDB.put('debts', debt);

    // Log payment history
    const pmtMethod = document.getElementById('pmtMethodSelect').value;
    const pmtNote = document.getElementById('pmtNoteInput').value.trim();

    await khataDB.put('debtPayments', {
      id: Utils.generateId('pmt'),
      debtId: debt.id,
      accountId: activeAccId,
      person: debt.person,
      amount: payAmount,
      date: document.getElementById('pmtDateInput').value,
      paymentMethod: pmtMethod,
      note: pmtNote || 'আংশিক কিস্তি পরিশোধ'
    });

    // Also auto-record a transaction for cashflow balance!
    // If Receivable (আমি পাব), getting money = INCOME / CASH IN
    // If Payable (আমি দেব), paying money = EXPENSE / CASH OUT
    if (debt.type === 'RECEIVABLE') {
      await khataDB.put('transactions', {
        id: Utils.generateId('tx'),
        accountId: activeAccId,
        type: 'INCOME',
        isSales: false,
        amount: payAmount,
        category: 'cat_inc_other_biz',
        categoryName: 'বাকি আদায় (' + debt.person + ')',
        date: document.getElementById('pmtDateInput').value,
        paymentMethod: pmtMethod,
        person: debt.person,
        description: 'কাস্টমার বাকি পরিশোধ: ' + (pmtNote || '')
      });
    }

    UI.toast(`৳ ${Utils.toBanglaNumber(payAmount)} পরিশোধ সংরক্ষণ হয়েছে ✓`, 'success');
    UI.closeModal('debtPaymentModal');
    await this.refreshAllData();
    this.renderDebtsView();
  },

  async openPersonLedger(debtId) {
    const debt = this.state.debts.find(d => d.id === debtId);
    if (!debt) return;

    document.getElementById('ledgerPersonName').textContent = `${debt.person} — খতিয়ান`;
    document.getElementById('ledgerPersonPhone').textContent = `মোবাইল: ${debt.phone || 'নেই'}`;
    document.getElementById('ledgerTotalAmt').textContent = Utils.formatCurrency(debt.amount);
    document.getElementById('ledgerPaidAmt').textContent = Utils.formatCurrency(debt.paidAmount || 0);
    document.getElementById('ledgerRemainingAmt').textContent = Utils.formatCurrency(debt.remainingAmount);

    const payments = await khataDB.getPaymentsForDebt(debt.id);
    const container = document.getElementById('ledgerTimelineList');

    if (payments.length === 0) {
      container.innerHTML = `
        <div style="padding: 1.5rem; text-align: center; color: var(--text-muted);">
          এখনো কোনো পরিশোধের এন্ট্রি নেই।
        </div>
      `;
    } else {
      container.innerHTML = payments.map(p => `
        <div class="transaction-row tx-income" style="padding: 0.65rem 1rem;">
          <div class="tx-left">
            <div class="tx-icon-circle" style="width: 32px; height: 32px; font-size: 0.9rem;">✓</div>
            <div class="tx-details">
              <span class="tx-category">${p.note || 'পরিশোধ জমা'}</span>
              <div class="tx-meta">
                <span>${Utils.formatDateBengali(p.date)}</span>
                <span class="tx-badge">${p.paymentMethod}</span>
              </div>
            </div>
          </div>
          <div class="tx-amount" style="font-size: 1rem; color: #059669;">
            + ${Utils.formatCurrency(p.amount)}
          </div>
        </div>
      `).join('');
    }

    document.getElementById('ledgerAddPaymentBtn').onclick = () => {
      UI.closeModal('personLedgerModal');
      this.openPartialPaymentModal(debt.id);
    };

    const memoBtn = document.getElementById('ledgerPrintMemoBtn');
    if (memoBtn) {
      memoBtn.onclick = () => {
        UI.closeModal('personLedgerModal');
        this.openCustomerMemoModal(debt.id);
      };
    }

    const newMemoWithDueBtn = document.getElementById('ledgerNewMemoWithOldDueBtn');
    if (newMemoWithDueBtn) {
      newMemoWithDueBtn.onclick = () => {
        UI.closeModal('personLedgerModal');
        this.openCreateMemoModal(debt.person);
      };
    }

    document.getElementById('ledgerDeleteDebtBtn').onclick = () => {
      UI.confirm({
        title: 'ধার মুছে ফেলবেন?',
        message: `${debt.person}-এর সম্পূর্ণ ধার ও খতিয়ান মুছে ফেলা হবে।`,
        onConfirm: async () => {
          await khataDB.delete('debts', debt.id);
          UI.closeModal('personLedgerModal');
          UI.toast('ধার মুছে ফেলা হয়েছে', 'info');
          await this.refreshAllData();
          this.renderDebtsView();
        }
      });
    };

    UI.openModal('personLedgerModal');
  },

  /**
   * ==========================================
   * 5. REPORTS VIEW CONTROLLER
   * ==========================================
   */
  renderReportsView() {
    const range = Accounting.getDateRangeForPeriod(this.state.reportPeriod);
    const filteredTxs = Accounting.filterTransactions(this.state.transactions, range.start, range.end);
    const metrics = Accounting.calculateMetrics(filteredTxs, this.state.debts);

    document.getElementById('statementHeaderTitle').textContent = `আয়-ব্যয় ও লাভ বিবরণী (${range.label})`;
    document.getElementById('statementDateRange').textContent = `${Utils.formatDateBengali(range.start)} হতে ${Utils.formatDateBengali(range.end)}`;

    // P&L Statement Table Values
    document.getElementById('repSalesVal').textContent = Utils.formatCurrency(metrics.totalSales);
    document.getElementById('repShopExpVal').textContent = Utils.formatCurrency(metrics.shopExpenses);
    document.getElementById('repProfitVal').textContent = Utils.formatCurrency(metrics.businessProfit);
    document.getElementById('repPersonalIncVal').textContent = Utils.formatCurrency(metrics.personalIncome);
    document.getElementById('repHouseExpVal').textContent = Utils.formatCurrency(metrics.householdExpenses);
    document.getElementById('repSavingsVal').textContent = Utils.formatCurrency(metrics.netSavings);

    // Render Charts
    setTimeout(() => {
      this.renderReportCharts(filteredTxs, metrics);
    }, 100);

    // Render Category Analytics (Section 22)
    this.renderCategoryAnalytics(metrics.topCategories);
  },

  handleReportPeriodChange(period) {
    this.state.reportPeriod = period;
    this.renderReportsView();
  },

  renderReportCharts(transactions, metrics) {
    // 1. Comparison Bar Chart (Income vs Shop Expense vs House Expense)
    const barData = [
      {
        label: 'আয় ও খরচ তুলনা',
        income: metrics.totalIncome,
        shopExpense: metrics.shopExpenses,
        houseExpense: metrics.householdExpenses
      }
    ];
    KhataCharts.drawBarChart('reportsBarChartCanvas', barData);

    // 2. Donut Chart for Expense Categories
    KhataCharts.drawDonutChart('reportsDonutChartCanvas', metrics.topCategories);
  },

  renderCategoryAnalytics(categories) {
    const container = document.getElementById('categoryAnalyticsList');
    if (!container) return;

    if (!categories || categories.length === 0) {
      container.innerHTML = `
        <div style="text-align: center; color: var(--text-muted); padding: 1.5rem;">
          এই সময়কালের কোনো খরচ পাওয়া যায়নি।
        </div>
      `;
      return;
    }

    container.innerHTML = categories.slice(0, 8).map(c => `
      <div style="display: flex; flex-direction: column; gap: 0.35rem;">
        <div style="display: flex; justify-content: space-between; font-size: 0.92rem; font-weight: 700;">
          <span>${c.name}</span>
          <span>${Utils.formatCurrency(c.amount)} (${Utils.toBanglaNumber(c.percentage)}%)</span>
        </div>
        <div class="debt-progress-bar-wrap">
          <div class="debt-progress-bar-fill" style="width: ${c.percentage}%; background-color: ${c.type === 'EXPENSE_SHOP' ? '#F59E0B' : '#EF4444'};"></div>
        </div>
      </div>
    `).join('');
  },

  renderActiveCharts() {
    if (this.state.currentView === 'dashboard') {
      this.renderDashboardTrendChart();
    } else if (this.state.currentView === 'reports') {
      const range = Accounting.getDateRangeForPeriod(this.state.reportPeriod);
      const filteredTxs = Accounting.filterTransactions(this.state.transactions, range.start, range.end);
      const metrics = Accounting.calculateMetrics(filteredTxs, this.state.debts);
      this.renderReportCharts(filteredTxs, metrics);
    }
  },

  /**
   * ==========================================
   * 6. BUDGET VIEW CONTROLLER (Section 18)
   * ==========================================
   */
  renderBudgetView() {
    const container = document.getElementById('budgetsCardsContainer');
    if (!container) return;

    const currentMonthRange = Accounting.getDateRangeForPeriod('thisMonth');
    const monthlyTxs = Accounting.filterTransactions(this.state.transactions, currentMonthRange.start, currentMonthRange.end);

    if (this.state.budgets.length === 0) {
      container.innerHTML = `
        <div class="card" style="grid-column: 1 / -1;">
          <div class="empty-state">
            <div class="empty-icon">📊</div>
            <div class="empty-title">কোনো বাজেট সেট করা নেই</div>
            <p class="empty-desc">খরচ নিয়ন্ত্রণে রাখতে দোকান বা সংসার খরচের বাজেট তৈরি করুন</p>
            <button class="btn btn-primary" onclick="App.openAddBudgetModal()">+ বাজেট সেট করুন</button>
          </div>
        </div>
      `;
      return;
    }

    container.innerHTML = this.state.budgets.map(b => {
      const evaluation = Accounting.evaluateBudget(b, monthlyTxs);
      const isShop = b.type === 'EXPENSE_SHOP';

      let alertBadge = '';
      if (evaluation.status === 'exceeded') {
        alertBadge = `<span class="budget-alert-badge exceeded">⚠️ বাজেট অতিক্রম করেছে (${Utils.toBanglaNumber(evaluation.actualPercent)}%)</span>`;
      } else if (evaluation.status === 'warning') {
        alertBadge = `<span class="budget-alert-badge warning"> সতর্কবার্তা: ৮০% ব্যয় হয়েছে</span>`;
      }

      return `
        <div class="budget-card budget-status-${evaluation.status}">
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <div>
              <strong style="font-size: 1.1rem; color: var(--text-main);">${b.name || (isShop ? 'দোকানের খরচ বাজেট' : 'সংসার খরচ বাজেট')}</strong>
              <div style="font-size: 0.78rem; color: var(--text-muted);">${currentMonthRange.label}</div>
            </div>
            ${alertBadge}
          </div>

          <div style="display: flex; justify-content: space-between; font-size: 0.95rem; margin-top: 0.25rem;">
            <span>ব্যয় হয়েছে: <strong style="color: var(--color-expense);">${Utils.formatCurrency(evaluation.usedAmount)}</strong></span>
            <span>অবশিষ্ট: <strong style="color: var(--primary-800);">${Utils.formatCurrency(evaluation.remainingAmount)}</strong></span>
          </div>

          <div class="budget-progress-track">
            <div class="budget-progress-fill" style="width: ${evaluation.percentUsed}%;"></div>
          </div>

          <div style="display: flex; justify-content: space-between; font-size: 0.82rem; color: var(--text-muted); margin-top: auto;">
            <span>মোট বাজেট: ${Utils.formatCurrency(evaluation.targetAmount)}</span>
            <span>${Utils.toBanglaNumber(evaluation.actualPercent)}% ব্যবহৃত</span>
          </div>
        </div>
      `;
    }).join('');
  },

  openAddBudgetModal() {
    const now = new Date();
    const currentMonth = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`;
    document.getElementById('budgetId').value = '';
    document.getElementById('budgetAmountInput').value = '';
    document.getElementById('budgetMonthInput').value = currentMonth;
    UI.openModal('budgetModal');
  },

  async handleSaveBudget(e) {
    e.preventDefault();
    const type = document.getElementById('budgetTypeSelect').value;
    const rawAmt = document.getElementById('budgetAmountInput').value;
    const parsed = Utils.parseSmartAmount(rawAmt);

    if (!parsed.valid || parsed.value <= 0) {
      UI.toast('সঠিক টাকার পরিমাণ দিন!', 'error');
      return;
    }

    const month = document.getElementById('budgetMonthInput').value;
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';
    const id = document.getElementById('budgetId').value || `bgt_${activeAccId}_${month}_${type}`;
    const name = type === 'EXPENSE_SHOP' ? 'দোকানের খরচ বাজেট' : 'সংসার খরচ বাজেট';

    await khataDB.put('budgets', {
      id,
      accountId: activeAccId,
      month,
      type,
      name,
      amount: parsed.value
    });

    UI.toast('বাজেট সংরক্ষণ করা হয়েছে ✓', 'success');
    UI.closeModal('budgetModal');
    await this.refreshAllData();
    this.renderBudgetView();
  },

  /**
   * ==========================================
   * 7. SAVING GOALS VIEW CONTROLLER (Section 19)
   * ==========================================
   */
  renderSavingGoalsView() {
    const container = document.getElementById('savingGoalsCardsContainer');
    if (!container) return;

    if (this.state.savingGoals.length === 0) {
      container.innerHTML = `
        <div class="card" style="grid-column: 1 / -1;">
          <div class="empty-state">
            <div class="empty-icon">🪙</div>
            <div class="empty-title">কোনো সঞ্চয় লক্ষ্য যুক্ত নেই</div>
            <p class="empty-desc">ভবিষ্যতের জন্য যেমন কম্পিউটার, ডেকোরেশন বা হজের জন্য সঞ্চয় শুরু করুন</p>
            <button class="btn btn-primary" onclick="App.openAddSavingGoalModal()">+ নতুন সঞ্চয় লক্ষ্য</button>
          </div>
        </div>
      `;
      return;
    }

    container.innerHTML = this.state.savingGoals.map(g => {
      const target = Number(g.targetAmount) || 0;
      const saved = Number(g.savedAmount) || 0;
      const remaining = Math.max(0, target - saved);
      const pct = target > 0 ? Math.min(100, Math.round((saved / target) * 100)) : 0;

      return `
        <div class="saving-goal-card">
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <div class="goal-title">${g.title}</div>
            <span class="nav-badge" style="background: var(--primary-100); color: var(--primary-800);">${Utils.toBanglaNumber(pct)}% সম্পন্ন</span>
          </div>

          <div class="debt-progress-bar-wrap" style="height: 8px;">
            <div class="debt-progress-bar-fill" style="width: ${pct}%; background: var(--color-savings);"></div>
          </div>

          <div class="goal-stats-row">
            <span>জমা হয়েছে: <strong style="color: var(--color-savings);">${Utils.formatCurrency(saved)}</strong></span>
            <span>অবশিষ্ট: <strong style="color: var(--text-main);">${Utils.formatCurrency(remaining)}</strong></span>
          </div>

          <div style="font-size: 0.8rem; color: var(--text-muted);">
            টার্গেট: ${Utils.formatCurrency(target)} ${g.targetDate ? `• মেয়াদ: ${Utils.formatDateBengali(g.targetDate)}` : ''}
          </div>

          <div style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
            <button class="btn btn-primary btn-sm" style="flex: 1;" onclick="App.depositToSavingGoal('${g.id}')">
              + টাকা জমা করুন
            </button>
            <button class="btn btn-danger btn-sm" onclick="App.deleteSavingGoal('${g.id}')">
              মুছুন
            </button>
          </div>
        </div>
      `;
    }).join('');
  },

  openAddSavingGoalModal() {
    document.getElementById('goalId').value = '';
    document.getElementById('goalTitleInput').value = '';
    document.getElementById('goalTargetInput').value = '';
    document.getElementById('goalSavedInput').value = '0';
    document.getElementById('goalDateInput').value = '';
    UI.openModal('savingGoalModal');
  },

  async handleSaveSavingGoal(e) {
    e.preventDefault();

    const title = document.getElementById('goalTitleInput').value.trim();
    const rawTarget = document.getElementById('goalTargetInput').value;
    const parsedTarget = Utils.parseSmartAmount(rawTarget);

    if (!parsedTarget.valid || parsedTarget.value <= 0) {
      UI.toast('টার্গেট টাকার পরিমাণ সঠিক নয়!', 'error');
      return;
    }

    const saved = Number(document.getElementById('goalSavedInput').value) || 0;
    const target = parsedTarget.value;
    const remaining = Math.max(0, target - saved);
    const progressPercent = target > 0 ? (saved / target) * 100 : 0;

    const id = document.getElementById('goalId').value || Utils.generateId('goal');
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    await khataDB.put('savingGoals', {
      id,
      accountId: activeAccId,
      title,
      targetAmount: target,
      savedAmount: saved,
      remainingAmount: remaining,
      progressPercent: Math.round(progressPercent * 100) / 100,
      targetDate: document.getElementById('goalDateInput').value,
      status: 'ACTIVE'
    });

    UI.toast('সঞ্চয় লক্ষ্য সংরক্ষিত হয়েছে ✓', 'success');
    UI.closeModal('savingGoalModal');
    await this.refreshAllData();
    this.renderSavingGoalsView();
  },

  async depositToSavingGoal(goalId) {
    const goal = this.state.savingGoals.find(g => g.id === goalId);
    if (!goal) return;

    const amountStr = prompt(`"${goal.title}" লক্ষ্যে কত টাকা জমা করতে চান?`);
    if (!amountStr) return;

    const parsed = Utils.parseSmartAmount(amountStr);
    if (!parsed.valid || parsed.value <= 0) {
      UI.toast('সঠিক টাকার পরিমাণ দিন', 'error');
      return;
    }

    const addAmt = parsed.value;
    goal.savedAmount = (Number(goal.savedAmount) || 0) + addAmt;
    goal.remainingAmount = Math.max(0, goal.targetAmount - goal.savedAmount);
    goal.progressPercent = Math.round((goal.savedAmount / goal.targetAmount) * 10000) / 100;

    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : (goal.accountId || 'acc_rafiqul_main');

    await khataDB.put('savingGoals', goal);

    // Also create a SAVING transaction
    await khataDB.put('transactions', {
      id: Utils.generateId('tx'),
      accountId: activeAccId,
      type: 'SAVING',
      amount: addAmt,
      category: 'cat_saving_general',
      categoryName: 'সঞ্চয়: ' + goal.title,
      date: Utils.getTodayDateString(),
      paymentMethod: 'Cash',
      description: 'লক্ষ্য পূরণে সঞ্চয় জমা'
    });

    UI.toast(`৳ ${Utils.toBanglaNumber(addAmt)} সফলভাবে জমা হয়েছে ✓`, 'success');
    await this.refreshAllData();
    this.renderSavingGoalsView();
  },

  deleteSavingGoal(goalId) {
    UI.confirm({
      title: 'সঞ্চয় লক্ষ্য মুছে ফেলবেন?',
      message: 'আপনি কি নিশ্চিত যে এই সঞ্চয় লক্ষ্যটি মুছে ফেলতে চান?',
      onConfirm: async () => {
        await khataDB.delete('savingGoals', goalId);
        UI.toast('সঞ্চয় লক্ষ্য মুছে ফেলা হয়েছে', 'info');
        await this.refreshAllData();
        this.renderSavingGoalsView();
      }
    });
  },

  /**
   * ==========================================
   * 8. COPY PREVIOUS ENTRY (Section 23)
   * ==========================================
   */
  openCopyEntryModal() {
    this.handleCopySourceChange('yesterday');
    UI.openModal('copyEntryModal');
  },

  handleCopySourceChange(source) {
    const customGroup = document.getElementById('copyCustomDateGroup');
    let targetDate = '';

    const today = new Date();
    if (source === 'yesterday') {
      const d = new Date(today);
      d.setDate(d.getDate() - 1);
      targetDate = Utils.toInputDate(d);
      customGroup.style.display = 'none';
    } else if (source === 'twoDaysAgo') {
      const d = new Date(today);
      d.setDate(d.getDate() - 2);
      targetDate = Utils.toInputDate(d);
      customGroup.style.display = 'none';
    } else if (source === 'lastWeek') {
      const d = new Date(today);
      d.setDate(d.getDate() - 7);
      targetDate = Utils.toInputDate(d);
      customGroup.style.display = 'none';
    } else if (source === 'custom') {
      customGroup.style.display = 'block';
      targetDate = document.getElementById('copyCustomDateInput').value || Utils.getTodayDateString();
    }

    this.populateCopyItems(targetDate);
  },

  populateCopyItems(targetDate) {
    const container = document.getElementById('copyItemsPicker');
    if (!container) return;

    // Filter non-sales expenses from source date
    const sourceTxs = this.state.transactions.filter(t => t.date === targetDate && t.type === 'EXPENSE');

    if (sourceTxs.length === 0) {
      container.innerHTML = `
        <div style="padding: 1rem; text-align: center; color: var(--text-muted); font-size: 0.88rem;">
          নির্বাচিত তারিখে (${Utils.formatDateBengali(targetDate)}) কপি করার মতো কোনো খরচের হিসাব পাওয়া যায়নি।
        </div>
      `;
      return;
    }

    container.innerHTML = sourceTxs.map(t => `
      <label style="display: flex; align-items: center; gap: 0.6rem; padding: 0.4rem 0.2rem; cursor: pointer; border-bottom: 1px solid var(--border-light);">
        <input type="checkbox" class="copy-item-chk" value="${t.id}" checked>
        <span style="flex: 1; font-size: 0.88rem;">${t.categoryName || t.category} (${t.description || 'নিয়মিত'})</span>
        <strong style="font-size: 0.88rem; color: var(--color-expense);">${Utils.formatCurrency(t.amount)}</strong>
      </label>
    `).join('');
  },

  async executeCopyEntries() {
    const selectedCheckboxes = document.querySelectorAll('.copy-item-chk:checked');
    if (selectedCheckboxes.length === 0) {
      UI.toast('কপি করার জন্য কমপক্ষে একটি হিসাব নির্বাচন করুন!', 'warning');
      return;
    }

    const today = Utils.getTodayDateString();
    let copiedCount = 0;

    for (const chk of selectedCheckboxes) {
      const originalTx = this.state.transactions.find(t => t.id === chk.value);
      if (originalTx) {
        const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : (originalTx.accountId || 'acc_rafiqul_main');
        const newTx = {
          ...originalTx,
          id: Utils.generateId('tx'),
          accountId: activeAccId,
          date: today,
          createdAt: new Date().toISOString()
        };
        await khataDB.put('transactions', newTx);
        copiedCount++;
      }
    }

    UI.toast(`${Utils.toBanglaNumber(copiedCount)}টি হিসাব আজকের তারিখে কপি হয়েছে ✓`, 'success');
    UI.closeModal('copyEntryModal');
    await this.refreshAllData();
    this.renderCurrentView();
  },

  /**
   * ==========================================
   * 9. BACKUP, RESTORE & EXPORT (Section 25 & 26)
   * ==========================================
   */
  async exportCompleteBackupJSON() {
    return this.handleOneClickBackup();
  },

  exportTransactionsCSV() {
    const headers = ['আইডি', 'তারিখ', 'ধরন', 'ক্যাটাগরি', 'পরিমাণ (টাকা)', 'পরিশোধ মাধ্যম', 'ব্যক্তি/কাস্টমার', 'বিবরণ', 'ভাউচার নং'];
    const rows = this.state.transactions.map(t => [
      t.id,
      t.date,
      t.type === 'INCOME' ? 'আয়' : (t.type === 'SAVING' ? 'সঞ্চয়' : (t.expenseType === 'EXPENSE_SHOP' ? 'দোকান খরচ' : 'সংসার খরচ')),
      t.categoryName || t.category,
      t.amount,
      t.paymentMethod || 'Cash',
      t.person || '',
      t.description || '',
      t.refNo || ''
    ]);

    Utils.exportToCSV(headers, rows, `khata_transactions_${Utils.getTodayDateString()}.csv`);
    UI.toast('Excel CSV ডাউনলোড সম্পন্ন হয়েছে ✓', 'success');
  },

  exportFinancialCSV() {
    const range = Accounting.getDateRangeForPeriod(this.state.reportPeriod);
    const filteredTxs = Accounting.filterTransactions(this.state.transactions, range.start, range.end);
    const metrics = Accounting.calculateMetrics(filteredTxs, this.state.debts);

    const headers = ['আর্থিক বিবরণী উপাদান', 'পরিমাণ (টাকা)'];
    const rows = [
      ['সময়কাল', `${range.label} (${range.start} হতে ${range.end})`],
      ['মোট বিক্রি ও ব্যবসায়িক আয়', metrics.totalSales],
      ['দোকানের খরচ', metrics.shopExpenses],
      ['ব্যবসার নিট লাভ (Business Profit)', metrics.businessProfit],
      ['অন্যান্য ব্যক্তিগত আয়', metrics.personalIncome],
      ['সংসার খরচ (Household Expenses)', metrics.householdExpenses],
      ['নিট সঞ্চয় (Net Savings)', metrics.netSavings],
      ['মোট দেনাদার বাকি (আমি পাব)', metrics.totalReceivable],
      ['মোট পাওনাদার বাকি (আমি দেব)', metrics.totalPayable]
    ];

    Utils.exportToCSV(headers, rows, `khata_financial_statement_${range.start}.csv`);
    UI.toast('আর্থিক বিবরণী CSV এক্সপোর্ট হয়েছে ✓', 'success');
  },

  printFinancialStatement() {
    const range = Accounting.getDateRangeForPeriod(this.state.reportPeriod);
    const filteredTxs = Accounting.filterTransactions(this.state.transactions, range.start, range.end);
    const metrics = Accounting.calculateMetrics(filteredTxs, this.state.debts);

    UI.printReport({
      title: `আর্থিক বিবরণী ও লাভ-লোকসান হিসাব (${range.label})`,
      dateRange: `${Utils.formatDateBengali(range.start)} হতে ${Utils.formatDateBengali(range.end)}`,
      profile: this.state.profile,
      items: filteredTxs,
      totals: metrics,
      type: 'TRANSACTION'
    });
  },

  printDebtLedger() {
    UI.printReport({
      title: 'ধার-বাকি দেনা-পাওনা খতিয়ান',
      dateRange: `আজ পর্যন্ত (${Utils.formatDateBengali(Utils.getTodayDateString())})`,
      profile: this.state.profile,
      items: this.state.debts,
      totals: {},
      type: 'DEBT'
    });
  },

  confirmResetAllData() {
    UI.confirm({
      title: 'সম্পূর্ণ ডাটা রিসেট করবেন?',
      message: 'সতর্কতা: সমস্ত লেনদেন ও ধার-বাকি মুছে ফেলা হবে। আপনি কি নিশ্চিত?',
      confirmText: 'হ্যাঁ, সব পরিষ্কার করুন',
      isDanger: true,
      onConfirm: async () => {
        await khataDB.resetDatabase(false);
        UI.toast('সমস্ত ডাটা পরিষ্কার করা হয়েছে', 'info');
        await this.refreshAllData();
        this.renderCurrentView();
      }
    });
  },

  async handleAddCustomCategory(e) {
    e.preventDefault();
    const name = document.getElementById('newCatNameInput').value.trim();
    const type = document.getElementById('newCatTypeSelect').value;

    const newCat = {
      id: Utils.generateId('cat_custom'),
      name,
      type,
      isSales: false,
      isDefault: false
    };

    await khataDB.put('categories', newCat);
    document.getElementById('newCatNameInput').value = '';
    UI.toast(`নতুন ক্যাটাগরি "${name}" যুক্ত হয়েছে ✓`, 'success');
    await this.refreshAllData();
  },

  /**
   * ==========================================
   * MULTI-BUSINESS & AUTH CONTROLLER
   * ==========================================
   */
  openAuthModal(defaultTab = 'LOGIN') {
    this.switchAuthTab(defaultTab);
    this.renderAccountsList();
    UI.openModal('authModal');
  },

  switchAuthTab(tab) {
    const loginSection = document.getElementById('authTabLoginSection');
    const registerSection = document.getElementById('authTabRegisterSection');
    const loginBtn = document.getElementById('authTabLoginBtn');
    const registerBtn = document.getElementById('authTabRegisterBtn');

    if (tab === 'REGISTER') {
      if (loginSection) loginSection.style.display = 'none';
      if (registerSection) registerSection.style.display = 'block';
      if (loginBtn) loginBtn.classList.remove('active');
      if (registerBtn) registerBtn.classList.add('active');
    } else {
      if (loginSection) loginSection.style.display = 'block';
      if (registerSection) registerSection.style.display = 'none';
      if (loginBtn) loginBtn.classList.add('active');
      if (registerBtn) registerBtn.classList.remove('active');
    }
  },

  renderAccountsList() {
    const container = document.getElementById('authAccountsList');
    if (!container) return;

    if (!this.state.accounts || this.state.accounts.length === 0) {
      container.innerHTML = '<div style="padding: 1rem; text-align: center; color: var(--text-muted);">কোনো নিবন্ধিত ব্যবসা অ্যাকাউন্ট পাওয়া যায়নি</div>';
      return;
    }

    const currentId = this.state.activeAccount ? this.state.activeAccount.id : '';

    container.innerHTML = this.state.accounts.map(acc => {
      const isActive = acc.id === currentId;
      const isFounder = acc.isFounder || acc.id === 'acc_rafiqul_main';
      const catMeta = getBusinessCategoryMeta(acc.businessCategory);

      const accPhoto = acc.photoUrl || 'icons/founder_rafiqul_islam.jpg';
      const avatarHtml = `<img src="${accPhoto}" alt="${acc.ownerName}" class="account-avatar-circle" style="object-fit: cover; border-radius: 50%; border: 1.5px solid var(--border-color);">`;

      const badgeHtml = isFounder
        ? `<span style="display: inline-block; background: #FEF3C7; color: #92400E; font-size: 0.72rem; font-weight: 700; padding: 2px 6px; border-radius: 4px; margin-left: 6px;">প্রতিষ্ঠাতা</span>`
        : `<span style="display: inline-block; background: #E0F2FE; color: #0369A1; font-size: 0.72rem; font-weight: 600; padding: 2px 6px; border-radius: 4px; margin-left: 6px;">ব্যবসায়ী</span>`;

      const catBadgeHtml = `<span class="biz-cat-badge biz-cat-badge-sm ${catMeta.badgeClass}" style="margin-left: 6px;">${catMeta.icon} ${catMeta.name}</span>`;

      const activeBadge = isActive
        ? `<span style="display: inline-block; background: #D1FAE5; color: #065F46; font-size: 0.72rem; font-weight: 700; padding: 2px 6px; border-radius: 4px;">বর্তমান সক্রিয় ✓</span>`
        : '';

      return `
        <div class="business-account-item ${isActive ? 'active' : ''}">
          ${avatarHtml}
          <div class="business-account-info">
            <div class="account-biz-name" style="display: flex; align-items: center; flex-wrap: wrap; gap: 4px;">
              <span>${acc.businessName || 'নামহীন ব্যবসা'}</span>
              ${badgeHtml}
              ${catBadgeHtml}
            </div>
            <div class="account-owner-meta" style="margin-top: 3px;">
              <span>👤 ${acc.ownerName || 'স্বত্বাধিকারী'}</span> • <span>📞 ${acc.phone || ''}</span>
            </div>
          </div>
          <div style="display: flex; flex-direction: column; align-items: flex-end; gap: 4px;">
            ${activeBadge}
            ${!isActive ? `<button type="button" class="btn btn-secondary btn-sm" onclick="App.switchAccount('${acc.id}')" style="padding: 4px 10px; font-size: 0.8rem;">লগইন</button>` : ''}
          </div>
        </div>
      `;
    }).join('');
  },

  async switchAccount(accountId) {
    const acc = this.state.accounts.find(a => a.id === accountId);
    if (!acc) {
      UI.toast('অ্যাকাউন্ট পাওয়া যায়নি', 'error');
      return;
    }

    this.state.activeAccount = acc;
    localStorage.setItem('khata_active_account_id', acc.id);

    // Also update appProfile activeAccountId and businessCategory
    await khataDB.put('appProfile', {
      key: 'main',
      businessCategory: acc.businessCategory || 'মুদি দোকান ও সুপারশপ',
      businessName: acc.businessName,
      ownerName: acc.ownerName,
      phone: acc.phone,
      address: acc.address || '',
      email: acc.email || '',
      photoUrl: acc.photoUrl || '',
      activeAccountId: acc.id
    });

    UI.closeModal('authModal');
    await this.refreshAllData();
    this.updateHeaderAndProfile();
    this.renderCurrentView();

    UI.toast(`সফলভাবে "${acc.businessName}" (${acc.businessCategory || 'ব্যবসায়ী'}) অ্যাকাউন্টে প্রবেশ করেছেন ✓`, 'success');
  },

  async handleDirectPhoneLogin(e) {
    e.preventDefault();
    const phone = document.getElementById('loginPhoneInput').value.trim();
    const pin = document.getElementById('loginPinInput').value.trim();

    if (!phone || !pin) {
      UI.toast('মোবাইল নম্বর ও পিন কোড উভয়ই প্রদান করুন', 'error');
      return;
    }

    // Match phone (clean hyphens and spaces for comparison)
    const cleanInputPhone = phone.replace(/[\s\-]/g, '');
    const matched = this.state.accounts.find(a => {
      const cleanAccPhone = (a.phone || '').replace(/[\s\-]/g, '');
      return cleanAccPhone === cleanInputPhone || a.phone === phone;
    });

    if (!matched) {
      UI.toast('এই মোবাইল নম্বরে কোনো অ্যাকাউন্ট পাওয়া যায়নি!', 'error');
      return;
    }

    if (matched.pin && matched.pin !== pin) {
      UI.toast('ভুল পিন কোড! আবার চেষ্টা করুন।', 'error');
      return;
    }

    // Logged in successfully
    document.getElementById('loginPhoneInput').value = '';
    document.getElementById('loginPinInput').value = '';
    await this.switchAccount(matched.id);
  },

  async handleRegisterNewBusiness(e) {
    e.preventDefault();
    const businessCategory = document.getElementById('regBusinessCategory')?.value.trim() || 'মুদি দোকান ও সুপারশপ';
    const businessName = document.getElementById('regBusinessName').value.trim();
    const ownerName = document.getElementById('regOwnerName').value.trim();
    const phone = document.getElementById('regPhone').value.trim();
    const email = document.getElementById('regEmail').value.trim();
    const pin = document.getElementById('regPin').value.trim();
    const address = document.getElementById('regAddress').value.trim();

    if (!businessName || !ownerName || !phone || !pin) {
      UI.toast('সবগুলো আবশ্যক তথ্য পূরণ করুন!', 'error');
      return;
    }

    // Check if phone already registered
    const cleanPhone = phone.replace(/[\s\-]/g, '');
    const exists = this.state.accounts.find(a => (a.phone || '').replace(/[\s\-]/g, '') === cleanPhone);
    if (exists) {
      UI.toast('এই মোবাইল নম্বর দিয়ে ইতিমধ্যে একটি অ্যাকাউন্ট রয়েছে!', 'warning');
      return;
    }

    const newAcc = {
      id: Utils.generateId('acc'),
      businessCategory,
      businessName,
      ownerName,
      tagline: `${businessCategory} হিসাব`,
      phone,
      email,
      pin,
      address,
      role: 'MERCHANT',
      isFounder: false,
      photoUrl: '',
      createdAt: new Date().toISOString()
    };

    try {
      await khataDB.put('accounts', newAcc);
      this.state.accounts.push(newAcc);

      // Reset form
      document.getElementById('regBusinessName').value = '';
      document.getElementById('regOwnerName').value = '';
      document.getElementById('regPhone').value = '';
      document.getElementById('regEmail').value = '';
      document.getElementById('regPin').value = '';
      document.getElementById('regAddress').value = '';

      UI.toast(`নতুন ব্যবসা অ্যাকাউন্ট "${businessName}" সফলভাবে তৈরি হয়েছে! স্বাগতম ✓`, 'success');
      await this.switchAccount(newAcc.id);
    } catch (err) {
      console.error('Register error:', err);
      UI.toast('অ্যাকাউন্ট তৈরিতে সমস্যা হয়েছে', 'error');
    }
  },

  handleLogout() {
    UI.confirm({
      title: 'লগআউট ও ব্যবসা পরিবর্তন',
      message: 'আপনি কি বর্তমান অ্যাকাউন্ট থেকে লগআউট করে অন্য অ্যাকাউন্টে প্রবেশ করতে চান?',
      confirmText: 'হ্যাঁ, একাউন্ট পরিবর্তন',
      onConfirm: () => {
        this.openAuthModal('LOGIN');
      }
    });
  },

  /**
   * ==========================================
   * CUSTOMER DUE LOOKUP & AUTO-ADD CALCULATION
   * ==========================================
   */

  /**
   * Calculate customer's previous unpaid debt balance
   * @param {string} personName - Customer Name
   * @param {string|null} excludeDebtId - Exclude a specific debt ID (useful when updating)
   * @returns {{ totalDue: number, debts: Array, phone: string, latestDebt: Object|null }}
   */
  getCustomerPreviousDue(personName, excludeDebtId = null) {
    if (!personName || !personName.trim()) {
      return { totalDue: 0, debts: [], phone: '', latestDebt: null };
    }

    const cleanName = personName.trim().toLowerCase();
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const matchingDebts = this.state.debts.filter(d => {
      const matchAcc = (d.accountId === activeAccId || (!d.accountId && activeAccId === 'acc_rafiqul_main'));
      const isCustomerDue = (d.type === 'RECEIVABLE'); // আমি পাব
      const notExcluded = (!excludeDebtId || d.id !== excludeDebtId);
      const matchPerson = (d.person && d.person.trim().toLowerCase() === cleanName);
      return matchAcc && isCustomerDue && notExcluded;
    });

    let totalDue = 0;
    let phone = '';

    matchingDebts.forEach(d => {
      const rem = d.remainingAmount !== undefined ? d.remainingAmount : Math.max(0, (d.amount || 0) - (d.paidAmount || 0));
      if (rem > 0) {
        totalDue += rem;
      }
      if (!phone && d.phone) {
        phone = d.phone;
      }
    });

    // Also check for customer phone in transactions if not found in debts
    if (!phone) {
      const txWithPhone = this.state.transactions.find(t => 
        (t.accountId === activeAccId || (!t.accountId && activeAccId === 'acc_rafiqul_main')) &&
        t.person && t.person.trim().toLowerCase() === cleanName &&
        t.phone
      );
      if (txWithPhone) phone = txWithPhone.phone;
    }

    return {
      totalDue: Math.round(totalDue * 100) / 100,
      debts: matchingDebts,
      phone: phone || '',
      latestDebt: matchingDebts.length > 0 ? matchingDebts[matchingDebts.length - 1] : null
    };
  },

  /**
   * Populate <datalist id="customersDatalist"> with all unique customers
   */
  updateCustomersDatalist() {
    const datalist = document.getElementById('customersDatalist');
    if (!datalist) return;

    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';
    const namesSet = new Set();

    // From Debts
    (this.state.debts || []).forEach(d => {
      if ((d.accountId === activeAccId || (!d.accountId && activeAccId === 'acc_rafiqul_main')) && d.person && d.person.trim()) {
        namesSet.add(d.person.trim());
      }
    });

    // From Transactions
    (this.state.transactions || []).forEach(t => {
      if ((t.accountId === activeAccId || (!t.accountId && activeAccId === 'acc_rafiqul_main')) && t.person && t.person.trim()) {
        namesSet.add(t.person.trim());
      }
    });

    const sortedNames = Array.from(namesSet).sort((a, b) => a.localeCompare(b, 'bn'));
    datalist.innerHTML = sortedNames.map(name => `<option value="${name}"></option>`).join('');
  },

  /**
   * Check old due as user types customer name in any modal (txModal, newDebtModal)
   */
  checkCustomerOldDue(name, alertContainerId, targetAmountInputId = null) {
    const alertEl = document.getElementById(alertContainerId);
    if (!alertEl) return;

    if (!name || name.trim().length < 2) {
      alertEl.style.display = 'none';
      return;
    }

    const prev = this.getCustomerPreviousDue(name);

    if (prev.totalDue > 0) {
      alertEl.className = 'smart-due-alert has-due';
      alertEl.style.display = 'flex';

      if (targetAmountInputId) {
        // Called from newDebtModal
        alertEl.innerHTML = `
          <span>⚠️ এই কাস্টমারের পূর্বের পুরাতন বাকি আছে: <strong>৳ ${Utils.toBanglaNumber(prev.totalDue)}</strong></span>
          <button type="button" class="btn btn-secondary btn-sm" onclick="App.addOldDueToNewDebtInput(${prev.totalDue})" style="padding: 2px 8px; font-size: 0.78rem; font-weight: 700; background: #FDE68A; color: #92400E; border-color: #F59E0B;">
            পূর্বের বাকি যোগ করুন (+)
          </button>
        `;
      } else {
        // Called from txModal
        alertEl.innerHTML = `
          <span>⚠️ এই কাস্টমারের পূর্বের পুরাতন বাকি আছে: <strong>৳ ${Utils.toBanglaNumber(prev.totalDue)}</strong></span>
          <button type="button" class="btn btn-secondary btn-sm" onclick="App.openCreateMemoModal('${name.replace(/'/g, "\\'")}')" style="padding: 2px 8px; font-size: 0.78rem; font-weight: 700; background: #D1FAE5; color: #065F46; border-color: #10B981;">
            ক্যাশ মেমো তৈরি 📄
          </button>
        `;
      }
    } else if (prev.debts.length > 0) {
      alertEl.className = 'smart-due-alert no-due';
      alertEl.style.display = 'flex';
      alertEl.innerHTML = `<span>✓ এই কাস্টমারের পূর্বের কোনো বকেয়া বাকি নেই (৳ ০)</span>`;
    } else {
      alertEl.style.display = 'none';
    }
  },

  /**
   * Helper to add old due into newDebtAmountInput in newDebtModal
   */
  addOldDueToNewDebtInput(oldDue) {
    const input = document.getElementById('newDebtAmountInput');
    if (!input) return;
    const currentVal = Utils.parseSmartAmount(input.value).value || 0;
    const combined = currentVal + oldDue;
    input.value = combined;
    this.handleAmountCalcInput(input.value, 'newDebtCalcBadge');
    UI.toast(`পূর্বের পুরাতন বাকি ৳ ${Utils.toBanglaNumber(oldDue)} সফলভাবে যোগ করা হয়েছে ✓`, 'success');
  },

  /**
   * Open the Create Cash Memo Modal (auto-adds previous customer due & shop details)
   */
  openCreateMemoModal(customerName = '') {
    const custInput = document.getElementById('memoCustomerInput');
    const phoneInput = document.getElementById('memoCustomerPhone');
    const dateInput = document.getElementById('memoDateInput');
    const itemsBody = document.getElementById('memoItemsBody');
    const oldDueInput = document.getElementById('memoOldDueInput');
    const todayPaidInput = document.getElementById('memoTodayPaidInput');

    if (custInput) custInput.value = customerName;
    if (phoneInput) phoneInput.value = '';
    if (dateInput) dateInput.value = Utils.getTodayDateString();
    if (oldDueInput) oldDueInput.value = 0;
    if (todayPaidInput) todayPaidInput.value = 0;

    // Populate active shop profile into memo form inputs
    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};
    const currentBizName = acc.businessName || prof.businessName || 'সহজ খাতা স্টোর';
    const currentOwnerName = acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';
    const currentPhone = acc.phone || prof.phone || '0131082-4987';
    const currentAddress = acc.address || prof.address || 'বাংলাদেশ';
    const currentTagline = acc.tagline || prof.tagline || 'এখানে পাইকারী ও খুচরা সকল প্রকার মুদি মাল সুলভ মূল্যে পাওয়া যায়';

    const sNameInput = document.getElementById('memoShopNameInput');
    if (sNameInput) sNameInput.value = currentBizName;
    const sOwnerInput = document.getElementById('memoShopOwnerInput');
    if (sOwnerInput) sOwnerInput.value = currentOwnerName;
    const sPhoneInput = document.getElementById('memoShopPhoneInput');
    if (sPhoneInput) sPhoneInput.value = currentPhone;
    const sAddrInput = document.getElementById('memoShopAddressInput');
    if (sAddrInput) sAddrInput.value = currentAddress;
    const sTagInput = document.getElementById('memoShopTaglineInput');
    if (sTagInput) sTagInput.value = currentTagline;

    this.updateMemoShopSummary();

    // Reset items table: 2 fresh rows
    if (itemsBody) {
      itemsBody.innerHTML = '';
      this.addMemoItemRow({ name: '', qty: 1, rate: '' });
      this.addMemoItemRow({ name: '', qty: 1, rate: '' });
    }

    if (customerName) {
      this.onMemoCustomerChange(customerName);
    } else {
      this.recalcMemoTotals();
      const alertEl = document.getElementById('memoCustomerDueAlert');
      if (alertEl) alertEl.style.display = 'none';
    }

    UI.openModal('createMemoModal');
  },

  toggleMemoShopDetails() {
    const el = document.getElementById('memoShopEditFields');
    const btn = document.getElementById('btnToggleMemoShopDetails');
    if (!el) return;
    const isHidden = el.style.display === 'none';
    el.style.display = isHidden ? 'block' : 'none';
    if (btn) btn.textContent = isHidden ? '▲ বন্ধ করুন' : '✏️ দোকানের নাম/তথ্য এডিট';
  },

  updateMemoShopSummary() {
    const sName = document.getElementById('memoShopNameInput')?.value.trim() || 'সহজ খাতা স্টোর';
    const sOwner = document.getElementById('memoShopOwnerInput')?.value.trim() || 'রফিকুল ইসলাম';
    const sPhone = document.getElementById('memoShopPhoneInput')?.value.trim() || '0131082-4987';

    const sumName = document.getElementById('memoShopSummaryName');
    if (sumName) sumName.textContent = sName;
    const sumOwner = document.getElementById('memoShopSummaryOwner');
    if (sumOwner) sumOwner.textContent = sOwner;
    const sumPhone = document.getElementById('memoShopSummaryPhone');
    if (sumPhone) sumPhone.textContent = sPhone;
  },

  /**
   * Triggered when typing or selecting a customer in createMemoModal
   */
  onMemoCustomerChange(name) {
    const prev = this.getCustomerPreviousDue(name);
    const alertEl = document.getElementById('memoCustomerDueAlert');
    const oldDueInput = document.getElementById('memoOldDueInput');
    const phoneInput = document.getElementById('memoCustomerPhone');
    const badgeEl = document.getElementById('memoOldDueBadge');

    if (oldDueInput) {
      oldDueInput.value = prev.totalDue;
    }

    if (phoneInput && prev.phone && !phoneInput.value) {
      phoneInput.value = prev.phone;
    }

    if (alertEl) {
      if (name && name.trim().length >= 2 && prev.totalDue > 0) {
        alertEl.className = 'smart-due-alert has-due';
        alertEl.style.display = 'flex';
        alertEl.innerHTML = `
          <span>⚠️ এই কাস্টমারের পূর্বের পুরাতন বাকি আছে: <strong>৳ ${Utils.toBanglaNumber(prev.totalDue)}</strong></span>
          <span class="badge" style="background: #FEF3C7; color: #92400E; font-size: 0.78rem;">নতুন মেমোতে অটো যোগ হয়েছে ✓</span>
        `;
        if (badgeEl) {
          badgeEl.textContent = `পূর্বের বাকি: ৳ ${Utils.toBanglaNumber(prev.totalDue)}`;
          badgeEl.style.background = '#FEF3C7';
          badgeEl.style.color = '#B45309';
        }
      } else if (name && name.trim().length >= 2 && prev.debts.length > 0) {
        alertEl.className = 'smart-due-alert no-due';
        alertEl.style.display = 'flex';
        alertEl.innerHTML = `<span>✓ এই কাস্টমারের পূর্বের সমস্ত দেনা পরিশোধিত (বাকি: ৳ ০)</span>`;
        if (badgeEl) {
          badgeEl.textContent = 'কোন বকেয়া নেই';
          badgeEl.style.background = '#ECFDF5';
          badgeEl.style.color = '#065F46';
        }
      } else {
        alertEl.style.display = 'none';
        if (badgeEl) {
          badgeEl.textContent = 'স্বয়ংক্রিয় জের';
          badgeEl.style.background = '#F3F4F6';
          badgeEl.style.color = '#4B5563';
        }
      }
    }

    this.recalcMemoTotals();
  },

  /**
   * Add a product row in createMemoModal
   */
  addMemoItemRow(item = { name: '', qty: 1, rate: '' }) {
    const tbody = document.getElementById('memoItemsBody');
    if (!tbody) return;

    const tr = document.createElement('tr');
    tr.innerHTML = `
      <td>
        <input type="text" class="memo-item-input memo-item-name" placeholder="পণ্যের নাম (যেমন: চাল, তেল, ডাল...)" value="${item.name || ''}" list="productsDatalist" oninput="App.onMemoProductNameInput(this)">
        <div class="memo-item-stock-hint" style="font-size: 0.75rem; color: #059669; margin-top: 2px;"></div>
      </td>
      <td>
        <input type="number" class="memo-item-input memo-item-qty" value="${item.qty || 1}" min="0.1" step="any" oninput="App.recalcMemoTotals()" style="text-align: center;">
      </td>
      <td>
        <input type="number" class="memo-item-input memo-item-rate" value="${item.rate !== undefined ? item.rate : ''}" placeholder="০" min="0" step="any" oninput="App.recalcMemoTotals()" style="text-align: right;">
      </td>
      <td style="text-align: right; font-weight: 700; color: var(--primary-800);" class="memo-item-total-cell">
        ৳ ০
      </td>
      <td style="text-align: center;">
        <button type="button" class="btn-remove-item" onclick="App.removeMemoItemRow(this)" style="background: none; border: none; color: #EF4444; font-weight: 800; cursor: pointer; font-size: 1.1rem; padding: 2px 6px;" title="পণ্য বাদ দিন">✕</button>
      </td>
    `;
    tbody.appendChild(tr);
    this.recalcMemoTotals();
  },

  /**
   * Remove a product row in createMemoModal
   */
  removeMemoItemRow(btn) {
    const tr = btn.closest('tr');
    if (tr) tr.remove();
    const tbody = document.getElementById('memoItemsBody');
    if (tbody && tbody.children.length === 0) {
      this.addMemoItemRow({ name: '', qty: 1, rate: '' });
    }
    this.recalcMemoTotals();
  },

  /**
   * Recalculate Live Mathematical Totals in createMemoModal
   * 1. Old Due (পূর্বের পুরাতন বাকি)
   * 2. Today's Bill (আজকের নতুন বিল)
   * 3. Total Due (সর্বমোট পাওনা = ১ + ২)
   * 4. Today's Cash Paid (আজকের নগদ জমা)
   * 5. Net Due Balance (বর্তমান মোট বকেয়া = ৩ - ৪)
   */
  recalcMemoTotals() {
    let todayBill = 0;
    const rows = document.querySelectorAll('#memoItemsBody tr');

    rows.forEach(tr => {
      const qtyInput = tr.querySelector('.memo-item-qty');
      const rateInput = tr.querySelector('.memo-item-rate');
      const totalCell = tr.querySelector('.memo-item-total-cell');

      const qty = parseFloat(qtyInput?.value) || 0;
      const rate = parseFloat(rateInput?.value) || 0;
      const total = Math.round(qty * rate * 100) / 100;

      if (totalCell) {
        totalCell.textContent = Utils.formatCurrency(total);
      }
      todayBill += total;
    });

    const oldDueInput = document.getElementById('memoOldDueInput');
    const oldDue = Math.max(0, parseFloat(oldDueInput?.value) || 0);

    const todayPaidInput = document.getElementById('memoTodayPaidInput');
    const todayPaid = Math.max(0, parseFloat(todayPaidInput?.value) || 0);

    const totalDue = Math.round((oldDue + todayBill) * 100) / 100;
    const netDue = Math.max(0, Math.round((totalDue - todayPaid) * 100) / 100);

    // Update UI elements
    const subtotalEl = document.getElementById('memoTodayBillSubtotal');
    if (subtotalEl) subtotalEl.textContent = Utils.formatCurrency(todayBill);

    const mathOldDueEl = document.getElementById('memoMathOldDue');
    if (mathOldDueEl) mathOldDueEl.textContent = Utils.formatCurrency(oldDue);

    const mathTodayBillEl = document.getElementById('memoMathTodayBill');
    if (mathTodayBillEl) mathTodayBillEl.textContent = `+ ${Utils.formatCurrency(todayBill)}`;

    const mathTotalDueEl = document.getElementById('memoMathTotalDue');
    if (mathTotalDueEl) mathTotalDueEl.textContent = Utils.formatCurrency(totalDue);

    const mathNetDueEl = document.getElementById('memoMathNetDue');
    if (mathNetDueEl) {
      mathNetDueEl.textContent = Utils.formatCurrency(netDue);
      mathNetDueEl.style.color = netDue > 0 ? '#DC2626' : '#059669';
    }

    const wordsEl = document.getElementById('memoMathInWords');
    if (wordsEl) {
      const highlightAmt = netDue > 0 ? netDue : (todayPaid > 0 ? todayPaid : totalDue);
      wordsEl.textContent = `কথায়: ${Utils.numberToBanglaWords(highlightAmt)}`;
    }

    return { oldDue, todayBill, totalDue, todayPaid, netDue };
  },

  /**
   * Handle Create & Save New Cash Memo
   */
  async handleGenerateAndPrintMemo() {
    const customerName = document.getElementById('memoCustomerInput')?.value.trim();
    if (!customerName) {
      UI.toast('কাস্টমারের নাম লিখুন বা তালিকা থেকে নির্বাচন করুন!', 'error');
      return;
    }

    const totals = this.recalcMemoTotals();
    const customerPhone = document.getElementById('memoCustomerPhone')?.value.trim() || '';
    const memoDate = document.getElementById('memoDateInput')?.value || Utils.getTodayDateString();
    const syncDebt = document.getElementById('memoSyncDebtCheckbox')?.checked ?? true;
    const recordCash = document.getElementById('memoRecordCashCheckbox')?.checked ?? true;
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    // Collect purchased items
    const items = [];
    document.querySelectorAll('#memoItemsBody tr').forEach(tr => {
      const name = tr.querySelector('.memo-item-name')?.value.trim();
      const qty = parseFloat(tr.querySelector('.memo-item-qty')?.value) || 0;
      const rate = parseFloat(tr.querySelector('.memo-item-rate')?.value) || 0;
      const total = Math.round(qty * rate * 100) / 100;
      if (name || total > 0) {
        items.push({
          name: name || 'পণ্য সামগ্রী',
          qty: qty || 1,
          rate: rate || total,
          total: total
        });
      }
    });

    const memoNo = `MEMO-${memoDate.replace(/[^0-9]/g, '')}-${Math.floor(1000 + Math.random() * 9000)}`;

    // 1. Record Cash Flow Transaction if today cash was paid
    if (recordCash && totals.todayPaid > 0) {
      await khataDB.put('transactions', {
        id: Utils.generateId('tx'),
        accountId: activeAccId,
        type: 'INCOME',
        isSales: true,
        amount: totals.todayPaid,
        category: 'cat_inc_sales',
        categoryName: 'নগদ বিক্রয় (মেমো)',
        date: memoDate,
        paymentMethod: 'Cash',
        person: customerName,
        phone: customerPhone,
        refNo: memoNo,
        description: `ক্যাশ মেমো নং: ${memoNo} বাবদ নগদ জমা`
      });
    }

    // 2. Synchronize Customer Ledger & Debt Record
    if (syncDebt) {
      const cleanName = customerName.toLowerCase();
      // Find customer's active unpaid debt record
      const existingDebt = this.state.debts.find(d => 
        (d.accountId === activeAccId || (!d.accountId && activeAccId === 'acc_rafiqul_main')) &&
        d.type === 'RECEIVABLE' &&
        d.person && d.person.trim().toLowerCase() === cleanName &&
        d.status !== 'PAID'
      );

      if (existingDebt) {
        // Merge today's bill into existing debt
        const updatedTotal = (existingDebt.amount || 0) + totals.todayBill;
        const updatedPaid = (existingDebt.paidAmount || 0) + totals.todayPaid;
        const updatedRemaining = Math.max(0, updatedTotal - updatedPaid);

        existingDebt.amount = updatedTotal;
        existingDebt.paidAmount = updatedPaid;
        existingDebt.remainingAmount = updatedRemaining;
        existingDebt.status = updatedRemaining <= 0 ? 'PAID' : 'PARTIAL';
        if (customerPhone) existingDebt.phone = customerPhone;

        await khataDB.put('debts', existingDebt);

        if (totals.todayPaid > 0) {
          await khataDB.put('debtPayments', {
            id: Utils.generateId('pmt'),
            debtId: existingDebt.id,
            accountId: activeAccId,
            person: customerName,
            amount: totals.todayPaid,
            date: memoDate,
            paymentMethod: 'Cash',
            note: `মেমো নং ${memoNo} বাবদ নগদ পরিশোধ`
          });
        }
      } else if (totals.netDue > 0 || totals.todayBill > 0) {
        // Create new customer debt record
        const newDebtId = Utils.generateId('debt');
        await khataDB.put('debts', {
          id: newDebtId,
          accountId: activeAccId,
          type: 'RECEIVABLE',
          person: customerName,
          phone: customerPhone,
          amount: totals.totalDue,
          paidAmount: totals.todayPaid,
          remainingAmount: totals.netDue,
          date: memoDate,
          status: totals.netDue <= 0 ? 'PAID' : (totals.todayPaid > 0 ? 'PARTIAL' : 'PENDING'),
          note: `মেমো নং ${memoNo} বিক্রয় চালান`
        });

        if (totals.todayPaid > 0) {
          await khataDB.put('debtPayments', {
            id: Utils.generateId('pmt'),
            debtId: newDebtId,
            accountId: activeAccId,
            person: customerName,
            amount: totals.todayPaid,
            date: memoDate,
            paymentMethod: 'Cash',
            note: `মেমো নং ${memoNo} প্রারম্ভিক নগদ পরিশোধ`
          });
        }
      }
    }

    // Deduct sold products from inventory stock
    for (const item of items) {
      if (!item.name) continue;
      const cleanItemName = item.name.trim().toLowerCase();
      const prod = this.state.products.find(p => p.name && p.name.trim().toLowerCase() === cleanItemName);
      if (prod) {
        const soldQty = parseFloat(item.qty) || 0;
        const currentStock = parseFloat(prod.currentStock) || 0;
        const newStock = Math.max(0, Math.round((currentStock - soldQty) * 100) / 100);
        prod.currentStock = newStock;
        prod.updatedAt = new Date().toISOString();
        await khataDB.put('products', prod);
      }
    }

    // Refresh application state
    await this.refreshAllData();
    this.renderDebtsView();
    this.renderDashboard();

    // Extract custom shop info entered in modal
    const shopName = document.getElementById('memoShopNameInput')?.value.trim() || this.state.activeAccount?.businessName || this.state.profile?.businessName || 'সহজ খাতা স্টোর';
    const ownerName = document.getElementById('memoShopOwnerInput')?.value.trim() || this.state.activeAccount?.ownerName || this.state.profile?.ownerName || 'রফিকুল ইসলাম';
    const shopPhone = document.getElementById('memoShopPhoneInput')?.value.trim() || this.state.activeAccount?.phone || this.state.profile?.phone || '0131082-4987';
    const shopAddress = document.getElementById('memoShopAddressInput')?.value.trim() || this.state.activeAccount?.address || this.state.profile?.address || 'বাংলাদেশ';
    const shopTagline = document.getElementById('memoShopTaglineInput')?.value.trim() || 'দোকান, ব্যবসা ও পাইকারি-খুচরা হিসাব কেন্দ্র';
    const saveToProfile = document.getElementById('memoSaveToProfileCheckbox')?.checked ?? true;

    // Seal Preferences
    const includeSeal = document.getElementById('memoIncludeSealCheckbox')?.checked ?? true;
    const sealColor = document.getElementById('memoSealColorSelect')?.value || 'violet';

    if (saveToProfile && this.state.activeAccount) {
      this.state.activeAccount.businessName = shopName;
      this.state.activeAccount.ownerName = ownerName;
      this.state.activeAccount.phone = shopPhone;
      this.state.activeAccount.address = shopAddress;
      await khataDB.put('accounts', this.state.activeAccount);
      this.updateSidebarAccountInfo();
    }

    UI.closeModal('createMemoModal');

    // Build memo presentation object
    const memoData = {
      memoNo: memoNo,
      shopName: shopName,
      ownerName: ownerName,
      shopPhone: shopPhone,
      shopAddress: shopAddress,
      shopTagline: shopTagline,
      includeSeal: includeSeal,
      sealColor: sealColor,
      customerName: customerName,
      customerPhone: customerPhone,
      date: memoDate,
      oldDue: totals.oldDue,
      todayBill: totals.todayBill,
      totalDue: totals.totalDue,
      todayPaid: totals.todayPaid,
      netDue: totals.netDue,
      items: items,
      inWords: Utils.numberToBanglaWords(totals.netDue > 0 ? totals.netDue : (totals.todayPaid > 0 ? totals.todayPaid : totals.totalDue))
    };

    // Render A4 sheet and thermal receipt
    this.renderGeneratedA4Memo(memoData);
    UI.openModal('customerMemoModal');
    UI.toast('ক্যাশ মেমো সফলভাবে তৈরি হয়েছে! পুরাতন বাকি ও সীলমোহর যুক্ত হয়েছে ✓', 'success');
  },

  /**
   * Render A4 Cash Memo Sheet with Auto Old-Due Addition & Official Seal
   */
  renderGeneratedA4Memo(data) {
    const container = document.getElementById('customerMemoSheet');
    if (!container) return;

    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};

    const shopName = data.shopName || acc.businessName || prof.businessName || 'সহজ খাতা স্টোর';
    const ownerName = data.ownerName || acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';
    const phone = data.shopPhone || acc.phone || prof.phone || '0131082-4987';
    const email = acc.email || prof.email || 'rafiqulislam.globalwork@gmail.com';
    const address = data.shopAddress || acc.address || prof.address || 'বাংলাদেশ';
    const tagline = data.shopTagline || 'দোকান, ব্যবসা ও পাইকারি-খুচরা হিসাব কেন্দ্র';

    const dateBangla = Utils.formatDateBengali(data.date);

    // Seal setup
    this.currentMemoSealColor = data.includeSeal !== false ? (data.sealColor || 'violet') : 'off';
    const sealMarkup = data.includeSeal !== false ? this.generateOfficialSealSvg(shopName, ownerName, dateBangla, this.currentMemoSealColor) : '';
    const statusStampMarkup = this.generateStatusStampHtml(data.netDue, data.todayPaid, data.totalDue);

    // Update seal toggle button in modal header
    const sealBtnText = document.getElementById('memoSealBtnText');
    const sealBtnIcon = document.getElementById('memoSealBtnIcon');
    if (sealBtnText) {
      const names = { violet: 'সীল: বেগুনি', blue: 'সীল: নীল', green: 'সীল: সবুজ', red: 'সীল: লাল', off: 'সীল: বন্ধ' };
      const icons = { violet: '🟣', blue: '🔵', green: '🟢', red: '🔴', off: '⚪' };
      sealBtnText.textContent = names[this.currentMemoSealColor] || 'সীলমোহর: অন';
      if (sealBtnIcon) sealBtnIcon.textContent = icons[this.currentMemoSealColor] || '💮';
    }

    let rowsHtml = '';
    let sl = 1;

    // 1. FIRST PROMINENT ROW: OLD DUE (If oldDue > 0)
    if (data.oldDue > 0) {
      rowsHtml += `
        <tr class="a4-prev-due-row">
          <td style="text-align: center;">${Utils.toBanglaNumber(sl++, false)}</td>
          <td style="text-align: center;">পূর্বের জের</td>
          <td>
            <span class="a4-prev-due-tag">পুরাতন বাকি</span>
            <strong>পূর্বের জের / পুরাতন বকেয়া বাকি (Auto Added)</strong>
            <div style="font-size: 8pt; color: #92400E; margin-top: 2px;">
              নতুন মেমোর সাথে পূর্বের অবশিষ্ট দেনা স্বয়ংক্রিয় যোগ হয়েছে
            </div>
          </td>
          <td style="text-align: center;">-</td>
          <td style="text-align: right;">-</td>
          <td style="text-align: right; font-weight: 800; color: #B45309; font-size: 10pt;">
            ${Utils.formatCurrency(data.oldDue)}
          </td>
        </tr>
      `;
    }

    // 2. TODAY'S PURCHASED ITEMS
    if (data.items && data.items.length > 0) {
      data.items.forEach(item => {
        rowsHtml += `
          <tr>
            <td style="text-align: center;">${Utils.toBanglaNumber(sl++, false)}</td>
            <td style="text-align: center;">${dateBangla}</td>
            <td>
              <strong>${item.name}</strong>
            </td>
            <td style="text-align: center;">${Utils.toBanglaNumber(item.qty)}</td>
            <td style="text-align: right;">${Utils.formatCurrency(item.rate)}</td>
            <td style="text-align: right; font-weight: 700;">${Utils.formatCurrency(item.total)}</td>
          </tr>
        `;
      });
    } else if (data.todayBill > 0) {
      rowsHtml += `
        <tr>
          <td style="text-align: center;">${Utils.toBanglaNumber(sl++, false)}</td>
          <td style="text-align: center;">${dateBangla}</td>
          <td>
            <strong>আজকের নতুন মালামাল ও সাধারণ কেনাকাটা</strong>
          </td>
          <td style="text-align: center;">১</td>
          <td style="text-align: right;">${Utils.formatCurrency(data.todayBill)}</td>
          <td style="text-align: right; font-weight: 700;">${Utils.formatCurrency(data.todayBill)}</td>
        </tr>
      `;
    }

    container.innerHTML = `
      <div class="a4-memo-container" style="position: relative;">
        <!-- Status Stamp (PAID or DUE) -->
        ${statusStampMarkup}

        <!-- Shop Header -->
        <div class="a4-shop-header">
          <div class="a4-shop-brand-left">
            <div class="a4-shop-logo-badge">
              <img src="icons/favicon.svg" alt="লোগো">
            </div>
            <div>
              <h1 class="a4-shop-name">${shopName}</h1>
              <div class="a4-shop-tagline">${tagline}</div>
              <div class="a4-shop-owner">স্বত্বাধিকারী: <strong>${ownerName}</strong></div>
            </div>
          </div>
          <div class="a4-shop-contact-right">
            <div class="a4-contact-item">📞 মোবাইল: <strong>${phone}</strong></div>
            ${email ? `<div class="a4-contact-item">✉️ ইমেইল: ${email}</div>` : ''}
            <div class="a4-contact-item">📍 ঠিকানা: ${address}</div>
            <div class="a4-app-brand-badge">সহজ খাতা ভেরিফাইড চালান ✓</div>
          </div>
        </div>

        <div class="a4-header-rule"></div>

        <!-- Memo Title Banner -->
        <div class="a4-memo-title-banner">
          <span>ক্যাশ মেমো ও বিক্রয় চালান (পুরাতন জের সমন্বিত)</span>
        </div>

        <!-- Customer & Memo Meta Grid -->
        <div class="a4-meta-grid">
          <div class="a4-meta-col">
            <div class="a4-box-title">গ্রাহক / কাস্টমারের তথ্য</div>
            <div class="a4-meta-row"><span>গ্রাহকের নাম:</span> <strong id="memoCustomerNameDisplay">${data.customerName}</strong></div>
            <div class="a4-meta-row"><span>মোবাইল নম্বর:</span> <strong id="memoCustomerPhoneDisplay">${data.customerPhone || 'প্রযোজ্য নয়'}</strong></div>
            <div class="a4-meta-row"><span>হিসাবের ধরন:</span> <span>কাস্টমার ক্যাশ ও বাকি মেমো</span></div>
          </div>
          <div class="a4-meta-col">
            <div class="a4-box-title">মেমোর বিবরণ</div>
            <div class="a4-meta-row"><span>মেমো নং:</span> <strong style="color: #0F5132;">${data.memoNo}</strong></div>
            <div class="a4-meta-row"><span>তারিখ:</span> <strong>${dateBangla}</strong></div>
            <div class="a4-meta-row"><span>প্রস্তুতকারক:</span> <span>${ownerName}</span></div>
          </div>
        </div>

        <!-- Items Table -->
        <table class="a4-memo-table">
          <thead>
            <tr>
              <th style="width: 38px; text-align: center;">ক্র.নং</th>
              <th style="width: 85px; text-align: center;">তারিখ</th>
              <th>বিবরণ ও পণ্যের নাম</th>
              <th style="width: 70px; text-align: center;">পরিমাণ</th>
              <th style="width: 85px; text-align: right;">দর (৳)</th>
              <th style="width: 105px; text-align: right;">মোট মূল্য (৳)</th>
            </tr>
          </thead>
          <tbody>
            ${rowsHtml}
          </tbody>
        </table>

        <!-- Bottom Calculation and In-Words Box -->
        <div class="a4-financials-container">
          <div class="a4-in-words-box">
            <div style="font-weight: 700; color: #0F5132; margin-bottom: 4px; font-size: 9.5pt;">কথায় (In Words):</div>
            <div style="font-size: 10.5pt; font-weight: 700; color: #111827;">${data.inWords}</div>
            
            <div class="a4-terms-box">
              <div style="font-weight: 700; margin-bottom: 2px;">শর্তাবলী ও নোটিশ:</div>
              <div>১. মালামাল সঠিকভাবে যাচাই করে গ্রহণ করুন। বিক্রিত পণ্য ফেরত নেওয়া হয় না।</div>
              <div>২. পূর্বের জের ৳ ${Utils.toBanglaNumber(data.oldDue)} সহ বর্তমান অবশিষ্ট বকেয়া ৳ ${Utils.toBanglaNumber(data.netDue)} অতিসত্বর পরিশোধযোগ্য।</div>
              <div>৩. এটি “সহজ খাতা” সফটওয়্যার দ্বারা প্রস্তুতকৃত অফিশিয়াল সীলমোহরযুক্ত কম্পিউটারাইজড মেমো।</div>
            </div>
          </div>

          <div class="a4-summary-card">
            <div class="a4-summary-line" style="color: #92400E; font-weight: 700;">
              <span>১. পূর্বের পুরাতন বকেয়া:</span>
              <span>${Utils.formatCurrency(data.oldDue)}</span>
            </div>
            <div class="a4-summary-line">
              <span>২. আজকের নতুন বিল:</span>
              <span>${Utils.formatCurrency(data.todayBill)}</span>
            </div>
            <div class="a4-summary-line" style="border-top: 1px dashed #059669; margin-top: 4px; padding-top: 5px; font-weight: 800; color: #0F5132; font-size: 11pt;">
              <span>সর্বমোট পাওনা (১ + ২):</span>
              <span>${Utils.formatCurrency(data.totalDue)}</span>
            </div>
            <div class="a4-summary-line" style="color: #059669; font-weight: 700;">
              <span>৩. আজকের নগদ জমা:</span>
              <span>- ${Utils.formatCurrency(data.todayPaid)}</span>
            </div>
            <div class="a4-summary-line a4-summary-due-highlight" style="font-size: 12.5pt;">
              <span>বর্তমান সর্বমোট বকেয়া:</span>
              <strong style="color: ${data.netDue > 0 ? '#DC2626' : '#059669'};">${Utils.formatCurrency(data.netDue)}</strong>
            </div>
          </div>
        </div>

        <!-- Signatures Footer with Official Rubber Seal -->
        <div class="a4-signatures-footer" style="position: relative;">
          <div class="a4-sign-block">
            <div class="a4-sign-line">গ্রাহক / কাস্টমারের স্বাক্ষর</div>
          </div>
          <div class="a4-sign-center">
            <div class="a4-digital-stamp">
              <div style="font-size: 8pt; color: #0F5132; font-weight: 800;">সহজ খাতা</div>
              <div style="font-size: 6.5pt; color: #666;">ডিজিটাল ভাউচার</div>
              <div style="font-size: 6.5pt; color: #059669; font-weight: 700;">✓ অনুমোদিত</div>
            </div>
          </div>
          <div class="a4-sign-block" style="text-align: right; position: relative; min-height: 65px; display: flex; flex-direction: column; justify-content: flex-end;">
            ${sealMarkup}
            <div class="proprietor-signature-facsimile">
              ${ownerName}
            </div>
            <div class="a4-sign-line">দোকানদারের স্বাক্ষর ও সীলমোহর</div>
          </div>
        </div>

        <div class="a4-bottom-note">
          সহজ খাতা — দোকান, ব্যবসা ও সংসারের সহজ হিসাব • স্বত্বাধিকারী: ${ownerName} (${phone})
        </div>
      </div>
    `;

    this.currentMemoData = {
      customerName: data.customerName,
      customerPhone: data.customerPhone,
      remainingDue: data.netDue,
      shopName: shopName,
      includeSeal: data.includeSeal,
      sealColor: this.currentMemoSealColor
    };

    // Render POS Thermal Slip representation
    this.renderThermalReceipt(data);

    // Reset toggle format view to A4 default
    const a4El = document.getElementById('customerMemoSheet');
    const posEl = document.getElementById('customerMemoThermalSheet');
    if (a4El) a4El.style.display = 'block';
    if (posEl) posEl.style.display = 'none';
    const btnText = document.getElementById('memoFormatBtnText');
    if (btnText) btnText.textContent = 'POS স্লিপ ভিউ';
    const btnIcon = document.getElementById('memoFormatBtnIcon');
    if (btnIcon) btnIcon.textContent = '🧾';
  },

  /**
   * Render 58mm/80mm POS Thermal Receipt Format
   */
  renderThermalReceipt(data) {
    const container = document.getElementById('customerMemoThermalSheet');
    if (!container) return;

    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};
    const shopName = data.shopName || acc.businessName || prof.businessName || 'সহজ খাতা স্টোর';
    const ownerName = data.ownerName || acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';
    const phone = data.shopPhone || acc.phone || prof.phone || '0131082-4987';
    const address = data.shopAddress || acc.address || prof.address || 'বাংলাদেশ';
    const tagline = data.shopTagline || 'পাইকারি ও খুচরা বিক্রয় কেন্দ্র';
    const dateBangla = Utils.formatDateBengali(data.date);

    let itemsHtml = '';
    let sl = 1;
    if (data.items && data.items.length > 0) {
      data.items.forEach(item => {
        itemsHtml += `
          <tr>
            <td>${Utils.toBanglaNumber(sl++, false)}. ${item.name}</td>
            <td style="text-align: center;">${Utils.toBanglaNumber(item.qty)}</td>
            <td style="text-align: right;">${Utils.toBanglaNumber(item.rate)}</td>
            <td style="text-align: right; font-weight: 700;">${Utils.toBanglaNumber(item.total)}</td>
          </tr>
        `;
      });
    } else if (data.todayBill > 0) {
      itemsHtml += `
        <tr>
          <td>১. কেনাকাটা</td>
          <td style="text-align: center;">১</td>
          <td style="text-align: right;">${Utils.toBanglaNumber(data.todayBill)}</td>
          <td style="text-align: right; font-weight: 700;">${Utils.toBanglaNumber(data.todayBill)}</td>
        </tr>
      `;
    }

    container.innerHTML = `
      <div class="pos-thermal-header">
        <h2 class="pos-shop-title">${shopName}</h2>
        <div class="pos-shop-sub">${tagline}</div>
        <div class="pos-shop-owner">স্বত্বাধিকারী: ${ownerName}</div>
        <div class="pos-shop-contact">মোবাইল: ${phone}</div>
        ${address ? `<div class="pos-shop-contact">ঠিকানা: ${address}</div>` : ''}
      </div>

      <div class="pos-divider"></div>

      <div class="pos-meta-row">
        <span>মেমো নং: <strong>${data.memoNo}</strong></span>
        <span>তারিখ: <strong>${dateBangla}</strong></span>
      </div>
      <div class="pos-meta-row">
        <span>গ্রাহক: <strong>${data.customerName}</strong></span>
        <span>${data.customerPhone ? `মোবাইল: ${data.customerPhone}` : ''}</span>
      </div>

      <div class="pos-divider"></div>

      <table class="pos-items-table">
        <thead>
          <tr>
            <th>পণ্য</th>
            <th style="text-align: center;">পরিমাণ</th>
            <th style="text-align: right;">দর</th>
            <th style="text-align: right;">মোট</th>
          </tr>
        </thead>
        <tbody>
          ${itemsHtml}
        </tbody>
      </table>

      ${data.oldDue > 0 ? `
        <div class="pos-prev-due-box">
          <div style="display: flex; justify-content: space-between; font-weight: 700; color: #92400E;">
            <span>⚠️ পূর্বের পুরাতন বাকি:</span>
            <span>৳ ${Utils.toBanglaNumber(data.oldDue)}</span>
          </div>
          <div style="font-size: 7.5pt; color: #6B7280; margin-top: 1px;">(অটোমেটিক বর্তমান বিলে যোগ হয়েছে)</div>
        </div>
      ` : ''}

      <div class="pos-divider"></div>

      <div class="pos-totals-row">
        <span>আজকের বিল:</span>
        <span>৳ ${Utils.toBanglaNumber(data.todayBill)}</span>
      </div>

      ${data.oldDue > 0 ? `
        <div class="pos-totals-row">
          <span>পূর্বের বাকি:</span>
          <span>৳ ${Utils.toBanglaNumber(data.oldDue)}</span>
        </div>
      ` : ''}

      <div class="pos-totals-row grand-total">
        <span>সর্বমোট পাওনা:</span>
        <span>৳ ${Utils.toBanglaNumber(data.totalDue)}</span>
      </div>

      <div class="pos-totals-row">
        <span>আজকের নগদ জমা:</span>
        <span>৳ ${Utils.toBanglaNumber(data.todayPaid)}</span>
      </div>

      <div class="pos-totals-row due-amount">
        <span>অবশিষ্ট মোট বকেয়া:</span>
        <span>৳ ${Utils.toBanglaNumber(data.netDue)}</span>
      </div>

      <div class="pos-divider"></div>

      <div style="font-size: 7.8pt; text-align: center; margin: 4px 0; font-weight: 600;">
        কথায়: ${data.inWords}
      </div>

      <div class="pos-barcode-sim">
        *${data.memoNo.replace(/[^0-9]/g, '')}*
      </div>

      <div class="pos-seal-stamp">
        <div style="font-size: 8.5pt; font-weight: 800; letter-spacing: 0.5px;">★ ${shopName} ★</div>
        <div style="font-size: 7.5pt; margin-top: 1px;">স্বত্বাধিকারী: ${ownerName}</div>
        <div style="font-size: 7pt; color: #555; margin-top: 1px;">✓ অফিশিয়াল ডিজিটাল সীলমোহর • তারিখ: ${dateBangla}</div>
      </div>

      <div class="pos-footer-msg">
        <div>*** ধন্যবাদ, আবার আসবেন! ***</div>
        <div style="font-size: 7.5pt; color: #6B7280; margin-top: 2px;">সহজ খাতা সফটওয়্যার দ্বারা প্রস্তুতকৃত</div>
      </div>
    `;
  },

  /**
   * Generate Authentic Circular Official Shop Rubber Seal (SVG with textPath)
   */
  generateOfficialSealSvg(shopName, ownerName, dateBangla, sealColor = 'violet') {
    const safeShop = (shopName || 'সহজ খাতা স্টোর').slice(0, 32);
    const safeOwner = (ownerName || 'রফিকুল ইসলাম').slice(0, 24);
    const safeDate = dateBangla || Utils.formatDateBengali(Utils.getTodayDateString());
    const uid = 'seal_' + Math.floor(10000 + Math.random() * 90000);

    return `
      <div class="a4-shop-seal-wrap seal-color-${sealColor}" id="memoOfficialSeal" title="দোকানের অফিশিয়াল সীলমোহর">
        <svg viewBox="0 0 160 160" width="125" height="125" class="official-rubber-seal-svg">
          <defs>
            <path id="topArc_${uid}" d="M 23 80 A 57 57 0 1 1 137 80" fill="none" />
            <path id="bottomArc_${uid}" d="M 137 80 A 57 57 0 1 1 23 80" fill="none" />
          </defs>
          
          <!-- Outer Solid Ring & Inner Dotted Ring -->
          <circle cx="80" cy="80" r="74" fill="none" stroke="currentColor" stroke-width="2.8" />
          <circle cx="80" cy="80" r="67" fill="none" stroke="currentColor" stroke-width="1.2" stroke-dasharray="3,2" />
          <circle cx="80" cy="80" r="48" fill="none" stroke="currentColor" stroke-width="1.6" />

          <!-- Curved Shop Name -->
          <text font-size="11" font-weight="900" fill="currentColor">
            <textPath href="#topArc_${uid}" startOffset="50%" text-anchor="middle">
              ${safeShop}
            </textPath>
          </text>

          <!-- Curved Bottom Tag -->
          <text font-size="8.5" font-weight="800" fill="currentColor">
            <textPath href="#bottomArc_${uid}" startOffset="50%" text-anchor="middle">
              ★ অফিশিয়াল ক্যাশ মেমো সীল ★
            </textPath>
          </text>

          <!-- Center Content -->
          <g text-anchor="middle" fill="currentColor">
            <text x="80" y="65" font-size="8" font-weight="700">স্বত্বাধিকারী</text>
            <text x="80" y="79" font-size="10.5" font-weight="900">${safeOwner}</text>
            <text x="80" y="92" font-size="8" font-weight="700">${safeDate}</text>
            <text x="80" y="103" font-size="7.5" font-weight="800">✓ অনুমোদিত</text>
          </g>
        </svg>
      </div>
    `;
  },

  /**
   * Generate Diagonal Rubber Stamp for Payment Status (PAID or DUE)
   */
  generateStatusStampHtml(netDue, todayPaid, totalDue) {
    if (netDue <= 0 && (todayPaid > 0 || totalDue > 0)) {
      return `
        <div class="a4-status-rubber-stamp stamp-paid" id="memoStatusStamp">
          <span>✓ সম্পূর্ণ পরিশোধিত</span>
          <div style="font-size: 8.5pt; font-weight: 800; letter-spacing: 1.5px;">PAID IN FULL</div>
        </div>
      `;
    } else if (netDue > 0) {
      return `
        <div class="a4-status-rubber-stamp stamp-due" id="memoStatusStamp">
          <span>⚠️ বকেয়া বাকি চালান</span>
          <div style="font-size: 8pt; font-weight: 800; letter-spacing: 1px;">CREDIT / DUE</div>
        </div>
      `;
    }
    return '';
  },

  /**
   * Toggle Memo Seal Visibility or Cycle Ink Colors
   */
  toggleMemoSeal() {
    const sealEl = document.getElementById('memoOfficialSeal');
    const statusEl = document.getElementById('memoStatusStamp');
    const btnText = document.getElementById('memoSealBtnText');
    const btnIcon = document.getElementById('memoSealBtnIcon');

    const colorCycle = ['violet', 'blue', 'green', 'red', 'off'];
    const colorNames = {
      violet: 'সীল: বেগুনি',
      blue: 'সীল: নীল',
      green: 'সীল: সবুজ',
      red: 'সীল: লাল',
      off: 'সীল: বন্ধ'
    };
    const colorIcons = {
      violet: '🟣',
      blue: '🔵',
      green: '🟢',
      red: '🔴',
      off: '⚪'
    };

    let currentIndex = colorCycle.indexOf(this.currentMemoSealColor || 'violet');
    let nextIndex = (currentIndex + 1) % colorCycle.length;
    this.currentMemoSealColor = colorCycle[nextIndex];

    if (this.currentMemoSealColor === 'off') {
      if (sealEl) sealEl.style.display = 'none';
      if (statusEl) statusEl.style.display = 'none';
      if (btnText) btnText.textContent = colorNames.off;
      if (btnIcon) btnIcon.textContent = colorIcons.off;
      UI.toast('মেমোর সীলমোহর বন্ধ করা হয়েছে', 'info', 1800);
    } else {
      if (sealEl) {
        sealEl.style.display = 'block';
        sealEl.className = `a4-shop-seal-wrap seal-color-${this.currentMemoSealColor}`;
      }
      if (statusEl) {
        statusEl.style.display = 'block';
      }
      if (btnText) btnText.textContent = colorNames[this.currentMemoSealColor];
      if (btnIcon) btnIcon.textContent = colorIcons[this.currentMemoSealColor];
      UI.toast(`সীলমোহর: ${colorNames[this.currentMemoSealColor]} এ পরিবর্তিত হয়েছে ✓`, 'success', 1800);
    }
  },

  /**
   * Toggle between A4 sheet and POS thermal receipt
   */
  toggleMemoFormat() {
    const a4El = document.getElementById('customerMemoSheet');
    const posEl = document.getElementById('customerMemoThermalSheet');
    const btnText = document.getElementById('memoFormatBtnText');
    const btnIcon = document.getElementById('memoFormatBtnIcon');
    if (!a4El || !posEl) return;

    const isA4Visible = a4El.style.display !== 'none';
    if (isA4Visible) {
      a4El.style.display = 'none';
      posEl.style.display = 'block';
      if (btnText) btnText.textContent = '📄 A4 মেমো ভিউ';
      if (btnIcon) btnIcon.textContent = '📄';
      UI.toast('থার্মাল POS স্লিপ ফরম্যাটে পরিবর্তিত হয়েছে', 'info', 2000);
    } else {
      a4El.style.display = 'block';
      posEl.style.display = 'none';
      if (btnText) btnText.textContent = '🧾 POS স্লিপ ভিউ';
      if (btnIcon) btnIcon.textContent = '🧾';
      UI.toast('A4 মেমো ভাউচার ফরম্যাটে পরিবর্তিত হয়েছে', 'info', 2000);
    }
  },

  /**
   * ==========================================
   * A4 CUSTOMER CASH MEMO & PDF GENERATOR
   * ==========================================
   */
  async openCustomerMemoModal(debtId) {
    const debt = this.state.debts.find(d => d.id === debtId);
    if (!debt) {
      UI.toast('হিসাবের তথ্য পাওয়া যায়নি', 'error');
      return;
    }

    const otherPrev = this.getCustomerPreviousDue(debt.person, debt.id);
    const oldDue = otherPrev.totalDue;
    const currentDebtAmount = debt.amount || 0;
    const currentDebtPaid = debt.paidAmount || 0;
    const currentDebtRemaining = debt.remainingAmount !== undefined ? debt.remainingAmount : Math.max(0, currentDebtAmount - currentDebtPaid);

    const totalDue = Math.round((oldDue + currentDebtAmount) * 100) / 100;
    const totalPaid = currentDebtPaid;
    const netDue = Math.max(0, Math.round((totalDue - totalPaid) * 100) / 100);

    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};

    const memoData = {
      memoNo: `MEMO-${(debt.date || Utils.getTodayDateString()).replace(/[^0-9]/g, '')}-${debt.id.slice(-4).toUpperCase()}`,
      shopName: acc.businessName || prof.businessName || 'সহজ খাতা স্টোর',
      ownerName: acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম',
      shopPhone: acc.phone || prof.phone || '0131082-4987',
      shopAddress: acc.address || prof.address || 'বাংলাদেশ',
      shopTagline: 'দোকান, ব্যবসা ও পাইকারি-খুচরা হিসাব কেন্দ্র',
      includeSeal: true,
      sealColor: 'violet',
      customerName: debt.person,
      customerPhone: debt.phone || otherPrev.phone || '',
      date: debt.date || Utils.getTodayDateString(),
      oldDue: oldDue,
      todayBill: currentDebtAmount,
      totalDue: totalDue,
      todayPaid: totalPaid,
      netDue: netDue,
      items: [
        {
          name: debt.note || (debt.type === 'RECEIVABLE' ? 'পণ্য কেনাকাটা ও বাকি বিল' : 'পণ্য ক্রয় চালান'),
          qty: 1,
          rate: currentDebtAmount,
          total: currentDebtAmount
        }
      ],
      inWords: Utils.numberToBanglaWords(netDue > 0 ? netDue : (totalPaid > 0 ? totalPaid : totalDue))
    };

    this.renderGeneratedA4Memo(memoData);
    UI.openModal('customerMemoModal');
  },

  /* ==========================================
   * BUSINESS CARD / VISITING CARD GENERATOR
   * ========================================== */
  currentBizCardTheme: 'royal-blue',
  currentBizCardSide: 'front',

  openBusinessCardModal() {
    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};

    const shopName = acc.businessName || prof.businessName || 'সহজ খাতা স্টোর';
    const ownerName = acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';
    const phone = acc.phone || prof.phone || '0131082-4987';
    const address = acc.address || prof.address || 'দোকান নং-৫, বাজার রোড, সদর';
    const email = acc.email || prof.email || '';

    const nameInput = document.getElementById('bcShopNameInput');
    if (nameInput) nameInput.value = shopName;
    const ownerInput = document.getElementById('bcOwnerNameInput');
    if (ownerInput) ownerInput.value = ownerName;
    const titleInput = document.getElementById('bcOwnerTitleInput');
    if (titleInput && !titleInput.value) titleInput.value = 'স্বত্বাধিকারী';
    const taglineInput = document.getElementById('bcTaglineInput');
    if (taglineInput && !taglineInput.value) taglineInput.value = 'এখানে পাইকারী ও খুচরা সকল প্রকার পণ্য সুলভ মূল্যে পাওয়া যায়।';
    const phone1Input = document.getElementById('bcPhone1Input');
    if (phone1Input) phone1Input.value = phone;
    const addressInput = document.getElementById('bcAddressInput');
    if (addressInput) addressInput.value = address;
    const emailInput = document.getElementById('bcEmailInput');
    if (emailInput) emailInput.value = email;
    const payPhoneInput = document.getElementById('bcPaymentPhoneInput');
    if (payPhoneInput) payPhoneInput.value = phone;

    this.currentBizCardTheme = 'royal-blue';
    this.currentBizCardSide = 'front';

    this.renderBusinessCardPreview();
    UI.openModal('businessCardModal');
  },

  setBusinessCardTemplate(theme) {
    this.currentBizCardTheme = theme;
    document.querySelectorAll('.card-theme-option').forEach(btn => {
      if (btn.getAttribute('data-theme') === theme) {
        btn.classList.add('active');
      } else {
        btn.classList.remove('active');
      }
    });
    this.renderBusinessCardPreview();
  },

  setBusinessCardSide(side) {
    this.currentBizCardSide = side;
    const btnFront = document.getElementById('btnCardSideFront');
    const btnBack = document.getElementById('btnCardSideBack');
    if (btnFront && btnBack) {
      if (side === 'front') {
        btnFront.classList.add('active');
        btnBack.classList.remove('active');
      } else {
        btnBack.classList.add('active');
        btnFront.classList.remove('active');
      }
    }
    this.renderBusinessCardPreview();
  },

  renderBusinessCardPreview() {
    const cardEl = document.getElementById('bizCardElement');
    if (!cardEl) return;

    const shopName = document.getElementById('bcShopNameInput')?.value.trim() || 'সহজ খাতা স্টোর';
    const ownerName = document.getElementById('bcOwnerNameInput')?.value.trim() || 'রফিকুল ইসলাম';
    const ownerTitle = document.getElementById('bcOwnerTitleInput')?.value.trim() || 'স্বত্বাধিকারী';
    const tagline = document.getElementById('bcTaglineInput')?.value.trim() || 'দোকান, ব্যবসা ও পাইকারি-খুচরা কেন্দ্র';
    const phone1 = document.getElementById('bcPhone1Input')?.value.trim() || '০১৩১০-৮২৪৯৮৭';
    const phone2 = document.getElementById('bcPhone2Input')?.value.trim() || '';
    const address = document.getElementById('bcAddressInput')?.value.trim() || 'বাজার, বাংলাদেশ';
    const email = document.getElementById('bcEmailInput')?.value.trim() || '';
    const payPhone = document.getElementById('bcPaymentPhoneInput')?.value.trim() || phone1;

    // Set container class for theme and side
    cardEl.className = `biz-card-canvas theme-${this.currentBizCardTheme} side-${this.currentBizCardSide}`;

    if (this.currentBizCardSide === 'front') {
      // FRONT FACE OF BUSINESS CARD
      cardEl.innerHTML = `
        <div class="bc-watermark-shape"></div>
        <div class="bc-watermark-lines"></div>

        <div class="bc-header-group">
          <div class="bc-top-meta">
            <span class="bc-gold-accent">★ প্রফেশনাল বিজনেস কার্ড</span>
            <span>সহজ খাতা আইডি: BIZ-${Math.abs(shopName.split('').reduce((a,b)=>{a=((a<<5)-a)+b.charCodeAt(0);return a&a},0)).toString().slice(0, 5)}</span>
          </div>
          <h2 class="bc-shop-title">${shopName}</h2>
          <div class="bc-shop-tagline">${tagline}</div>
          <div class="bc-metallic-divider"></div>
          <div class="bc-owner-row">
            <span class="bc-owner-name">${ownerName}</span>
            <span class="bc-owner-title">${ownerTitle}</span>
            <span style="font-size: 0.85rem; color: #34D399;" title="ভেরিফাইড মার্চেন্ট">✓</span>
          </div>
        </div>

        <div class="bc-contacts-grid">
          <div class="bc-contact-item" title="ফোন">
            <span class="bc-contact-icon">📞</span>
            <span>${phone1}</span>
          </div>
          ${phone2 ? `
            <div class="bc-contact-item" title="হোয়াটসঅ্যাপ / অপর নম্বর">
              <span class="bc-contact-icon">💬</span>
              <span>${phone2}</span>
            </div>
          ` : (email ? `
            <div class="bc-contact-item" title="ইমেইল">
              <span class="bc-contact-icon">✉️</span>
              <span>${email}</span>
            </div>
          ` : `
            <div class="bc-contact-item" title="হোয়াটসঅ্যাপ">
              <span class="bc-contact-icon">💬</span>
              <span>${phone1}</span>
            </div>
          `)}
          <div class="bc-contact-item" style="grid-column: span 2;" title="ঠিকানা">
            <span class="bc-contact-icon">📍</span>
            <span style="white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${address}</span>
          </div>
        </div>
      `;
    } else {
      // BACK FACE OF BUSINESS CARD
      const qrData = encodeURIComponent(`https://wa.me/88${phone1.replace(/[^0-9]/g, '')}?text=${encodeURIComponent(`আসসালামু আলাইকুম! ${shopName} এর সাথে যোগাযোগ করতে চাই।`)}`);
      const qrUrl = `https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=${qrData}`;

      cardEl.innerHTML = `
        <div class="bc-watermark-shape"></div>
        <div class="bc-watermark-lines"></div>

        <div class="bc-back-container">
          <div class="bc-back-logo-circle">🏪</div>
          <h3 class="bc-back-shop-name">${shopName}</h3>
          <p class="bc-back-instructions">মোবাইল ক্যামেরা দিয়ে QR কোড স্ক্যান করে যোগাযোগ করুন</p>
          
          <div class="bc-qr-box">
            <img src="${qrUrl}" alt="QR কোড" class="bc-qr-img" onerror="this.onerror=null; this.src='icons/favicon.svg';">
          </div>

          <div class="bc-payment-badges">
            <span class="bc-payment-pill">বিকাশ</span>
            <span class="bc-payment-pill">নগদ</span>
            <span class="bc-payment-pill">রকেট</span>
            <span style="font-size: 0.72rem; margin-left: 2px;">পেমেন্ট: ${payPhone}</span>
          </div>

          <div style="font-size: 0.68rem; opacity: 0.8; margin-top: 2px;">
            সহজ খাতা ডিজিটাল প্ল্যাটফর্মে নিবন্ধিত প্রতিষ্ঠান ✓
          </div>
        </div>
      `;
    }
  },

  async saveBusinessCardProfile() {
    const shopName = document.getElementById('bcShopNameInput')?.value.trim();
    const ownerName = document.getElementById('bcOwnerNameInput')?.value.trim();
    const phone = document.getElementById('bcPhone1Input')?.value.trim();
    const address = document.getElementById('bcAddressInput')?.value.trim();
    const email = document.getElementById('bcEmailInput')?.value.trim();

    if (!shopName || !ownerName) {
      UI.toast('দোকানের নাম ও স্বত্বাধিকারীর নাম পূরণ করুন', 'error');
      return;
    }

    if (this.state.activeAccount) {
      this.state.activeAccount.businessName = shopName;
      this.state.activeAccount.ownerName = ownerName;
      if (phone) this.state.activeAccount.phone = phone;
      if (address) this.state.activeAccount.address = address;
      if (email) this.state.activeAccount.email = email;
      await khataDB.put('accounts', this.state.activeAccount);
    }

    if (this.state.profile) {
      this.state.profile.businessName = shopName;
      this.state.profile.ownerName = ownerName;
      if (phone) this.state.profile.phone = phone;
      if (address) this.state.profile.address = address;
      if (email) this.state.profile.email = email;
      await khataDB.put('profile', this.state.profile);
    }

    this.updateSidebarAccountInfo();
    UI.toast('দোকান ও স্বত্বাধিকারীর তথ্য সফলভাবে সেভ করা হয়েছে ✓', 'success');
  },

  printBusinessCard() {
    window.print();
  },

  downloadBusinessCardImage() {
    const cardEl = document.getElementById('bizCardElement');
    if (!cardEl) return;

    const shopName = document.getElementById('bcShopNameInput')?.value.trim() || 'Shop';
    const cleanName = shopName.replace(/[\s\/\\]/g, '_');
    const side = this.currentBizCardSide;
    const filename = `Visiting_Card_${cleanName}_${side}.png`;

    if (typeof html2canvas === 'undefined') {
      UI.toast('প্রিন্ট অপশন থেকে কার্ডটি সংরক্ষণ করুন', 'info');
      window.print();
      return;
    }

    UI.toast('কার্ডের হাই-কোয়ালিটি ছবি তৈরি হচ্ছে...', 'info', 2000);

    html2canvas(cardEl, {
      scale: 3,
      useCORS: true,
      backgroundColor: null,
      logging: false
    }).then(canvas => {
      const link = document.createElement('a');
      link.download = filename;
      link.href = canvas.toDataURL('image/png');
      link.click();
      UI.toast('ভিজিটিং কার্ড (PNG ছবি) সফলভাবে ডাউনলোড হয়েছে ✓', 'success');
    }).catch(err => {
      console.error('Image capture error:', err);
      UI.toast('সরাসরি প্রিন্ট উইন্ডো ব্যবহার করুন', 'info');
      window.print();
    });
  },

  downloadBusinessCardPDF() {
    const cardEl = document.getElementById('bizCardElement');
    if (!cardEl) return;

    const shopName = document.getElementById('bcShopNameInput')?.value.trim() || 'Shop';
    const cleanName = shopName.replace(/[\s\/\\]/g, '_');
    const side = this.currentBizCardSide;
    const filename = `Visiting_Card_${cleanName}_${side}.pdf`;

    if (typeof html2pdf === 'undefined') {
      window.print();
      return;
    }

    UI.toast('PDF বিজনেস কার্ড তৈরি হচ্ছে...', 'info', 2000);

    const opt = {
      margin: [5, 5, 5, 5],
      filename: filename,
      image: { type: 'jpeg', quality: 0.98 },
      html2canvas: { scale: 3, useCORS: true, letterRendering: true, logging: false },
      jsPDF: { unit: 'in', format: [3.5, 2.0], orientation: 'landscape' }
    };

    html2pdf().set(opt).from(cardEl).save().then(() => {
      UI.toast('PDF কার্ড সফলভাবে ডাউনলোড হয়েছে ✓', 'success');
    }).catch(err => {
      console.error('PDF error:', err);
      window.print();
    });
  },

  shareBusinessCardWhatsApp() {
    const shopName = document.getElementById('bcShopNameInput')?.value.trim() || 'সহজ খাতা স্টোর';
    const ownerName = document.getElementById('bcOwnerNameInput')?.value.trim() || 'রফিকুল ইসলাম';
    const phone = document.getElementById('bcPhone1Input')?.value.trim() || '০১৩১০-৮২৪৯৮৭';
    const address = document.getElementById('bcAddressInput')?.value.trim() || '';
    const tagline = document.getElementById('bcTaglineInput')?.value.trim() || '';

    const msg = `🏪 *${shopName}*\n👤 স্বত্বাধিকারী: ${ownerName}\n📞 মোবাইল: ${phone}\n📍 ঠিকানা: ${address}\n✨ ${tagline}\n\nধন্যবাদ! আমাদের সাথে যোগাযোগ করতে পারেন। (সহজ খাতা ডিজিটাল কার্ড)`;
    const url = `https://wa.me/?text=${encodeURIComponent(msg)}`;
    window.open(url, '_blank');
  },

  printCustomerMemo() {
    window.print();
  },

  downloadCustomerMemoPDF() {
    const isThermal = document.getElementById('customerMemoThermalSheet')?.style.display !== 'none';
    const element = isThermal ? document.getElementById('customerMemoThermalSheet') : document.getElementById('customerMemoSheet');
    if (!element) {
      UI.toast('মেমোর তথ্য পাওয়া যায়নি', 'error');
      return;
    }

    const customerName = this.currentMemoData?.customerName || 'Customer';
    const cleanName = customerName.replace(/[\s\/\\]/g, '_');
    const dateStr = Utils.getTodayDateString();
    const filename = `${isThermal ? 'POS_Receipt' : 'Cash_Memo'}_${cleanName}_${dateStr}.pdf`;

    if (typeof html2pdf === 'undefined') {
      UI.toast('ব্রাউজারের প্রিন্ট উইন্ডো থেকে "Save as PDF" নির্বাচন করুন', 'info', 3500);
      window.print();
      return;
    }

    UI.toast('PDF মেমো তৈরি হচ্ছে, অনুগ্রহ করে অপেক্ষা করুন...', 'info', 2500);

    const opt = isThermal ? {
      margin: [4, 4, 4, 4],
      filename: filename,
      image: { type: 'jpeg', quality: 0.98 },
      html2canvas: { scale: 2.5, useCORS: true, letterRendering: true, logging: false },
      jsPDF: { unit: 'mm', format: [80, 220], orientation: 'portrait' }
    } : {
      margin: [5, 8, 5, 8],
      filename: filename,
      image: { type: 'jpeg', quality: 0.98 },
      html2canvas: { scale: 2, useCORS: true, letterRendering: true, logging: false },
      jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' },
      pagebreak: { mode: ['avoid-all', 'css', 'legacy'] }
    };

    html2pdf().set(opt).from(element).save().then(() => {
      UI.toast('PDF মেমো সফলভাবে ডাউনলোড হয়েছে ✓', 'success');
    }).catch(err => {
      console.error('PDF error:', err);
      UI.toast('সরাসরি প্রিন্ট উইন্ডো থেকে PDF সংরক্ষণ করুন', 'info');
      window.print();
    });
  },

  shareCustomerMemoWhatsApp() {
    if (!this.currentMemoData) return;
    const { customerName, customerPhone, remainingDue, shopName } = this.currentMemoData;
    const phone = (customerPhone || '').replace(/[^0-9]/g, '');
    const msg = `আসসালামু আলাইকুম ${customerName}, ${shopName}-এ আপনার বর্তমান বাকি হিসাব ৳ ${Utils.toBanglaNumber(remainingDue)} টাকা। অনুগ্রহ করে সময়মতো পরিশোধ করার অনুরোধ করা হলো। ধন্যবাদ! - ${shopName}`;
    const url = phone ? `https://wa.me/88${phone}?text=${encodeURIComponent(msg)}` : `https://wa.me/?text=${encodeURIComponent(msg)}`;
    window.open(url, '_blank');
  },

  openSingleTxMemoModal(txId) {
    const tx = this.state.transactions.find(t => t.id === txId);
    if (!tx) {
      UI.toast('লেনদেনের তথ্য পাওয়া যায়নি', 'error');
      return;
    }

    const container = document.getElementById('customerMemoSheet');
    if (!container) return;

    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};

    const shopName = acc.businessName || prof.businessName || 'সহজ খাতা স্টোর';
    const ownerName = acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';
    const phone = acc.phone || prof.phone || '0131082-4987';
    const email = acc.email || prof.email || 'rafiqulislam.globalwork@gmail.com';
    const address = acc.address || prof.address || 'বাংলাদেশ';

    const customerName = tx.person || 'সম্মানিত ক্রেতা';
    const memoNo = `MEMO-${(tx.date || Utils.getTodayDateString()).replace(/[^0-9]/g, '')}-${tx.id.slice(-4).toUpperCase()}`;
    const todayBangla = Utils.formatDateBengali(tx.date || Utils.getTodayDateString());
    const amountInWords = Utils.numberToBanglaWords(tx.amount);

    const isIncome = tx.type === 'INCOME';
    const memoTitle = isIncome ? 'নগদ বিক্রয় ক্যাশ মেমো' : 'ব্যয় / খরচ ভাউচার';

    container.innerHTML = `
      <div class="a4-memo-container" style="position: relative;">
        <!-- Status Rubber Stamp -->
        ${this.generateStatusStampHtml(0, tx.amount, tx.amount)}

        <!-- Shop Header -->
        <div class="a4-shop-header">
          <div class="a4-shop-brand-left">
            <div class="a4-shop-logo-badge">
              <img src="icons/favicon.svg" alt="লোগো">
            </div>
            <div>
              <h1 class="a4-shop-name">${shopName}</h1>
              <div class="a4-shop-tagline">দোকান, ব্যবসা ও পাইকারি-খুচরা হিসাব কেন্দ্র</div>
              <div class="a4-shop-owner">স্বত্বাধিকারী: <strong>${ownerName}</strong></div>
            </div>
          </div>
          <div class="a4-shop-contact-right">
            <div class="a4-contact-item">📞 মোবাইল: <strong>${phone}</strong></div>
            ${email ? `<div class="a4-contact-item">✉️ ইমেইল: ${email}</div>` : ''}
            <div class="a4-contact-item">📍 ঠিকানা: ${address}</div>
            <div class="a4-app-brand-badge">সহজ খাতা ভেরিফাইড মেমো ✓</div>
          </div>
        </div>

        <div class="a4-header-rule"></div>

        <!-- Memo Title Banner -->
        <div class="a4-memo-title-banner">
          <span>${memoTitle}</span>
        </div>

        <!-- Customer & Memo Meta Grid -->
        <div class="a4-meta-grid">
          <div class="a4-meta-col">
            <div class="a4-box-title">গ্রাহকের তথ্য</div>
            <div class="a4-meta-row"><span>গ্রাহকের নাম:</span> <strong id="memoCustomerNameDisplay">${customerName}</strong></div>
            <div class="a4-meta-row"><span>লেনদেনের খাত:</span> <span>${tx.categoryName || tx.category}</span></div>
            <div class="a4-meta-row"><span>পরিশোধ মাধ্যম:</span> <span>${tx.paymentMethod || 'Cash'}</span></div>
          </div>
          <div class="a4-meta-col">
            <div class="a4-box-title">মেমো বিবরণ</div>
            <div class="a4-meta-row"><span>মেমো নং:</span> <strong style="color: #0F5132;">${memoNo}</strong></div>
            <div class="a4-meta-row"><span>তারিখ:</span> <strong>${todayBangla}</strong></div>
            <div class="a4-meta-row"><span>ভাউচার / রেফারেন্স নং:</span> <span>${tx.refNo || 'নেই'}</span></div>
          </div>
        </div>

        <!-- Ledger Table -->
        <table class="a4-memo-table">
          <thead>
            <tr>
              <th style="width: 40px; text-align: center;">ক্র.নং</th>
              <th style="width: 90px;">তারিখ</th>
              <th>বিবরণ ও পণ্যের বিবরণ</th>
              <th style="width: 80px; text-align: center;">মাধ্যম</th>
              <th style="width: 100px; text-align: right;">মোট মূল্য (৳)</th>
              <th style="width: 100px; text-align: right;">নগদ পরিশোধ (৳)</th>
              <th style="width: 90px; text-align: right;">বাকি (৳)</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td style="text-align: center;">১</td>
              <td>${todayBangla}</td>
              <td>
                <strong>${tx.categoryName || tx.category}</strong>
                ${tx.description ? `<div style="font-size: 8.5pt; color: #555;">${tx.description}</div>` : ''}
              </td>
              <td style="text-align: center;"><span class="badge badge-success" style="font-size:8pt;">${tx.paymentMethod || 'Cash'}</span></td>
              <td style="text-align: right; font-weight: 700;">${Utils.formatCurrency(tx.amount)}</td>
              <td style="text-align: right; font-weight: 700; color: #059669;">${Utils.formatCurrency(tx.amount)}</td>
              <td style="text-align: right; color: #6B7280;">৳ ০</td>
            </tr>
          </tbody>
        </table>

        <!-- Bottom Calculation and In-Words Box -->
        <div class="a4-financials-container">
          <div class="a4-in-words-box">
            <div style="font-weight: 700; color: #0F5132; margin-bottom: 4px; font-size: 9.5pt;">কথায় (In Words):</div>
            <div style="font-size: 10.5pt; font-weight: 700; color: #111827;">${amountInWords}</div>
            
            <div class="a4-terms-box">
              <div style="font-weight: 700; margin-bottom: 2px;">শর্তাবলী:</div>
              <div>১. ক্যাশ মেমো অনুযায়ী পণ্য বুঝে পেয়েছি।</div>
              <div>২. কোনো প্রকার অসামঞ্জস্য থাকলে অবিলম্বে যোগাযোগ করুন।</div>
              <div>৩. সহজ খাতা সফটওয়্যার দ্বারা প্রস্তুতকৃত কম্পিউটারাইজড রসিদ।</div>
            </div>
          </div>

          <div class="a4-summary-card">
            <div class="a4-summary-line">
              <span>মোট বিল:</span>
              <strong>${Utils.formatCurrency(tx.amount)}</strong>
            </div>
            <div class="a4-summary-line">
              <span>পরিশোধিত অর্থ:</span>
              <strong style="color: #059669;">${Utils.formatCurrency(tx.amount)}</strong>
            </div>
            <div class="a4-summary-line a4-summary-due-highlight" style="color: #059669;">
              <span>সর্বমোট বকেয়া:</span>
              <strong>৳ ০</strong>
            </div>
          </div>
        </div>

        <!-- Signatures Footer with Seal -->
        <div class="a4-signatures-footer" style="position: relative; margin-top: 1.8rem;">
          <div class="a4-sign-block">
            <div class="a4-sign-line">গ্রাহকের স্বাক্ষর</div>
          </div>
          <div class="a4-sign-center">
            <div class="a4-digital-stamp">
              <div style="font-size: 8pt; color: #0F5132; font-weight: 800;">সহজ খাতা</div>
              <div style="font-size: 6.5pt; color: #059669; font-weight: 700;">পরিশোধিত ✓</div>
            </div>
          </div>
          <div class="a4-sign-block" style="text-align: right; position: relative; min-height: 85px; display: flex; flex-direction: column; justify-content: flex-end;">
            ${this.generateOfficialSealSvg(shopName, ownerName, todayBangla, 'violet')}
            <div class="proprietor-signature-facsimile">
              ${ownerName}
            </div>
            <div class="a4-sign-line">দোকানদারের স্বাক্ষর ও সীলমোহর</div>
          </div>
        </div>

        <div class="a4-bottom-note">
          সহজ খাতা — দোকান, ব্যবসা ও সংসারের সহজ হিসাব • প্রস্তুতকারক: ${shopName} (${phone})
        </div>
      </div>
    `;

    this.currentMemoData = {
      customerName: customerName,
      customerPhone: '',
      remainingDue: 0,
      shopName: shopName
    };

    UI.openModal('customerMemoModal');
  },

  /**
   * ==========================================
   * PRODUCT INVENTORY & STOCK MANAGEMENT
   * ==========================================
   */

  /**
   * Refresh datalist of products for memo autocomplete
   */
  updateProductsDatalist() {
    const datalist = document.getElementById('productsDatalist');
    if (!datalist) return;
    const products = this.state.products || [];
    datalist.innerHTML = products.map(p => {
      const stockTxt = Utils.toBanglaNumber(p.currentStock || 0);
      const sellPriceTxt = Utils.toBanglaNumber(p.sellingPrice || 0);
      return `<option value="${p.name}">মজুত: ${stockTxt} ${p.unit || 'পিস'} | বিক্রয় দর: ৳${sellPriceTxt}</option>`;
    }).join('');
  },

  /**
   * Handle typing or selecting product in createMemoModal row
   */
  onMemoProductNameInput(inputElem) {
    const tr = inputElem.closest('tr');
    if (!tr) return;
    const val = inputElem.value.trim().toLowerCase();
    const hintEl = tr.querySelector('.memo-item-stock-hint');
    const rateInput = tr.querySelector('.memo-item-rate');

    if (!val) {
      if (hintEl) hintEl.textContent = '';
      return;
    }

    const matchedProd = this.state.products.find(p => p.name && p.name.trim().toLowerCase() === val);
    if (matchedProd) {
      if (rateInput && (!rateInput.value || parseFloat(rateInput.value) === 0)) {
        rateInput.value = matchedProd.sellingPrice || '';
      }
      if (hintEl) {
        const stock = parseFloat(matchedProd.currentStock) || 0;
        const alertLimit = matchedProd.minStockAlert !== undefined ? parseFloat(matchedProd.minStockAlert) : 5;
        if (stock <= 0) {
          hintEl.textContent = '⚠️ স্টক আউট (০)!';
          hintEl.style.color = '#DC2626';
        } else if (stock <= alertLimit) {
          hintEl.textContent = `⚠️ মজুত: ${Utils.toBanglaNumber(stock)} ${matchedProd.unit || 'পিস'} (কম স্টক) | দর: ৳${Utils.toBanglaNumber(matchedProd.sellingPrice)}`;
          hintEl.style.color = '#D97706';
        } else {
          hintEl.textContent = `✓ মজুত: ${Utils.toBanglaNumber(stock)} ${matchedProd.unit || 'পিস'} | বিক্রয় দর: ৳${Utils.toBanglaNumber(matchedProd.sellingPrice)}`;
          hintEl.style.color = '#059669';
        }
      }
      this.recalcMemoTotals();
    } else {
      if (hintEl) hintEl.textContent = '';
    }
  },

  /**
   * Render Product Inventory View
   */
  renderProductsView() {
    const products = this.state.products || [];
    const searchQuery = (this.state.productSearch || '').toLowerCase().trim();
    const categoryFilter = this.state.productCategoryFilter || 'ALL';
    const stockFilter = this.state.productStockFilter || 'ALL';
    const viewMode = this.state.productViewMode || 'TABLE';

    // 1. Calculate overall metrics for all products of active account
    const totalCount = products.length;
    let totalStockCost = 0;
    let totalSalesVal = 0;
    let lowStockCount = 0;

    const categoryCounts = {};

    products.forEach(p => {
      const stock = parseFloat(p.currentStock !== undefined ? p.currentStock : p.stockQty) || 0;
      const buyPrice = parseFloat(p.buyingPrice !== undefined ? p.buyingPrice : p.buyPrice) || 0;
      const sellPrice = parseFloat(p.sellingPrice !== undefined ? p.sellingPrice : p.sellPrice) || 0;
      const alertLimit = p.minStockAlert !== undefined && p.minStockAlert !== null ? parseFloat(p.minStockAlert) : (p.lowStockAlert || 5);

      totalStockCost += stock * buyPrice;
      totalSalesVal += stock * sellPrice;

      if (stock <= alertLimit) {
        lowStockCount++;
      }

      const cat = (p.category || 'সাধারণ').trim();
      categoryCounts[cat] = (categoryCounts[cat] || 0) + 1;
    });

    // Update Top Summary Cards
    const countValEl = document.getElementById('prodTotalCountVal');
    if (countValEl) countValEl.textContent = `${Utils.toBanglaNumber(totalCount, false)} টি`;

    const costValEl = document.getElementById('prodTotalStockCostVal');
    if (costValEl) costValEl.textContent = Utils.formatCurrency(Math.round(totalStockCost));

    const salesValEl = document.getElementById('prodTotalSalesVal');
    if (salesValEl) salesValEl.textContent = Utils.formatCurrency(Math.round(totalSalesVal));

    const lowStockValEl = document.getElementById('prodLowStockVal');
    if (lowStockValEl) lowStockValEl.textContent = `${Utils.toBanglaNumber(lowStockCount, false)} টি`;

    // Render Category Filter Chips Bar
    const chipBar = document.getElementById('categoryFilterBar');
    if (chipBar) {
      const sortedCats = Object.keys(categoryCounts).sort();
      let chipsHtml = `
        <button type="button" class="category-chip-btn ${categoryFilter === 'ALL' ? 'active' : ''}" onclick="App.handleProductCategoryFilter('ALL')">
          <span>সব ক্যাটাগরি</span>
          <span class="chip-count">${Utils.toBanglaNumber(totalCount, false)}</span>
        </button>
      `;
      sortedCats.forEach(cat => {
        const count = categoryCounts[cat];
        chipsHtml += `
          <button type="button" class="category-chip-btn ${categoryFilter === cat ? 'active' : ''}" onclick="App.handleProductCategoryFilter('${cat.replace(/'/g, "\\'")}')">
            <span>${cat}</span>
            <span class="chip-count">${Utils.toBanglaNumber(count, false)}</span>
          </button>
        `;
      });
      chipBar.innerHTML = chipsHtml;
    }

    // Update Category Filter Dropdown
    const catSelect = document.getElementById('prodCategoryFilter');
    if (catSelect) {
      const currentSelected = this.state.productCategoryFilter || 'ALL';
      let optionsHtml = '<option value="ALL">সব ক্যাটাগরি</option>';
      Object.keys(categoryCounts).sort().forEach(cat => {
        optionsHtml += `<option value="${cat}" ${cat === currentSelected ? 'selected' : ''}>${cat} (${Utils.toBanglaNumber(categoryCounts[cat], false)})</option>`;
      });
      catSelect.innerHTML = optionsHtml;
    }

    // 2. Filter products
    const filtered = products.filter(p => {
      if (searchQuery) {
        const matchesName = (p.name || '').toLowerCase().includes(searchQuery);
        const matchesCat = (p.category || '').toLowerCase().includes(searchQuery);
        const matchesCode = (p.code || '').toLowerCase().includes(searchQuery);
        const matchesDesc = (p.description || '').toLowerCase().includes(searchQuery);
        if (!matchesName && !matchesCat && !matchesCode && !matchesDesc) return false;
      }

      if (categoryFilter !== 'ALL' && (p.category || 'সাধারণ') !== categoryFilter) {
        return false;
      }

      const stock = parseFloat(p.currentStock !== undefined ? p.currentStock : p.stockQty) || 0;
      const alertLimit = p.minStockAlert !== undefined && p.minStockAlert !== null ? parseFloat(p.minStockAlert) : (p.lowStockAlert || 5);
      if (stockFilter === 'LOW') {
        if (stock > alertLimit) return false;
      } else if (stockFilter === 'IN_STOCK') {
        if (stock <= alertLimit) return false;
      }

      return true;
    });

    // 3. Switch between TABLE View and CATEGORY Grouped View
    const tableCard = document.getElementById('productsTableCard');
    const categoryGroupedView = document.getElementById('productsCategoryGroupedView');
    const tableBtn = document.getElementById('viewModeTableBtn');
    const catBtn = document.getElementById('viewModeCategoryBtn');

    if (viewMode === 'CATEGORY') {
      if (tableCard) tableCard.style.display = 'none';
      if (categoryGroupedView) categoryGroupedView.style.display = 'flex';
      if (tableBtn) tableBtn.classList.remove('active');
      if (catBtn) catBtn.classList.add('active');
      this.renderCategoryGroupedView(filtered);
    } else {
      if (tableCard) tableCard.style.display = 'block';
      if (categoryGroupedView) categoryGroupedView.style.display = 'none';
      if (tableBtn) tableBtn.classList.add('active');
      if (catBtn) catBtn.classList.remove('active');
      this.renderProductsTableView(filtered);
    }
  },

  /**
   * Render Standard Products Table Rows
   */
  renderProductsTableView(filtered) {
    const tbody = document.getElementById('productsTableBody');
    if (!tbody) return;

    if (filtered.length === 0) {
      tbody.innerHTML = `
        <tr>
          <td colspan="8" style="text-align: center; padding: 3rem 1rem;">
            <div style="font-size: 2.5rem; margin-bottom: 0.5rem;">📦</div>
            <h4 style="color: var(--text-main); font-weight: 700; margin-bottom: 0.25rem;">কোন পণ্য পাওয়া যায়নি</h4>
            <p style="color: var(--text-muted); font-size: 0.88rem; margin-bottom: 1rem;">
              ফিল্টার পরিবর্তন করে খুঁজুন অথবা আপনার ক্যাটাগরি উপযোগী নতুন পণ্য যুক্ত করুন
            </p>
            <button type="button" class="btn btn-primary btn-sm" onclick="App.openProductModal()">
              + নতুন পণ্য যোগ করুন
            </button>
          </td>
        </tr>
      `;
      return;
    }

    tbody.innerHTML = filtered.map((p, idx) => {
      const stock = parseFloat(p.currentStock !== undefined ? p.currentStock : p.stockQty) || 0;
      const buyPrice = parseFloat(p.buyingPrice !== undefined ? p.buyingPrice : p.buyPrice) || 0;
      const sellPrice = parseFloat(p.sellingPrice !== undefined ? p.sellingPrice : p.sellPrice) || 0;
      const profit = Math.round((sellPrice - buyPrice) * 100) / 100;
      const profitPercent = buyPrice > 0 ? Math.round((profit / buyPrice) * 100) : 0;
      const alertLimit = p.minStockAlert !== undefined && p.minStockAlert !== null ? parseFloat(p.minStockAlert) : (p.lowStockAlert || 5);

      let stockBadge = '';
      if (stock <= 0) {
        stockBadge = `<span class="stock-badge stock-out">স্টক আউট (০)</span>`;
      } else if (stock <= alertLimit) {
        stockBadge = `<span class="stock-badge stock-low">${Utils.toBanglaNumber(stock)} ${p.unit || ''} (কম স্টক)</span>`;
      } else {
        stockBadge = `<span class="stock-badge stock-healthy">${Utils.toBanglaNumber(stock)} ${p.unit || ''}</span>`;
      }

      return `
        <tr>
          <td style="text-align: center; color: var(--text-muted);">${Utils.toBanglaNumber(idx + 1, false)}</td>
          <td>
            <div style="font-weight: 700; color: var(--text-main);">${p.name}</div>
            <div style="display: flex; gap: 0.4rem; align-items: center; margin-top: 2px; flex-wrap: wrap;">
              ${p.code ? `<span class="product-code-tag">${p.code}</span>` : ''}
              ${p.description ? `<span style="font-size: 0.78rem; color: var(--text-muted);">${p.description}</span>` : ''}
            </div>
          </td>
          <td>
            <span class="category-pill">${p.category || 'সাধারণ'}</span>
          </td>
          <td style="text-align: right; font-weight: 600; color: var(--text-muted);">
            ৳ ${Utils.toBanglaNumber(buyPrice)}
          </td>
          <td style="text-align: right; font-weight: 700; color: var(--primary-800);">
            ৳ ${Utils.toBanglaNumber(sellPrice)}
          </td>
          <td style="text-align: right;">
            <div style="font-weight: 700; color: ${profit >= 0 ? '#059669' : '#DC2626'};">
              ${profit >= 0 ? '+' : ''}৳ ${Utils.toBanglaNumber(profit)}
            </div>
            <span class="profit-badge" style="background: ${profit >= 0 ? '#ECFDF5' : '#FEE2E2'}; color: ${profit >= 0 ? '#065F46' : '#991B1B'}; font-size: 0.72rem;">
              ${Utils.toBanglaNumber(profitPercent)}%
            </span>
          </td>
          <td style="text-align: center;">
            ${stockBadge}
          </td>
          <td style="text-align: center;">
            <div style="display: flex; gap: 4px; justify-content: center;">
              <button type="button" class="btn btn-secondary btn-sm" onclick="App.openStockAdjustModal('${p.id}')" title="স্টক বাড়ান বা কমান" style="padding: 3px 7px; font-size: 0.8rem;">
                ± স্টক
              </button>
              <button type="button" class="btn btn-secondary btn-sm" onclick="App.openProductModal('${p.id}')" title="এডিট করুন" style="padding: 3px 7px; font-size: 0.8rem;">
                ✏️
              </button>
              <button type="button" class="btn btn-danger btn-sm" onclick="App.deleteProduct('${p.id}')" title="মুছে ফেলুন" style="padding: 3px 7px; font-size: 0.8rem;">
                🗑️
              </button>
            </div>
          </td>
        </tr>
      `;
    }).join('');
  },

  /**
   * Render Category-Grouped Cards View
   */
  renderCategoryGroupedView(products) {
    const container = document.getElementById('productsCategoryGroupedView');
    if (!container) return;

    if (!products || products.length === 0) {
      container.innerHTML = `
        <div class="card" style="text-align: center; padding: 3rem 1rem;">
          <div style="font-size: 2.5rem; margin-bottom: 0.5rem;">📦</div>
          <h4 style="color: var(--text-main); font-weight: 700; margin-bottom: 0.25rem;">কোন পণ্য পাওয়া যায়নি</h4>
          <p style="color: var(--text-muted); font-size: 0.88rem; margin-bottom: 1rem;">
            ফিল্টার পরিবর্তন করে খুঁজুন অথবা নতুন পণ্য যোগ করুন
          </p>
          <button type="button" class="btn btn-primary btn-sm" onclick="App.openProductModal()">
            + নতুন পণ্য যোগ করুন
          </button>
        </div>
      `;
      return;
    }

    // Group products by category
    const grouped = {};
    products.forEach(p => {
      const cat = (p.category || 'সাধারণ').trim();
      if (!grouped[cat]) grouped[cat] = [];
      grouped[cat].push(p);
    });

    const categoryNames = Object.keys(grouped).sort();

    container.innerHTML = categoryNames.map(cat => {
      const items = grouped[cat];
      const itemCount = items.length;
      let catTotalVal = 0;
      items.forEach(p => {
        const stock = parseFloat(p.currentStock !== undefined ? p.currentStock : p.stockQty) || 0;
        const buy = parseFloat(p.buyingPrice !== undefined ? p.buyingPrice : p.buyPrice) || 0;
        catTotalVal += stock * buy;
      });

      const itemsHtml = items.map(p => {
        const stock = parseFloat(p.currentStock !== undefined ? p.currentStock : p.stockQty) || 0;
        const buyPrice = parseFloat(p.buyingPrice !== undefined ? p.buyingPrice : p.buyPrice) || 0;
        const sellPrice = parseFloat(p.sellingPrice !== undefined ? p.sellingPrice : p.sellPrice) || 0;
        const profit = Math.round((sellPrice - buyPrice) * 100) / 100;
        const profitPercent = buyPrice > 0 ? Math.round((profit / buyPrice) * 100) : 0;
        const alertLimit = p.minStockAlert !== undefined && p.minStockAlert !== null ? parseFloat(p.minStockAlert) : (p.lowStockAlert || 5);

        let stockBadge = '';
        if (stock <= 0) {
          stockBadge = `<span class="stock-badge stock-out">স্টক আউট (০)</span>`;
        } else if (stock <= alertLimit) {
          stockBadge = `<span class="stock-badge stock-low">${Utils.toBanglaNumber(stock)} ${p.unit || ''} (কম)</span>`;
        } else {
          stockBadge = `<span class="stock-badge stock-healthy">${Utils.toBanglaNumber(stock)} ${p.unit || ''}</span>`;
        }

        return `
          <div class="category-item-card">
            <div class="category-item-top">
              <div>
                <div class="category-item-name">${p.name}</div>
                <div style="display: flex; gap: 4px; align-items: center; margin-top: 3px; flex-wrap: wrap;">
                  ${p.code ? `<span class="product-code-tag">${p.code}</span>` : ''}
                  ${p.description ? `<span style="font-size: 0.76rem; color: var(--text-muted);">${p.description}</span>` : ''}
                </div>
              </div>
              <div>${stockBadge}</div>
            </div>

            <div class="category-item-prices">
              <div>
                <div style="font-size: 0.72rem; color: var(--text-muted);">ক্রয় দর</div>
                <strong style="color: var(--text-main);">৳ ${Utils.toBanglaNumber(buyPrice)}</strong>
              </div>
              <div style="text-align: center;">
                <div style="font-size: 0.72rem; color: var(--text-muted);">বিক্রয় দর</div>
                <strong style="color: var(--primary-800);">৳ ${Utils.toBanglaNumber(sellPrice)}</strong>
              </div>
              <div style="text-align: right;">
                <div style="font-size: 0.72rem; color: var(--text-muted);">লাভ / মার্জিন</div>
                <strong style="color: ${profit >= 0 ? '#059669' : '#DC2626'};">${profit >= 0 ? '+' : ''}৳ ${Utils.toBanglaNumber(profit)} (${Utils.toBanglaNumber(profitPercent)}%)</strong>
              </div>
            </div>

            <div class="category-item-actions">
              <span style="font-size: 0.8rem; color: var(--text-muted);">একক: <strong>${p.unit || 'পিস'}</strong></span>
              <div style="display: flex; gap: 4px;">
                <button type="button" class="btn btn-secondary btn-sm" onclick="App.openStockAdjustModal('${p.id}')" title="স্টক পরিবর্তন" style="padding: 2px 7px; font-size: 0.78rem;">
                  ± স্টক
                </button>
                <button type="button" class="btn btn-secondary btn-sm" onclick="App.openProductModal('${p.id}')" title="এডিট" style="padding: 2px 7px; font-size: 0.78rem;">
                  ✏️
                </button>
                <button type="button" class="btn btn-danger btn-sm" onclick="App.deleteProduct('${p.id}')" title="মুছে ফেলুন" style="padding: 2px 7px; font-size: 0.78rem;">
                  🗑️
                </button>
              </div>
            </div>
          </div>
        `;
      }).join('');

      return `
        <div class="category-group-card">
          <div class="category-group-header">
            <div class="category-group-title">
              <span>📁 ${cat}</span>
              <span class="badge badge-info" style="font-size: 0.78rem; padding: 2px 8px;">${Utils.toBanglaNumber(itemCount, false)} টি পণ্য</span>
            </div>
            <div class="category-group-meta">
              <span>মজুত ইনভেস্ট: <strong>${Utils.formatCurrency(Math.round(catTotalVal))}</strong></span>
              <button type="button" class="btn btn-primary btn-sm" onclick="App.openProductModal(null, '${cat.replace(/'/g, "\\'")}')" style="padding: 3px 9px; font-size: 0.8rem;">
                + এই ক্যাটাগরিতে পণ্য
              </button>
            </div>
          </div>
          <div class="category-items-grid">
            ${itemsHtml}
          </div>
        </div>
      `;
    }).join('');
  },

  /**
   * Switch between TABLE and CATEGORY view modes
   */
  switchProductViewMode(mode) {
    this.state.productViewMode = mode;
    this.renderProductsView();
  },

  /**
   * Search and filter handlers
   */
  handleProductSearch(val) {
    this.state.productSearch = val;
    this.renderProductsView();
  },

  handleProductCategoryFilter(val) {
    this.state.productCategoryFilter = val;
    this.renderProductsView();
  },

  handleProductStockFilter(val) {
    this.state.productStockFilter = val;
    this.renderProductsView();
  },

  /**
   * Open Product Add / Edit Modal (Customized for Active Merchant's Category)
   */
  openProductModal(productId = null, presetCategory = '') {
    const form = document.getElementById('productForm');
    if (!form) return;
    form.reset();

    const titleEl = document.getElementById('productModalTitle');
    const editIdEl = document.getElementById('prodEditId');
    const saveBtn = document.getElementById('prodSaveBtn');

    // Get active business category and preset product categories
    const activeCategory = this.state.activeAccount?.businessCategory || this.state.profile?.businessCategory || 'মুদি দোকান ও সুপারশপ';
    const catMeta = getBusinessCategoryMeta(activeCategory);

    // Combine preset product categories and existing products' categories
    const allCategories = new Set([...catMeta.productCategories]);
    (this.state.products || []).forEach(p => {
      if (p.category && p.category.trim()) allCategories.add(p.category.trim());
    });

    // Populate Datalist
    const datalist = document.getElementById('productCategoriesList');
    if (datalist) {
      datalist.innerHTML = Array.from(allCategories).map(c => `<option value="${c}"></option>`).join('');
    }

    let initialCategory = presetCategory || '';

    if (productId) {
      const prod = this.state.products.find(p => p.id === productId);
      if (!prod) return;

      if (titleEl) titleEl.textContent = 'পণ্য এডিট করুন';
      if (editIdEl) editIdEl.value = prod.id;
      if (saveBtn) saveBtn.textContent = 'আপডেট করুন ✓';

      document.getElementById('prodNameInput').value = prod.name || '';
      initialCategory = prod.category || '';
      document.getElementById('prodCategoryInput').value = initialCategory;
      document.getElementById('prodUnitSelect').value = prod.unit || 'পিস';
      document.getElementById('prodBuyPriceInput').value = prod.buyingPrice !== undefined ? prod.buyingPrice : (prod.buyPrice !== undefined ? prod.buyPrice : '');
      document.getElementById('prodSellPriceInput').value = prod.sellingPrice !== undefined ? prod.sellingPrice : (prod.sellPrice !== undefined ? prod.sellPrice : '');
      document.getElementById('prodStockQtyInput').value = prod.currentStock !== undefined ? prod.currentStock : (prod.stockQty !== undefined ? prod.stockQty : 0);
      document.getElementById('prodLowStockAlertInput').value = prod.minStockAlert !== undefined ? prod.minStockAlert : (prod.lowStockAlert !== undefined ? prod.lowStockAlert : 5);
      document.getElementById('prodCodeInput').value = prod.code || '';
      document.getElementById('prodDescInput').value = prod.description || '';
    } else {
      if (titleEl) titleEl.textContent = `নতুন পণ্য যোগ করুন (${catMeta.name})`;
      if (editIdEl) editIdEl.value = '';
      if (saveBtn) saveBtn.textContent = 'পণ্য সংরক্ষণ করুন ✓';

      if (presetCategory) {
        document.getElementById('prodCategoryInput').value = presetCategory;
        initialCategory = presetCategory;
      } else if (catMeta.productCategories.length > 0) {
        initialCategory = catMeta.productCategories[0];
        document.getElementById('prodCategoryInput').value = initialCategory;
      }

      document.getElementById('prodUnitSelect').value = 'পিস';
      document.getElementById('prodStockQtyInput').value = '0';
      document.getElementById('prodLowStockAlertInput').value = '5';
    }

    // Render Quick Category Selection Pills
    this.renderModalQuickCategoryPills(allCategories, initialCategory);

    this.calcProductModalProfit();
    UI.openModal('productModal');
  },

  /**
   * Render Quick Category Pills inside Product Modal
   */
  renderModalQuickCategoryPills(allCategories, selectedCategory = '') {
    const quickPillsEl = document.getElementById('productModalQuickPills');
    if (!quickPillsEl) return;
    const currentVal = (selectedCategory || document.getElementById('prodCategoryInput')?.value || '').trim();
    quickPillsEl.innerHTML = Array.from(allCategories).slice(0, 10).map(c => {
      const isSelected = c === currentVal;
      return `<button type="button" class="modal-cat-pill ${isSelected ? 'selected' : ''}" onclick="App.selectModalQuickCategory('${c.replace(/'/g, "\\'")}')">${c}</button>`;
    }).join('');
  },

  selectModalQuickCategory(catName) {
    const input = document.getElementById('prodCategoryInput');
    if (input) {
      input.value = catName;
    }
    this.onProductModalCategoryInput(catName);
  },

  onProductModalCategoryInput(val) {
    const pills = document.querySelectorAll('#productModalQuickPills .modal-cat-pill');
    const clean = (val || '').trim().toLowerCase();
    pills.forEach(p => {
      if (p.textContent.trim().toLowerCase() === clean) {
        p.classList.add('selected');
      } else {
        p.classList.remove('selected');
      }
    });
  },

  /**
   * Open Category Manager Modal
   */
  openCategoryManagerModal() {
    const activeCategory = this.state.activeAccount?.businessCategory || this.state.profile?.businessCategory || 'মুদি দোকান ও সুপারশপ';
    const catMeta = getBusinessCategoryMeta(activeCategory);

    const badgeEl = document.getElementById('catManagerActiveBizBadge');
    if (badgeEl) {
      badgeEl.textContent = `${catMeta.icon} ${catMeta.name}`;
      badgeEl.className = `biz-cat-badge biz-cat-badge-sm ${catMeta.badgeClass}`;
    }

    this.renderCategoryManagerList();
    UI.openModal('categoryManagerModal');
  },

  renderCategoryManagerList() {
    const container = document.getElementById('categoryManagerList');
    if (!container) return;

    const activeCategory = this.state.activeAccount?.businessCategory || this.state.profile?.businessCategory || 'মুদি দোকান ও সুপারশপ';
    const catMeta = getBusinessCategoryMeta(activeCategory);

    // Compute counts
    const categoryCounts = {};
    (this.state.products || []).forEach(p => {
      const cat = (p.category || 'সাধারণ').trim();
      categoryCounts[cat] = (categoryCounts[cat] || 0) + 1;
    });

    const allCategories = new Set([...catMeta.productCategories, ...Object.keys(categoryCounts)]);
    const sorted = Array.from(allCategories).sort();

    if (sorted.length === 0) {
      container.innerHTML = '<div style="text-align: center; color: var(--text-muted); padding: 1.5rem;">কোনো ক্যাটাগরি পাওয়া যায়নি</div>';
      return;
    }

    container.innerHTML = sorted.map(cat => {
      const count = categoryCounts[cat] || 0;
      const isPreset = catMeta.productCategories.includes(cat);

      return `
        <div style="display: flex; justify-content: space-between; align-items: center; padding: 0.65rem 0.85rem; background: var(--bg-surface-alt); border: 1px solid var(--border-color); border-radius: var(--radius-md);">
          <div style="display: flex; align-items: center; gap: 6px;">
            <span style="font-weight: 700; color: var(--text-main);">${cat}</span>
            ${isPreset ? `<span style="font-size: 0.68rem; background: #E0E7FF; color: #3730A3; padding: 1px 5px; border-radius: 4px;">সুপারিশকৃত</span>` : `<span style="font-size: 0.68rem; background: #FEF3C7; color: #92400E; padding: 1px 5px; border-radius: 4px;">কাস্টম</span>`}
          </div>
          <div style="display: flex; align-items: center; gap: 8px;">
            <span style="font-size: 0.82rem; font-weight: 600; color: var(--text-muted);">${Utils.toBanglaNumber(count, false)} টি পণ্য</span>
            <button type="button" class="btn btn-secondary btn-sm" onclick="UI.closeModal('categoryManagerModal'); App.openProductModal(null, '${cat.replace(/'/g, "\\'")}')" style="padding: 2px 7px; font-size: 0.78rem;">
              + পণ্য যোগ
            </button>
          </div>
        </div>
      `;
    }).join('');
  },

  handleAddNewCustomCategory(e) {
    e.preventDefault();
    const input = document.getElementById('newCategoryNameInput');
    const name = input?.value?.trim();
    if (!name) return;

    input.value = '';
    UI.toast(`নতুন ক্যাটাগরি "${name}" যুক্ত হয়েছে! পণ্য যোগ করার সময় এটি বেছে নিতে পারবেন ✓`, 'success');
    this.renderCategoryManagerList();
    this.renderProductsView();
  },

  /**
   * Calculate live profit margin inside Product Modal
   */
  calcProductModalProfit() {
    const buyPrice = parseFloat(document.getElementById('prodBuyPriceInput')?.value) || 0;
    const sellPrice = parseFloat(document.getElementById('prodSellPriceInput')?.value) || 0;
    const profit = Math.round((sellPrice - buyPrice) * 100) / 100;
    const profitPercent = buyPrice > 0 ? Math.round((profit / buyPrice) * 100) : 0;

    const valEl = document.getElementById('prodProfitVal');
    if (!valEl) return;

    if (sellPrice > 0 || buyPrice > 0) {
      valEl.textContent = `৳ ${Utils.toBanglaNumber(profit)} (${Utils.toBanglaNumber(profitPercent)}%)`;
      valEl.style.color = profit >= 0 ? '#059669' : '#DC2626';
    } else {
      valEl.textContent = '৳ ০ (০%)';
      valEl.style.color = '#059669';
    }
  },

  /**
   * Handle Save Product (Add / Update)
   */
  async handleSaveProduct(e) {
    e.preventDefault();
    const editId = document.getElementById('prodEditId')?.value.trim();
    const name = document.getElementById('prodNameInput')?.value.trim();
    const category = document.getElementById('prodCategoryInput')?.value.trim() || 'সাধারণ';
    const unit = document.getElementById('prodUnitSelect')?.value || 'পিস';
    const buyingPrice = parseFloat(document.getElementById('prodBuyPriceInput')?.value) || 0;
    const sellingPrice = parseFloat(document.getElementById('prodSellPriceInput')?.value) || 0;
    const currentStock = parseFloat(document.getElementById('prodStockQtyInput')?.value) || 0;
    const minStockAlert = parseFloat(document.getElementById('prodLowStockAlertInput')?.value) || 5;
    const code = document.getElementById('prodCodeInput')?.value.trim() || '';
    const description = document.getElementById('prodDescInput')?.value.trim() || '';

    if (!name) {
      UI.toast('পণ্যের নাম লিখুন!', 'error');
      return;
    }

    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const prodRecord = {
      id: editId || Utils.generateId('prod'),
      accountId: activeAccId,
      name,
      category,
      unit,
      buyingPrice,
      sellingPrice,
      currentStock,
      minStockAlert,
      buyPrice: buyingPrice,
      sellPrice: sellingPrice,
      stockQty: currentStock,
      lowStockAlert: minStockAlert,
      code,
      description,
      updatedAt: new Date().toISOString()
    };

    if (!editId) {
      prodRecord.createdAt = new Date().toISOString();
    }

    await khataDB.put('products', prodRecord);
    await this.refreshAllData();

    this.renderProductsView();
    UI.closeModal('productModal');
    UI.toast(editId ? 'পণ্য সফলভাবে আপডেট হয়েছে ✓' : 'নতুন পণ্য সফলভাবে যোগ হয়েছে ✓', 'success');
  },

  /**
   * Delete Product
   */
  deleteProduct(id) {
    const prod = this.state.products.find(p => p.id === id);
    if (!prod) return;

    UI.confirm({
      title: 'পণ্য মুছে ফেলবেন?',
      message: `আপনি কি নিশ্চিত যে "${prod.name}" পণ্যটি তালিকা থেকে মুছে ফেলতে চান?`,
      confirmText: 'মুছে ফেলুন',
      isDanger: true,
      onConfirm: async () => {
        await khataDB.delete('products', id);
        await this.refreshAllData();
        this.renderProductsView();
        UI.toast('পণ্য মুছে ফেলা হয়েছে ✓', 'info');
      }
    });
  },

  /**
   * Quick Stock Adjustment Modal
   */
  openStockAdjustModal(id) {
    const prod = this.state.products.find(p => p.id === id);
    if (!prod) return;

    document.getElementById('stockAdjustProdId').value = prod.id;
    document.getElementById('stockAdjustProdName').textContent = prod.name;
    document.getElementById('stockAdjustCurrentQty').textContent = `${Utils.toBanglaNumber(prod.currentStock || 0)} ${prod.unit || ''}`;
    document.getElementById('stockAdjustQtyInput').value = '';
    document.getElementById('stockAdjustNoteInput').value = '';
    this.setStockAdjustType('ADD');

    UI.openModal('stockAdjustModal');
  },

  setStockAdjustType(type) {
    document.getElementById('stockAdjustTypeInput').value = type;
    const addBtn = document.getElementById('stockAddBtn');
    const reduceBtn = document.getElementById('stockReduceBtn');

    if (type === 'ADD') {
      if (addBtn) addBtn.className = 'type-pill-btn active';
      if (reduceBtn) reduceBtn.className = 'type-pill-btn';
    } else {
      if (addBtn) addBtn.className = 'type-pill-btn';
      if (reduceBtn) reduceBtn.className = 'type-pill-btn active';
    }
  },

  async handleSaveStockAdjust(e) {
    e.preventDefault();
    const id = document.getElementById('stockAdjustProdId')?.value;
    const prod = this.state.products.find(p => p.id === id);
    if (!prod) return;

    const type = document.getElementById('stockAdjustTypeInput')?.value || 'ADD';
    const qty = parseFloat(document.getElementById('stockAdjustQtyInput')?.value) || 0;

    if (qty <= 0) {
      UI.toast('সঠিক পরিমাণ লিখুন!', 'error');
      return;
    }

    const currentStock = parseFloat(prod.currentStock) || 0;
    let newStock = currentStock;

    if (type === 'ADD') {
      newStock = Math.round((currentStock + qty) * 100) / 100;
    } else {
      if (qty > currentStock) {
        UI.toast(`সতর্কতা: মজুতের চেয়ে বেশি পরিমাণ বাদ দেওয়া হচ্ছে!`, 'warning');
      }
      newStock = Math.max(0, Math.round((currentStock - qty) * 100) / 100);
    }

    prod.currentStock = newStock;
    prod.updatedAt = new Date().toISOString();

    await khataDB.put('products', prod);
    await this.refreshAllData();

    this.renderProductsView();
    UI.closeModal('stockAdjustModal');
    UI.toast(`স্টক সফলভাবে আপডেট হয়েছে (নতুন মজুত: ${Utils.toBanglaNumber(newStock)} ${prod.unit || ''}) ✓`, 'success');
  },

  /**
   * Export Products Inventory to CSV with UTF-8 BOM
   */
  exportProductsCSV() {
    const products = this.state.products || [];
    if (products.length === 0) {
      UI.toast('এক্সপোর্ট করার মতো কোনো পণ্য নেই', 'info');
      return;
    }

    const headers = ['ক্রমিক', 'পণ্যের নাম', 'ক্যাটাগরি', 'একক', 'ক্রয় মূল্য (টাকা)', 'বিক্রয় মূল্য (টাকা)', 'সম্ভাব্য লাভ (টাকা)', 'বর্তমান স্টক', 'কম স্টক লিমিট', 'বারকোড', 'বিবরণ'];
    const rows = products.map((p, idx) => {
      const buy = p.buyingPrice || 0;
      const sell = p.sellingPrice || 0;
      const profit = Math.round((sell - buy) * 100) / 100;
      return [
        idx + 1,
        `"${(p.name || '').replace(/"/g, '""')}"`,
        `"${(p.category || '').replace(/"/g, '""')}"`,
        `"${(p.unit || '').replace(/"/g, '""')}"`,
        buy,
        sell,
        profit,
        p.currentStock || 0,
        p.minStockAlert || 5,
        `"${(p.code || '').replace(/"/g, '""')}"`,
        `"${(p.description || '').replace(/"/g, '""')}"`
      ];
    });

    const csvContent = '\uFEFF' + [headers.join(','), ...rows.map(r => r.join(','))].join('\r\n');
    const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.setAttribute('download', `Khata_Products_Inventory_${Utils.getTodayDateString()}.csv`);
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
    UI.toast('পণ্য ও স্টক তালিকা CSV হিসেবে ডাউনলোড হয়েছে ✓', 'success');
  },

  /**
   * ==========================================
   * 1. POLITE WHATSAPP DUE REMINDERS
   * ==========================================
   */
  openPoliteReminderModal(debtId) {
    const debt = this.state.debts.find(d => d.id === debtId);
    if (!debt) {
      UI.toast('হিসাব খুঁজে পাওয়া যায়নি', 'error');
      return;
    }

    const remaining = debt.remainingAmount !== undefined ? debt.remainingAmount : (debt.amount - (debt.paidAmount || 0));

    document.getElementById('reminderDebtId').value = debt.id;
    document.getElementById('reminderCustomerName').textContent = debt.person;
    document.getElementById('reminderCustomerPhone').textContent = debt.phone || 'মোবাইল নম্বর দেওয়া নেই';
    document.getElementById('reminderDueAmount').textContent = Utils.formatCurrency(remaining);
    document.getElementById('reminderPromiseDate').value = debt.promiseDate || '';

    this.currentReminderDebt = debt;
    this.selectReminderTemplate('GENTLE');

    const timeEl = document.getElementById('reminderPreviewTime');
    if (timeEl) {
      const now = new Date();
      timeEl.textContent = `${now.getHours() % 12 || 12}:${String(now.getMinutes()).padStart(2, '0')} ${now.getHours() >= 12 ? 'PM' : 'AM'} ✓✓`;
    }

    UI.openModal('politeReminderModal');
  },

  selectReminderTemplate(type) {
    const debt = this.currentReminderDebt;
    if (!debt) return;

    const remaining = debt.remainingAmount !== undefined ? debt.remainingAmount : (debt.amount - (debt.paidAmount || 0));
    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};
    const shopName = acc.businessName || prof.businessName || 'সহজ খাতা';
    const ownerName = acc.ownerName || prof.ownerName || 'দোকানদার';

    ['pillGentle', 'pillFriendly', 'pillUrgent'].forEach(id => {
      const el = document.getElementById(id);
      if (el) el.classList.remove('active');
    });

    let msg = '';
    if (type === 'GENTLE') {
      const pill = document.getElementById('pillGentle');
      if (pill) pill.classList.add('active');
      msg = `আসসালামু আলাইকুম ${debt.person} ভাই/আপা, আশা করি ভালো আছেন। ${shopName}-এ আপনার বর্তমান বাকি হিসাব ৳ ${Utils.toBanglaNumber(remaining)} টাকা। আপনার সুবিধাজনক সময়ে পরিশোধের বিনীত অনুরোধ রইল। ধন্যবাদ ও শুভকামনা! — ${ownerName}, ${shopName}`;
    } else if (type === 'FRIENDLY') {
      const pill = document.getElementById('pillFriendly');
      if (pill) pill.classList.add('active');
      msg = `শ্রদ্ধেয় ${debt.person}, কেমন আছেন? আপনার কেনাকাটার বাকি বকেয়া ৳ ${Utils.toBanglaNumber(remaining)} টাকা জমা দেওয়ার জন্য একটু স্মারক বার্তা পাঠালাম। সময় সুযোগ মতো পরিশোধ করলে খুব উপকার হয়। আপনার প্রিয় দোকান: ${shopName}`;
    } else if (type === 'URGENT') {
      const pill = document.getElementById('pillUrgent');
      if (pill) pill.classList.add('active');
      msg = `আসসালামু আলাইকুম ${debt.person} সাহেব, আপনার জরুরি অবগতির জন্য জানানো যাচ্ছে যে ${shopName}-এ আপনার বাকি পাওনা ৳ ${Utils.toBanglaNumber(remaining)} টাকা এখনও বকেয়া রয়েছে। দোকানের ক্যাশ ফ্লো স্বাভাবিক রাখতে অতিদ্রুত পরিশোধের বিশেষ অনুরোধ জানাচ্ছি। বিনীত — ${shopName}`;
    }

    const textEl = document.getElementById('reminderMessageText');
    const prevEl = document.getElementById('reminderMessagePreview');
    if (textEl) textEl.value = msg;
    if (prevEl) prevEl.textContent = msg;
  },

  onReminderMessageEdit() {
    const textEl = document.getElementById('reminderMessageText');
    const prevEl = document.getElementById('reminderMessagePreview');
    if (textEl && prevEl) {
      prevEl.textContent = textEl.value;
    }
  },

  sendWhatsAppPoliteReminder() {
    const debt = this.currentReminderDebt;
    if (!debt) return;
    const phone = (debt.phone || '').replace(/[^0-9]/g, '');
    const textEl = document.getElementById('reminderMessageText');
    const msg = textEl ? textEl.value : '';

    if (!msg) {
      UI.toast('বার্তা খালি রাখা যাবে না', 'warning');
      return;
    }

    const url = phone ? `https://wa.me/88${phone}?text=${encodeURIComponent(msg)}` : `https://wa.me/?text=${encodeURIComponent(msg)}`;
    window.open(url, '_blank');
    UI.toast('হোয়াটসঅ্যাপ চালু হচ্ছে...', 'info');
  },

  copyReminderSMS() {
    const textEl = document.getElementById('reminderMessageText');
    if (!textEl || !textEl.value) return;

    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(textEl.value).then(() => {
        UI.toast('বার্তার টেক্সট কপি হয়েছে! যেকোনো মেসেঞ্জারে পেস্ট করুন ✓', 'success');
      });
    } else {
      textEl.select();
      document.execCommand('copy');
      UI.toast('বার্তার টেক্সট কপি হয়েছে ✓', 'success');
    }
  },

  async saveDebtPromiseDate() {
    const debt = this.currentReminderDebt;
    if (!debt) return;

    const dateVal = document.getElementById('reminderPromiseDate')?.value;
    debt.promiseDate = dateVal || null;
    await khataDB.put('debts', debt);

    await this.refreshAllData();
    this.renderDebtsView();
    UI.toast(dateVal ? `ওয়াদা তারিখ সংরক্ষিত হয়েছে: ${Utils.formatDateBengali(dateVal)} ✓` : 'ওয়াদা তারিখ মুছে ফেলা হয়েছে', 'success');
  },

  /**
   * ==========================================
   * 2. DAY-END CASH DRAWER CLOSING & BALANCING
   * ==========================================
   */
  async openCashClosingModal(targetDate = null) {
    const date = targetDate || Utils.getTodayDateString();
    const dateInput = document.getElementById('cashClosingDateInput');
    if (dateInput) dateInput.value = date;

    await this.loadCashClosingForDate(date);
    UI.openModal('cashClosingModal');
  },

  async onCashClosingDateChange(dateVal) {
    if (!dateVal) return;
    await this.loadCashClosingForDate(dateVal);
  },

  async loadCashClosingForDate(date) {
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const dayTxs = this.state.transactions.filter(t => t.date === date && (t.paymentMethod === 'Cash' || !t.paymentMethod));
    
    let cashInflow = 0;
    let shopExp = 0;
    let houseExp = 0;

    dayTxs.forEach(t => {
      const amt = parseFloat(t.amount) || 0;
      if (t.type === 'INCOME') {
        cashInflow += amt;
      } else if (t.type === 'EXPENSE_SHOP') {
        shopExp += amt;
      } else if (t.type === 'EXPENSE_HOUSE') {
        houseExp += amt;
      }
    });

    let debtCollections = 0;
    const allPayments = await khataDB.getAll('debtPayments');
    allPayments.filter(p => p.date === date && (p.paymentMethod === 'Cash' || !p.paymentMethod) && (p.accountId === activeAccId || (!p.accountId && activeAccId === 'acc_rafiqul_main'))).forEach(p => {
      debtCollections += (parseFloat(p.amount) || 0);
    });

    const existingClosings = await khataDB.getAll('dailyClosings');
    const existing = existingClosings.find(c => c.date === date && (c.accountId === activeAccId || (!c.accountId && activeAccId === 'acc_rafiqul_main')));

    const openingCashInput = document.getElementById('closingOpeningCashInput');
    const physicalInput = document.getElementById('closingPhysicalCashInput');
    const notesInput = document.getElementById('closingNotesInput');

    if (existing) {
      if (openingCashInput) openingCashInput.value = existing.openingCash || 0;
      if (physicalInput) physicalInput.value = existing.physicalCash || 0;
      if (notesInput) notesInput.value = existing.notes || '';
    } else {
      const sortedClosings = existingClosings
        .filter(c => c.date < date && (c.accountId === activeAccId || (!c.accountId && activeAccId === 'acc_rafiqul_main')))
        .sort((a, b) => b.date.localeCompare(a.date));
      const prevClosing = sortedClosings[0];

      if (openingCashInput) openingCashInput.value = prevClosing ? prevClosing.physicalCash : 0;
      if (physicalInput) physicalInput.value = '';
      if (notesInput) notesInput.value = '';
    }

    document.getElementById('closingTodayInflow').textContent = `+ ${Utils.formatCurrency(cashInflow)}`;
    document.getElementById('closingTodayDebtPaid').textContent = `+ ${Utils.formatCurrency(debtCollections)}`;
    document.getElementById('closingTodayShopExp').textContent = `- ${Utils.formatCurrency(shopExp)}`;
    document.getElementById('closingTodayHouseExp').textContent = `- ${Utils.formatCurrency(houseExp)}`;

    this.currentClosingCalculations = {
      date,
      cashInflow,
      debtCollections,
      shopExp,
      houseExp
    };

    this.recalcCashClosingDrawer();
  },

  recalcCashClosingDrawer() {
    if (!this.currentClosingCalculations) return;
    const { cashInflow, debtCollections, shopExp, houseExp } = this.currentClosingCalculations;

    const openingCash = parseFloat(document.getElementById('closingOpeningCashInput')?.value) || 0;
    const physicalVal = document.getElementById('closingPhysicalCashInput')?.value;
    const physicalCash = physicalVal !== '' && physicalVal !== undefined ? parseFloat(physicalVal) : NaN;

    const expectedCash = Math.round((openingCash + cashInflow + debtCollections - shopExp - houseExp) * 100) / 100;

    const expEl = document.getElementById('closingExpectedCashVal');
    if (expEl) expEl.textContent = Utils.formatCurrency(expectedCash);

    const banner = document.getElementById('closingStatusBanner');
    const icon = document.getElementById('closingStatusIcon');
    const text = document.getElementById('closingStatusText');

    if (isNaN(physicalCash)) {
      if (banner) banner.style.display = 'none';
      return;
    }

    if (banner) banner.style.display = 'flex';

    const diff = Math.round((physicalCash - expectedCash) * 100) / 100;

    if (diff === 0) {
      banner.className = 'drawer-status-banner drawer-status-match';
      if (icon) icon.textContent = '✓';
      if (text) text.innerHTML = `<strong>ক্যাশ হিসাব ১০০% নির্ভুল ও মিলেছে!</strong> কোনো গরমিল নেই।`;
    } else if (diff < 0) {
      banner.className = 'drawer-status-banner drawer-status-short';
      if (icon) icon.textContent = '⚠️';
      if (text) text.innerHTML = `<strong>ক্যাশ ড্রয়ারে ঘাটতি:</strong> ৳ ${Utils.toBanglaNumber(Math.abs(diff))} টাকা কম পাওয়া গেছে!`;
    } else {
      banner.className = 'drawer-status-banner drawer-status-surplus';
      if (icon) icon.textContent = 'ℹ️';
      if (text) text.innerHTML = `<strong>ক্যাশ ড্রয়ারে উদ্বৃত্ত:</strong> ৳ ${Utils.toBanglaNumber(diff)} টাকা বেশি রয়েছে।`;
    }
  },

  async handleSaveCashClosing(e) {
    e.preventDefault();
    if (!this.currentClosingCalculations) return;

    const date = document.getElementById('cashClosingDateInput')?.value || Utils.getTodayDateString();
    const openingCash = parseFloat(document.getElementById('closingOpeningCashInput')?.value) || 0;
    const physicalCash = parseFloat(document.getElementById('closingPhysicalCashInput')?.value);
    const notes = document.getElementById('closingNotesInput')?.value.trim() || '';

    if (isNaN(physicalCash)) {
      UI.toast('ক্যাশ বাক্সে গুনে পাওয়া টাকার পরিমাণ লিখুন!', 'error');
      return;
    }

    const { cashInflow, debtCollections, shopExp, houseExp } = this.currentClosingCalculations;
    const expectedCash = Math.round((openingCash + cashInflow + debtCollections - shopExp - houseExp) * 100) / 100;
    const difference = Math.round((physicalCash - expectedCash) * 100) / 100;
    const activeAccId = this.state.activeAccount ? this.state.activeAccount.id : 'acc_rafiqul_main';

    const closingRecord = {
      id: `closing_${activeAccId}_${date.replace(/[^0-9]/g, '')}`,
      accountId: activeAccId,
      date,
      openingCash,
      cashInflow,
      debtCollections,
      shopExp,
      houseExp,
      expectedCash,
      physicalCash,
      difference,
      notes,
      closedAt: new Date().toISOString()
    };

    await khataDB.put('dailyClosings', closingRecord);
    UI.closeModal('cashClosingModal');
    UI.toast(`দিনের ক্যাশ ড্রয়ার ক্লোজিং সফলভাবে সংরক্ষিত হয়েছে (${Utils.formatDateBengali(date)}) ✓`, 'success');
  },

  printCashClosingReport() {
    window.print();
  },

  /**
   * ==========================================
   * 3. CUSTOMER DIGITAL PASSBOOK
   * ==========================================
   */
  async openCustomerPassbookModal(debtId) {
    const debt = this.state.debts.find(d => d.id === debtId);
    if (!debt) {
      UI.toast('কাস্টমারের তথ্য পাওয়া যায়নি', 'error');
      return;
    }

    const container = document.getElementById('customerPassbookSheet');
    if (!container) return;

    const acc = this.state.activeAccount || {};
    const prof = this.state.profile || {};
    const shopName = acc.businessName || prof.businessName || 'সহজ খাতা স্টোর';
    const ownerName = acc.ownerName || prof.ownerName || 'রফিকুল ইসলাম';
    const phone = acc.phone || prof.phone || '0131082-4987';

    const custTxs = this.state.transactions.filter(t => t.person && t.person.trim().toLowerCase() === debt.person.trim().toLowerCase());
    const custPayments = await khataDB.getPaymentsForDebt(debt.id);

    const remaining = debt.remainingAmount !== undefined ? debt.remainingAmount : (debt.amount - (debt.paidAmount || 0));
    const paid = debt.paidAmount || 0;
    const total = debt.amount || 0;

    let rowsHtml = '';
    custTxs.forEach((tx) => {
      rowsHtml += `
        <tr>
          <td>${Utils.formatDateBengali(tx.date)}</td>
          <td><strong>${tx.categoryName || tx.description || 'কেনাকাটা'}</strong></td>
          <td style="text-align: right; color: #1E40AF; font-weight: 700;">${Utils.formatCurrency(tx.amount)}</td>
          <td style="text-align: right; color: #059669;">-</td>
        </tr>
      `;
    });

    custPayments.forEach(p => {
      rowsHtml += `
        <tr style="background: #F0FDF4;">
          <td>${Utils.formatDateBengali(p.date)}</td>
          <td><span class="badge badge-success" style="font-size:0.75rem;">নগদ জমা</span> ${p.note || 'বাকি পরিশোধ'}</td>
          <td style="text-align: right;">-</td>
          <td style="text-align: right; color: #059669; font-weight: 700;">+ ${Utils.formatCurrency(p.amount)}</td>
        </tr>
      `;
    });

    if (!rowsHtml) {
      rowsHtml = `
        <tr>
          <td>${Utils.formatDateBengali(debt.date)}</td>
          <td><strong>প্রারম্ভিক বাকি ও কেনাকাটা</strong></td>
          <td style="text-align: right; font-weight: 700;">${Utils.formatCurrency(total)}</td>
          <td style="text-align: right; color: #059669;">${paid > 0 ? Utils.formatCurrency(paid) : '-'}</td>
        </tr>
      `;
    }

    container.innerHTML = `
      <div class="passbook-header">
        <div>
          <div style="font-size: 0.8rem; color: #059669; font-weight: 700; text-transform: uppercase;">গ্রাহক ডিজিটাল খতিয়ান পাসবুক</div>
          <h2 style="font-size: 1.4rem; color: #0F5132; margin: 2px 0 4px; font-weight: 800;">${shopName}</h2>
          <div style="font-size: 0.82rem; color: var(--text-muted);">প্রোপ্রাইটর: ${ownerName} • মোবাইল: ${phone}</div>
        </div>
        <div class="passbook-seal-badge">
          <span>সহজ খাতা</span>
          <span style="font-size: 0.85rem;">✓</span>
          <span>ভেরিফাইড</span>
        </div>
      </div>

      <div style="background: #F9FAFB; padding: 0.85rem 1rem; border-radius: var(--radius-md); margin-bottom: 1rem; display: flex; justify-content: space-between; align-items: center; border: 1px solid #E5E7EB;">
        <div>
          <div style="font-size: 0.78rem; color: var(--text-muted);">সম্মানিত গ্রাহক:</div>
          <strong style="font-size: 1.15rem; color: var(--text-main);">${debt.person}</strong>
          <div style="font-size: 0.82rem; color: var(--text-muted);">${debt.phone || 'মোবাইল নম্বর নেই'}</div>
        </div>
        <div style="text-align: right;">
          <div style="font-size: 0.78rem; color: var(--text-muted);">বর্তমান অবশিষ্ট দেনা:</div>
          <strong style="font-size: 1.35rem; color: ${remaining > 0 ? '#DC2626' : '#059669'};">${Utils.formatCurrency(remaining)}</strong>
        </div>
      </div>

      <div style="font-weight: 700; font-size: 0.88rem; color: #374151; margin-bottom: 4px;">হিসাব বিবরণী (খতিয়ান রেকর্ড):</div>
      <table class="passbook-table">
        <thead>
          <tr>
            <th style="width: 85px;">তারিখ</th>
            <th>বিবরণ ও পণ্য</th>
            <th style="width: 100px; text-align: right;">মোট দাবি (৳)</th>
            <th style="width: 100px; text-align: right;">জমা / পরিশোধ (৳)</th>
          </tr>
        </thead>
        <tbody>
          ${rowsHtml}
        </tbody>
      </table>

      <div style="background: #ECFDF5; border: 1px solid #A7F3D0; border-radius: var(--radius-md); padding: 0.75rem 1rem; margin-top: 1rem; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.5rem;">
        <div>
          <span style="font-size: 0.8rem; color: #065F46;">সর্বমোট কেনাকাটা: <strong>${Utils.formatCurrency(total)}</strong></span> • 
          <span style="font-size: 0.8rem; color: #065F46;">সর্বমোট জমা: <strong>${Utils.formatCurrency(paid)}</strong></span>
        </div>
        <div>
          <span style="font-size: 0.88rem; font-weight: 800; color: #0F5132;">নিট বকেয়া: ${Utils.formatCurrency(remaining)}</span>
        </div>
      </div>

      <div style="text-align: center; margin-top: 1.25rem; font-size: 0.75rem; color: #9CA3AF;">
        এটি সহজ খাতা ডিজিটাল খতিয়ান সফটওয়্যার দ্বারা সংরক্ষিত নির্ভরযোগ্য কম্পিউটারাইজড পাসবুক।
      </div>
    `;

    this.currentPassbookData = {
      person: debt.person,
      phone: debt.phone,
      remaining: remaining,
      shopName: shopName
    };

    UI.openModal('customerPassbookModal');
  },

  sharePassbookWhatsApp() {
    if (!this.currentPassbookData) return;
    const { person, phone, remaining, shopName } = this.currentPassbookData;
    const cleanPhone = (phone || '').replace(/[^0-9]/g, '');
    const msg = `আসসালামু আলাইকুম ${person} ভাই/আপা, ${shopName}-এ আপনার ডিজিটাল পাসবুক অনুযায়ী বর্তমান বকেয়া বাকি ৳ ${Utils.toBanglaNumber(remaining)} টাকা। যেকোনো সময় দোকানে এসে অথবা অনলাইনে হিসাব মিলিয়ে নিতে পারেন। ধন্যবাদ! - ${shopName}`;
    const url = cleanPhone ? `https://wa.me/88${cleanPhone}?text=${encodeURIComponent(msg)}` : `https://wa.me/?text=${encodeURIComponent(msg)}`;
    window.open(url, '_blank');
  },

  printCustomerPassbook() {
    window.print();
  },

  /**
   * ==========================================
   * 4. 1-CLICK COMPLETE BACKUP & RESTORE
   * ==========================================
   */
  async handleOneClickBackup() {
    try {
      UI.toast('ব্যাকআপ ফাইল প্রস্তুত করা হচ্ছে...', 'info', 1500);
      const backupData = await khataDB.exportCompleteDatabase();
      const jsonStr = JSON.stringify(backupData, null, 2);
      const blob = new Blob([jsonStr], { type: 'application/json' });
      const url = URL.createObjectURL(blob);
      const dateStr = Utils.getTodayDateString();
      const shopName = (this.state.activeAccount?.businessName || 'Khata').replace(/[\s\/\\]/g, '_');
      const filename = `Shohoj_Khata_Backup_${shopName}_${dateStr}.json`;

      const link = document.createElement('a');
      link.href = url;
      link.setAttribute('download', filename);
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
      URL.revokeObjectURL(url);

      UI.toast('সম্পূর্ণ ডাটা ব্যাকআপ সফলভাবে ডাউনলোড হয়েছে! আপনার হিসাব শতভাগ নিরাপদ ✓', 'success', 4000);
    } catch (err) {
      console.error('Backup error:', err);
      UI.toast('ব্যাকআপ নিতে সমস্যা হয়েছে', 'error');
    }
  },

  triggerOneClickRestore() {
    const fileInput = document.getElementById('oneClickRestoreFileInput') || document.getElementById('restoreFileInput');
    if (fileInput) {
      fileInput.click();
    }
  },

  async handleRestoreFile(inputOrEvent) {
    const file = inputOrEvent?.target?.files?.[0] || inputOrEvent?.files?.[0];
    if (!file) return;

    UI.confirm({
      title: 'ডাটা রিস্টোর করবেন?',
      message: `আপনি কি "${file.name}" ফাইল থেকে সমস্ত হিসাব রিস্টোর করতে চান? পূর্বের তথ্য প্রতিস্থাপিত হবে।`,
      confirmText: 'রিস্টোর করুন',
      isDanger: true,
      onConfirm: async () => {
        try {
          const text = await file.text();
          const backupObj = JSON.parse(text);
          await khataDB.restoreCompleteDatabase(backupObj);
          await this.refreshAllData();
          this.renderCurrentView();
          UI.toast('সমস্ত হিসাব সফলভাবে ফিরিয়ে আনা হয়েছে! ✓', 'success', 3500);
        } catch (err) {
          console.error('Restore error:', err);
          UI.toast('ব্যাকআপ ফাইলটি সঠিক নয় বা রিস্টোর ব্যর্থ হয়েছে!', 'error');
        }
      }
    });

    if (inputOrEvent?.target) inputOrEvent.target.value = '';
    else if (inputOrEvent?.value) inputOrEvent.value = '';
  },

  /**
   * ==========================================
   * 15. RI CONTEXT SWITCHER (ALL | BUSINESS | FAMILY)
   * ==========================================
   */
  switchContext(context = 'ALL') {
    this.state.activeContext = context;
    document.querySelectorAll('.context-pill-btn').forEach(btn => {
      btn.classList.toggle('active', btn.getAttribute('data-context') === context);
    });
    this.renderCurrentView();
    const label = context === 'BUSINESS' ? 'ব্যবসা (দোকান)' : (context === 'FAMILY' ? 'সংসার (পারিবারিক)' : 'সব হিসাব (একত্রে)');
    UI.toast(`ভিউ ফিল্টার: ${label}`, 'info', 1800);
  },

  /**
   * ==========================================
   * 16. QUICK ADD MODAL SYSTEM (8 ACTIONS)
   * ==========================================
   */
  openQuickAddModal() {
    UI.openModal('quickAddModal');
  },

  /**
   * ==========================================
   * 17. GLOBAL INSTANT SEARCH (Ctrl+K)
   * ==========================================
   */
  openGlobalSearch() {
    UI.openModal('globalSearchModal');
    setTimeout(() => {
      const input = document.getElementById('globalSearchInputField');
      if (input) {
        input.value = '';
        input.focus();
        this.executeGlobalSearch('');
      }
    }, 150);
  },

  executeGlobalSearch(rawQuery = '') {
    const q = (rawQuery || '').trim().toLowerCase();
    const container = document.getElementById('globalSearchResultsContainer');
    if (!container) return;

    if (!q) {
      container.innerHTML = '<p style="text-align: center; color: var(--text-muted); padding: 1.5rem 0;">খুঁজতে যেকোনো শব্দ, নাম, মোবাইল বা ক্যাটাগরি লিখুন...</p>';
      return;
    }

    const results = [];

    // Customers
    (this.state.customers || []).forEach(c => {
      if ((c.name && c.name.toLowerCase().includes(q)) || (c.phone && c.phone.includes(q))) {
        results.push({
          type: 'কাস্টমার',
          title: c.name,
          subtitle: `মোবাইল: ${c.phone || 'নেই'} | বর্তমান বাকি: ৳${Utils.toBanglaNumber(c.currentDue || 0)}`,
          action: () => { UI.closeModal('globalSearchModal'); this.navigateView('customers'); }
        });
      }
    });

    // Suppliers
    (this.state.suppliers || []).forEach(s => {
      if ((s.name && s.name.toLowerCase().includes(q)) || (s.phone && s.phone.includes(q)) || (s.company && s.company.toLowerCase().includes(q))) {
        results.push({
          type: 'মহাজন',
          title: s.name,
          subtitle: `${s.company ? s.company + ' | ' : ''}দেনা: ৳${Utils.toBanglaNumber(s.currentDue || 0)}`,
          action: () => { UI.closeModal('globalSearchModal'); this.navigateView('suppliers'); }
        });
      }
    });

    // Products
    (this.state.products || []).forEach(p => {
      if ((p.name && p.name.toLowerCase().includes(q)) || (p.code && p.code.toLowerCase().includes(q)) || (p.category && p.category.toLowerCase().includes(q))) {
        results.push({
          type: 'পণ্য স্টক',
          title: p.name,
          subtitle: `স্টক: ${Utils.toBanglaNumber(p.stock || 0)} ${p.unit || 'টি'} | বিক্রয় দর: ৳${Utils.toBanglaNumber(p.sellingPrice || 0)}`,
          action: () => { UI.closeModal('globalSearchModal'); this.navigateView('products'); }
        });
      }
    });

    // Sales
    (this.state.sales || []).forEach(s => {
      if ((s.invoiceNo && s.invoiceNo.toLowerCase().includes(q)) || (s.customerName && s.customerName.toLowerCase().includes(q))) {
        results.push({
          type: 'বিক্রয় চালান',
          title: `${s.invoiceNo || 'চালান'} - ${s.customerName || 'খুচরা ক্রেতা'}`,
          subtitle: `তারিখ: ${s.date} | মোট: ৳${Utils.toBanglaNumber(s.totalAmount || 0)}`,
          action: () => { UI.closeModal('globalSearchModal'); this.navigateView('sales'); }
        });
      }
    });

    // Transactions
    (this.state.transactions || []).forEach(t => {
      if ((t.categoryName && t.categoryName.toLowerCase().includes(q)) || (t.description && t.description.toLowerCase().includes(q)) || (t.person && t.person.toLowerCase().includes(q))) {
        results.push({
          type: 'লেনদেন',
          title: `${t.categoryName || 'হিসাব'} - ৳${Utils.toBanglaNumber(t.amount || 0)}`,
          subtitle: `${t.date} | ${t.person ? t.person + ' | ' : ''}${t.description || ''}`,
          action: () => { UI.closeModal('globalSearchModal'); this.navigateView('transactions'); }
        });
      }
    });

    if (results.length === 0) {
      container.innerHTML = `<p style="text-align: center; color: var(--text-muted); padding: 2rem 0;">"${Utils.escapeHTML(q)}"-এর সাথে মিল রেখে কিছু পাওয়া যায়নি</p>`;
      return;
    }

    container.innerHTML = results.slice(0, 15).map((r, idx) => `
      <div class="search-result-item" style="padding: 0.75rem 1rem; border-bottom: 1px solid var(--border-color); cursor: pointer; display: flex; justify-content: space-between; align-items: center;" onclick="App._searchItemClick(${idx})">
        <div>
          <div style="display: flex; align-items: center; gap: 0.5rem;">
            <span class="badge" style="font-size: 0.72rem; padding: 2px 6px; background: var(--bg-surface-alt);">${r.type}</span>
            <strong style="font-size: 0.95rem; color: var(--text-main);">${Utils.escapeHTML(r.title)}</strong>
          </div>
          <p style="font-size: 0.8rem; color: var(--text-muted); margin-top: 3px;">${Utils.escapeHTML(r.subtitle)}</p>
        </div>
        <span style="color: var(--primary-600); font-size: 0.85rem;">খুলুন →</span>
      </div>
    `).join('');

    this._lastSearchResults = results;
  },

  _searchItemClick(idx) {
    if (this._lastSearchResults && this._lastSearchResults[idx]) {
      this._lastSearchResults[idx].action();
    }
  },

  /**
   * ==========================================
   * 18. POS SALES VIEW & CONTROLLER
   * ==========================================
   */
  renderSalesView() {
    const today = Utils.getTodayDateString();
    const todaySales = (this.state.sales || []).filter(s => s.date === today);
    const totalToday = todaySales.reduce((sum, s) => sum + (Number(s.totalAmount) || 0), 0);
    const paidToday = todaySales.reduce((sum, s) => sum + (Number(s.paidAmount) || 0), 0);
    const dueToday = todaySales.reduce((sum, s) => sum + (Number(s.dueAmount) || 0), 0);

    const elTotal = document.getElementById('salesTodayTotal');
    const elCount = document.getElementById('salesTodayCount');
    const elPaid = document.getElementById('salesTodayPaid');
    const elDue = document.getElementById('salesTodayDue');
    const tbody = document.getElementById('salesTableBody');

    if (elTotal) elTotal.textContent = Utils.formatCurrency(totalToday);
    if (elCount) elCount.textContent = `${Utils.toBanglaNumber(todaySales.length)} টি চালান`;
    if (elPaid) elPaid.textContent = Utils.formatCurrency(paidToday);
    if (elDue) elDue.textContent = Utils.formatCurrency(dueToday);

    if (!tbody) return;
    const salesList = [...(this.state.sales || [])].sort((a, b) => new Date(b.date + ' ' + (b.createdAt || '')) - new Date(a.date + ' ' + (a.createdAt || '')));

    if (salesList.length === 0) {
      tbody.innerHTML = '<tr><td colspan="8" style="text-align: center; color: #94A3B8; padding: 2.5rem;">কোন বিক্রয় চালান পাওয়া যায়নি। উপরের বাটনে চাপ দিয়ে প্রথম পিওএস বিক্রি করুন।</td></tr>';
      return;
    }

    tbody.innerHTML = salesList.map((s, idx) => `
      <tr>
        <td style="text-align: center; font-weight: 600;">${Utils.toBanglaNumber(idx + 1)}</td>
        <td style="font-weight: 700; color: var(--primary-700);">${s.invoiceNo || 'INV-' + s.id.slice(-5)}</td>
        <td>${Utils.formatDateBengali(s.date)}</td>
        <td><strong>${Utils.escapeHTML(s.customerName || 'খুচরা কাস্টমার')}</strong>${s.customerPhone ? `<br><small style="color: var(--text-muted);">${s.customerPhone}</small>` : ''}</td>
        <td style="text-align: right; font-weight: 700;">${Utils.formatCurrency(s.totalAmount)}</td>
        <td style="text-align: right; color: #059669; font-weight: 600;">${Utils.formatCurrency(s.paidAmount)}</td>
        <td style="text-align: right; color: ${s.dueAmount > 0 ? '#DC2626' : 'var(--text-muted)'}; font-weight: 700;">${Utils.formatCurrency(s.dueAmount)}</td>
        <td style="text-align: center;">
          <button class="btn-action-icon" title="মেমো প্রিন্ট করুন" onclick="App.printPOSInvoice('${s.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9V2h12v7M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><path d="M6 14h12v8H6z"/></svg>
          </button>
          <button class="btn-action-icon btn-delete" title="মুছে ফেলুন" onclick="App.deletePOSSale('${s.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>
          </button>
        </td>
      </tr>
    `).join('');
  },

  openPOSSaleModal() {
    const form = document.getElementById('posSaleForm');
    if (form) form.reset();
    document.getElementById('posSaleDate').value = Utils.getTodayDateString();
    
    const dl = document.getElementById('customersDatalist');
    if (dl) {
      dl.innerHTML = (this.state.customers || []).map(c => `<option value="${c.name}">${c.phone ? ' (' + c.phone + ')' : ''} - বাকি: ৳${c.currentDue || 0}</option>`).join('');
    }

    const dueAlert = document.getElementById('posCustomerDueAlert');
    if (dueAlert) {
      dueAlert.style.display = 'none';
      dueAlert.innerHTML = '';
    }

    const tbody = document.getElementById('posItemsBody');
    if (tbody) {
      tbody.innerHTML = '';
      this.addPOSItemRow();
    }

    this.recalcPOSTotals();
    UI.openModal('posSaleModal');
  },

  onPOSCustomerChange(nameVal) {
    const cust = (this.state.customers || []).find(c => c.name.toLowerCase() === (nameVal || '').trim().toLowerCase());
    const phoneInput = document.getElementById('posCustomerPhone');
    const alertBox = document.getElementById('posCustomerDueAlert');

    if (cust) {
      if (phoneInput && !phoneInput.value) phoneInput.value = cust.phone || '';
      if (alertBox) {
        if (cust.currentDue > 0) {
          alertBox.style.display = 'block';
          alertBox.innerHTML = `⚠️ এই কাস্টমারের পূর্বে বকেয়া বাকি রয়েছে: <strong>৳ ${Utils.toBanglaNumber(cust.currentDue)}</strong>`;
        } else {
          alertBox.style.display = 'none';
        }
      }
    } else {
      if (alertBox) alertBox.style.display = 'none';
    }
  },

  addPOSItemRow(productId = '', qty = 1, price = null) {
    const tbody = document.getElementById('posItemsBody');
    if (!tbody) return;

    const rowId = 'pos_row_' + Date.now() + '_' + Math.random().toString(36).substring(2, 5);
    const prodOptions = (this.state.products || []).map(p => {
      const stockBadge = p.stock <= 0 ? ' [স্টক শেষ]' : ` [মজুত: ${p.stock}]`;
      const selected = p.id === productId ? 'selected' : '';
      return `<option value="${p.id}" data-price="${p.sellingPrice || 0}" data-stock="${p.stock || 0}" data-unit="${p.unit || 'টি'}" ${selected}>${p.name}${stockBadge}</option>`;
    }).join('');

    const tr = document.createElement('tr');
    tr.id = rowId;
    tr.innerHTML = `
      <td>
        <select class="form-select pos-item-select" onchange="App.onPOSProductSelect(this)" required style="font-size: 0.88rem;">
          <option value="">-- পণ্য নির্বাচন করুন --</option>
          ${prodOptions}
        </select>
        <span class="pos-item-stock-hint" style="font-size: 0.75rem; color: var(--text-muted); display: block; margin-top: 2px;"></span>
      </td>
      <td>
        <input type="number" class="form-control pos-item-qty" value="${qty}" min="0.01" step="any" required oninput="App.recalcPOSTotals()" style="text-align: center;">
      </td>
      <td>
        <input type="number" class="form-control pos-item-price" value="${price !== null ? price : 0}" min="0" step="any" required oninput="App.recalcPOSTotals()" style="text-align: right;">
      </td>
      <td style="text-align: right; font-weight: 700; vertical-align: middle;">
        <span class="pos-item-subtotal">৳ ০</span>
      </td>
      <td style="text-align: center; vertical-align: middle;">
        <button type="button" class="btn-action-icon btn-delete" onclick="App.removePOSItemRow(this)" title="মুছুন">✕</button>
      </td>
    `;
    tbody.appendChild(tr);
    this.recalcPOSTotals();
  },

  removePOSItemRow(btn) {
    const tr = btn.closest('tr');
    if (tr) tr.remove();
    this.recalcPOSTotals();
  },

  onPOSProductSelect(selectElem) {
    const tr = selectElem.closest('tr');
    if (!tr) return;
    const opt = selectElem.selectedOptions[0];
    if (opt && opt.value) {
      const price = opt.getAttribute('data-price') || 0;
      const stock = opt.getAttribute('data-stock') || 0;
      const unit = opt.getAttribute('data-unit') || 'টি';
      
      const priceInput = tr.querySelector('.pos-item-price');
      if (priceInput) priceInput.value = price;

      const hint = tr.querySelector('.pos-item-stock-hint');
      if (hint) {
        if (Number(stock) <= 0) {
          hint.innerHTML = '<span style="color: #DC2626; font-weight: 700;">⚠️ স্টক শূন্য!</span>';
        } else {
          hint.innerHTML = `মজুত: ${Utils.toBanglaNumber(stock)} ${unit}`;
        }
      }
    }
    this.recalcPOSTotals();
  },

  recalcPOSTotals() {
    const rows = document.querySelectorAll('#posItemsBody tr');
    let subtotal = 0;

    rows.forEach(r => {
      const qty = parseFloat(r.querySelector('.pos-item-qty')?.value) || 0;
      const price = parseFloat(r.querySelector('.pos-item-price')?.value) || 0;
      const rowTotal = Math.round(qty * price * 100) / 100;
      subtotal += rowTotal;
      const subDisplay = r.querySelector('.pos-item-subtotal');
      if (subDisplay) subDisplay.textContent = Utils.formatCurrency(rowTotal);
    });

    const discount = parseFloat(document.getElementById('posDiscountInput')?.value) || 0;
    const grandTotal = Math.max(0, Math.round((subtotal - discount) * 100) / 100);
    const paid = parseFloat(document.getElementById('posPaidInput')?.value) || 0;
    const due = Math.max(0, Math.round((grandTotal - paid) * 100) / 100);

    const elSub = document.getElementById('posSubtotalDisplay');
    const elTot = document.getElementById('posTotalDisplay');
    const elDue = document.getElementById('posDueDisplay');

    if (elSub) elSub.textContent = Utils.formatCurrency(subtotal);
    if (elTot) elTot.textContent = Utils.formatCurrency(grandTotal);
    if (elDue) elDue.textContent = Utils.formatCurrency(due);
  },

  async handleSavePOSSale(e) {
    e.preventDefault();
    const rows = document.querySelectorAll('#posItemsBody tr');
    if (rows.length === 0) {
      UI.toast('অন্তত একটি পণ্য বিক্রয় তালিকায় যুক্ত করুন', 'warning');
      return;
    }

    const items = [];
    let hasInvalidStock = false;
    let invalidProdName = '';

    rows.forEach(r => {
      const select = r.querySelector('.pos-item-select');
      const productId = select?.value;
      const opt = select?.selectedOptions[0];
      const prodName = opt?.text?.split(' [')[0] || 'পণ্য';
      const qty = parseFloat(r.querySelector('.pos-item-qty')?.value) || 0;
      const price = parseFloat(r.querySelector('.pos-item-price')?.value) || 0;

      if (productId && qty > 0) {
        const product = (this.state.products || []).find(p => p.id === productId);
        if (product && product.stock !== undefined && (product.stock - qty) < 0) {
          hasInvalidStock = true;
          invalidProdName = product.name;
        }
        items.push({
          productId,
          productName: prodName,
          qty,
          unitPrice: price,
          costPrice: product ? (product.buyingPrice || 0) : 0,
          subtotal: Math.round(qty * price * 100) / 100
        });
      }
    });

    if (items.length === 0) {
      UI.toast('অনুগ্রহ করে পণ্যের নাম ও পরিমাণ সঠিকভাবে দিন', 'warning');
      return;
    }

    if (hasInvalidStock) {
      if (!confirm(`সতর্কতা: "${invalidProdName}" পণ্যের যথেষ্ট স্টক নেই। তবুও কি বিক্রি করতে চান? (স্টক মাইনাস হবে)`)) {
        return;
      }
    }

    const custName = (document.getElementById('posCustomerInput')?.value || '').trim() || 'খুচরা কাস্টমার';
    const custPhone = (document.getElementById('posCustomerPhone')?.value || '').trim();
    const date = document.getElementById('posSaleDate')?.value || Utils.getTodayDateString();
    const discount = parseFloat(document.getElementById('posDiscountInput')?.value) || 0;
    const paid = parseFloat(document.getElementById('posPaidInput')?.value) || 0;
    const paymentMethod = document.getElementById('posPaymentMethodSelect')?.value || 'Cash';
    const notes = (document.getElementById('posNoteInput')?.value || '').trim();

    const subtotal = items.reduce((s, it) => s + it.subtotal, 0);
    const totalAmount = Math.max(0, Math.round((subtotal - discount) * 100) / 100);
    const dueAmount = Math.max(0, Math.round((totalAmount - paid) * 100) / 100);

    const saleId = 'sale_' + Date.now();
    const invoiceNo = 'INV-' + Date.now().toString().slice(-6);

    // 1. Find or create customer
    let customer = (this.state.customers || []).find(c => c.name.toLowerCase() === custName.toLowerCase());
    let customerId = customer ? customer.id : null;
    if (!customer && custName !== 'খুচরা কাস্টমার') {
      customerId = 'cust_' + Date.now();
      customer = {
        id: customerId,
        accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
        name: custName,
        phone: custPhone,
        address: '',
        currentDue: dueAmount,
        totalSales: totalAmount,
        createdAt: new Date().toISOString()
      };
      await khataDB.put('customers', customer);
    } else if (customer) {
      customer.currentDue = (customer.currentDue || 0) + dueAmount;
      customer.totalSales = (customer.totalSales || 0) + totalAmount;
      if (custPhone && !customer.phone) customer.phone = custPhone;
      await khataDB.put('customers', customer);
    }

    // 2. Save Sale Record
    const saleRecord = {
      id: saleId,
      accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
      invoiceNo,
      customerId,
      customerName: custName,
      customerPhone: custPhone,
      date,
      time: new Date().toLocaleTimeString('bn-BD', { hour: '2-digit', minute: '2-digit' }),
      subtotal,
      discount,
      totalAmount,
      paidAmount: paid,
      dueAmount,
      paymentMethod,
      notes,
      createdAt: new Date().toISOString()
    };
    await khataDB.put('sales', saleRecord);

    // 3. Save Sale Items and decrement product stocks
    for (const it of items) {
      const itemId = 'sitem_' + Date.now() + '_' + Math.random().toString(36).substring(2, 6);
      await khataDB.put('saleItems', {
        id: itemId,
        saleId,
        productId: it.productId,
        productName: it.productName,
        quantity: it.qty,
        unitPrice: it.unitPrice,
        costPrice: it.costPrice,
        subtotal: it.subtotal
      });

      const prod = (this.state.products || []).find(p => p.id === it.productId);
      if (prod) {
        prod.stock = (Number(prod.stock) || 0) - it.qty;
        await khataDB.put('products', prod);
        await khataDB.recordStockMovement({
          productId: it.productId,
          productName: it.productName,
          type: 'SALE_OUT',
          quantity: it.qty,
          referenceId: saleId,
          date
        });
      }
    }

    // 4. Record Unified Transaction for paid amount
    if (paid > 0) {
      const txId = 'tx_' + Date.now();
      await khataDB.put('transactions', {
        id: txId,
        accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
        type: 'INCOME',
        category: 'cat_inc_sales',
        categoryName: 'দোকান বিক্রি (POS)',
        amount: paid,
        date,
        person: custName,
        paymentMethod,
        description: `চালান #${invoiceNo}`,
        context: 'BUSINESS',
        isSales: true,
        referenceId: saleId,
        createdAt: new Date().toISOString()
      });
    }

    // 5. If due amount > 0, record in debts
    if (dueAmount > 0 && custName !== 'খুচরা কাস্টমার') {
      const debtId = 'debt_' + Date.now();
      await khataDB.put('debts', {
        id: debtId,
        accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
        personName: custName,
        personPhone: custPhone,
        amount: dueAmount,
        paidAmount: 0,
        remainingAmount: dueAmount,
        type: 'RECEIVABLE',
        status: 'UNPAID',
        date,
        description: `চালান #${invoiceNo}-এর বাকি`,
        referenceId: saleId,
        createdAt: new Date().toISOString()
      });
    }

    await khataDB.logAudit('POS_SALE', `বিক্রয় চালান #${invoiceNo} তৈরি করা হয়েছে। মোট: ৳${totalAmount}`);

    UI.closeModal('posSaleModal');
    UI.toast('পিওএস বিক্রি সফলভাবে সম্পন্ন হয়েছে! ✓', 'success');

    await this.refreshAllData();
    this.renderCurrentView();

    // Print Invoice Prompt
    setTimeout(() => {
      UI.printInvoice({
        ...saleRecord,
        businessName: this.state.activeAccount?.businessName || 'RI Family & Business Hisab',
        ownerName: this.state.activeAccount?.ownerName || 'রফিকুল ইসলাম',
        phone: this.state.activeAccount?.phone || '01700-000000',
        address: this.state.activeAccount?.address || 'বাংলাদেশ',
        items
      });
    }, 400);
  },

  async printPOSInvoice(saleId) {
    const sale = (this.state.sales || []).find(s => s.id === saleId);
    if (!sale) return;
    const allItems = await khataDB.getAll('saleItems');
    const items = allItems.filter(it => it.saleId === saleId);

    UI.printInvoice({
      ...sale,
      businessName: this.state.activeAccount?.businessName || 'RI Family & Business Hisab',
      ownerName: this.state.activeAccount?.ownerName || 'রফিকুল ইসলাম',
      phone: this.state.activeAccount?.phone || '01700-000000',
      address: this.state.activeAccount?.address || 'বাংলাদেশ',
      items
    });
  },

  async deletePOSSale(saleId) {
    const sale = (this.state.sales || []).find(s => s.id === saleId);
    if (!sale) return;

    UI.confirm({
      title: 'চালান মুছে ফেলবেন?',
      message: `আপনি কি নিশ্চিত যে চালান #${sale.invoiceNo} মুছে ফেলতে চান?`,
      confirmText: 'মুছে ফেলুন',
      isDanger: true,
      onConfirm: async () => {
        await khataDB.delete('sales', saleId);
        const allItems = await khataDB.getAll('saleItems');
        for (const it of allItems.filter(i => i.saleId === saleId)) {
          await khataDB.delete('saleItems', it.id);
        }
        await this.refreshAllData();
        this.renderCurrentView();
        UI.toast('চালানটি মুছে ফেলা হয়েছে', 'info');
      }
    });
  },

  /**
   * ==========================================
   * 19. PURCHASES VIEW & CONTROLLER
   * ==========================================
   */
  renderPurchasesView() {
    const today = Utils.getTodayDateString();
    const todayPur = (this.state.purchases || []).filter(p => p.date === today);
    const totalToday = todayPur.reduce((sum, p) => sum + (Number(p.totalAmount) || 0), 0);
    const paidToday = todayPur.reduce((sum, p) => sum + (Number(p.paidAmount) || 0), 0);
    const dueToday = todayPur.reduce((sum, p) => sum + (Number(p.dueAmount) || 0), 0);

    const elTotal = document.getElementById('purchaseTodayTotal');
    const elPaid = document.getElementById('purchaseTodayPaid');
    const elDue = document.getElementById('purchaseTodayDue');
    const tbody = document.getElementById('purchasesTableBody');

    if (elTotal) elTotal.textContent = Utils.formatCurrency(totalToday);
    if (elPaid) elPaid.textContent = Utils.formatCurrency(paidToday);
    if (elDue) elDue.textContent = Utils.formatCurrency(dueToday);

    if (!tbody) return;
    const purchasesList = [...(this.state.purchases || [])].sort((a, b) => new Date(b.date + ' ' + (b.createdAt || '')) - new Date(a.date + ' ' + (a.createdAt || '')));

    if (purchasesList.length === 0) {
      tbody.innerHTML = '<tr><td colspan="8" style="text-align: center; color: #94A3B8; padding: 2.5rem;">কোন ক্রয় চালান পাওয়া যায়নি। উপরের বাটনে চাপ দিয়ে নতুন ক্রয় চালান যোগ করুন।</td></tr>';
      return;
    }

    tbody.innerHTML = purchasesList.map((p, idx) => `
      <tr>
        <td style="text-align: center; font-weight: 600;">${Utils.toBanglaNumber(idx + 1)}</td>
        <td style="font-weight: 700; color: #D97706;">${p.invoiceNo || 'PUR-' + p.id.slice(-5)}</td>
        <td>${Utils.formatDateBengali(p.date)}</td>
        <td><strong>${Utils.escapeHTML(p.supplierName || 'সাপ্লায়ার')}</strong></td>
        <td style="text-align: right; font-weight: 700;">${Utils.formatCurrency(p.totalAmount)}</td>
        <td style="text-align: right; color: #059669; font-weight: 600;">${Utils.formatCurrency(p.paidAmount)}</td>
        <td style="text-align: right; color: ${p.dueAmount > 0 ? '#DC2626' : 'var(--text-muted)'}; font-weight: 700;">${Utils.formatCurrency(p.dueAmount)}</td>
        <td style="text-align: center;">
          <button class="btn-action-icon btn-delete" title="মুছে ফেলুন" onclick="App.deletePurchase('${p.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>
          </button>
        </td>
      </tr>
    `).join('');
  },

  openPurchaseModal() {
    const form = document.getElementById('purchaseForm');
    if (form) form.reset();
    document.getElementById('purchaseDate').value = Utils.getTodayDateString();

    const dl = document.getElementById('suppliersDatalist');
    if (dl) {
      dl.innerHTML = (this.state.suppliers || []).map(s => `<option value="${s.name}">${s.company ? ' (' + s.company + ')' : ''} - দেনা: ৳${s.currentDue || 0}</option>`).join('');
    }

    const tbody = document.getElementById('purchaseItemsBody');
    if (tbody) {
      tbody.innerHTML = '';
      this.addPurchaseItemRow();
    }

    this.recalcPurchaseTotals();
    UI.openModal('purchaseModal');
  },

  addPurchaseItemRow(productId = '', qty = 1, price = null) {
    const tbody = document.getElementById('purchaseItemsBody');
    if (!tbody) return;

    const rowId = 'pur_row_' + Date.now() + '_' + Math.random().toString(36).substring(2, 5);
    const prodOptions = (this.state.products || []).map(p => {
      const selected = p.id === productId ? 'selected' : '';
      return `<option value="${p.id}" data-buyprice="${p.buyingPrice || 0}" ${selected}>${p.name}</option>`;
    }).join('');

    const tr = document.createElement('tr');
    tr.id = rowId;
    tr.innerHTML = `
      <td>
        <select class="form-select pur-item-select" onchange="App.onPurchaseProductSelect(this)" required style="font-size: 0.88rem;">
          <option value="">-- পণ্য নির্বাচন করুন --</option>
          ${prodOptions}
        </select>
      </td>
      <td>
        <input type="number" class="form-control pur-item-qty" value="${qty}" min="0.01" step="any" required oninput="App.recalcPurchaseTotals()" style="text-align: center;">
      </td>
      <td>
        <input type="number" class="form-control pur-item-price" value="${price !== null ? price : 0}" min="0" step="any" required oninput="App.recalcPurchaseTotals()" style="text-align: right;">
      </td>
      <td style="text-align: right; font-weight: 700; vertical-align: middle;">
        <span class="pur-item-subtotal">৳ ০</span>
      </td>
      <td style="text-align: center; vertical-align: middle;">
        <button type="button" class="btn-action-icon btn-delete" onclick="App.removePurchaseItemRow(this)" title="মুছুন">✕</button>
      </td>
    `;
    tbody.appendChild(tr);
    this.recalcPurchaseTotals();
  },

  removePurchaseItemRow(btn) {
    const tr = btn.closest('tr');
    if (tr) tr.remove();
    this.recalcPurchaseTotals();
  },

  onPurchaseProductSelect(selectElem) {
    const tr = selectElem.closest('tr');
    if (!tr) return;
    const opt = selectElem.selectedOptions[0];
    if (opt && opt.value) {
      const buyPrice = opt.getAttribute('data-buyprice') || 0;
      const priceInput = tr.querySelector('.pur-item-price');
      if (priceInput) priceInput.value = buyPrice;
    }
    this.recalcPurchaseTotals();
  },

  recalcPurchaseTotals() {
    const rows = document.querySelectorAll('#purchaseItemsBody tr');
    let total = 0;

    rows.forEach(r => {
      const qty = parseFloat(r.querySelector('.pur-item-qty')?.value) || 0;
      const price = parseFloat(r.querySelector('.pur-item-price')?.value) || 0;
      const rowTotal = Math.round(qty * price * 100) / 100;
      total += rowTotal;
      const subDisplay = r.querySelector('.pur-item-subtotal');
      if (subDisplay) subDisplay.textContent = Utils.formatCurrency(rowTotal);
    });

    const paid = parseFloat(document.getElementById('purchasePaidInput')?.value) || 0;
    const due = Math.max(0, Math.round((total - paid) * 100) / 100);

    const elTot = document.getElementById('purchaseTotalDisplay');
    const elDue = document.getElementById('purchaseDueDisplay');

    if (elTot) elTot.textContent = Utils.formatCurrency(total);
    if (elDue) elDue.textContent = Utils.formatCurrency(due);
  },

  async handleSavePurchase(e) {
    e.preventDefault();
    const rows = document.querySelectorAll('#purchaseItemsBody tr');
    if (rows.length === 0) {
      UI.toast('কমপক্ষে একটি পণ্য ক্রয়ের বিবরণ যোগ করুন', 'warning');
      return;
    }

    const items = [];
    rows.forEach(r => {
      const select = r.querySelector('.pur-item-select');
      const productId = select?.value;
      const opt = select?.selectedOptions[0];
      const prodName = opt?.text || 'পণ্য';
      const qty = parseFloat(r.querySelector('.pur-item-qty')?.value) || 0;
      const price = parseFloat(r.querySelector('.pur-item-price')?.value) || 0;

      if (productId && qty > 0) {
        items.push({
          productId,
          productName: prodName,
          qty,
          unitPrice: price,
          subtotal: Math.round(qty * price * 100) / 100
        });
      }
    });

    if (items.length === 0) {
      UI.toast('পণ্য ও পরিমাণ সঠিকভাবে প্রদান করুন', 'warning');
      return;
    }

    const suppName = (document.getElementById('purchaseSupplierInput')?.value || '').trim() || 'সাধারণ সাপ্লায়ার';
    const invoiceNo = (document.getElementById('purchaseInvoiceNo')?.value || '').trim() || ('PUR-' + Date.now().toString().slice(-6));
    const date = document.getElementById('purchaseDate')?.value || Utils.getTodayDateString();
    const paid = parseFloat(document.getElementById('purchasePaidInput')?.value) || 0;
    const paymentMethod = document.getElementById('purchasePaymentMethodSelect')?.value || 'Cash';

    const totalAmount = items.reduce((s, it) => s + it.subtotal, 0);
    const dueAmount = Math.max(0, Math.round((totalAmount - paid) * 100) / 100);

    const purchaseId = 'pur_' + Date.now();

    // 1. Supplier update or creation
    let supplier = (this.state.suppliers || []).find(s => s.name.toLowerCase() === suppName.toLowerCase());
    let supplierId = supplier ? supplier.id : null;
    if (!supplier) {
      supplierId = 'supp_' + Date.now();
      supplier = {
        id: supplierId,
        accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
        name: suppName,
        phone: '',
        company: '',
        currentDue: dueAmount,
        totalPurchases: totalAmount,
        createdAt: new Date().toISOString()
      };
      await khataDB.put('suppliers', supplier);
    } else {
      supplier.currentDue = (supplier.currentDue || 0) + dueAmount;
      supplier.totalPurchases = (supplier.totalPurchases || 0) + totalAmount;
      await khataDB.put('suppliers', supplier);
    }

    // 2. Save Purchase Record
    const purchaseRecord = {
      id: purchaseId,
      accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
      invoiceNo,
      supplierId,
      supplierName: suppName,
      date,
      totalAmount,
      paidAmount: paid,
      dueAmount,
      paymentMethod,
      createdAt: new Date().toISOString()
    };
    await khataDB.put('purchases', purchaseRecord);

    // 3. Save Purchase Items, update product stock & buying price
    for (const it of items) {
      const itemId = 'pitem_' + Date.now() + '_' + Math.random().toString(36).substring(2, 6);
      await khataDB.put('purchaseItems', {
        id: itemId,
        purchaseId,
        productId: it.productId,
        productName: it.productName,
        quantity: it.qty,
        unitPrice: it.unitPrice,
        subtotal: it.subtotal
      });

      const prod = (this.state.products || []).find(p => p.id === it.productId);
      if (prod) {
        prod.stock = (Number(prod.stock) || 0) + it.qty;
        prod.buyingPrice = it.unitPrice;
        await khataDB.put('products', prod);

        await khataDB.recordStockMovement({
          productId: it.productId,
          productName: it.productName,
          type: 'PURCHASE_IN',
          quantity: it.qty,
          referenceId: purchaseId,
          date
        });
      }
    }

    // 4. Unified Transaction for paid amount
    if (paid > 0) {
      const txId = 'tx_' + Date.now();
      await khataDB.put('transactions', {
        id: txId,
        accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
        type: 'EXPENSE',
        category: 'cat_exp_stock',
        categoryName: 'মালামাল ক্রয় (Stock Purchase)',
        expenseType: 'EXPENSE_SHOP',
        amount: paid,
        date,
        person: suppName,
        paymentMethod,
        description: `ক্রয় চালান #${invoiceNo}`,
        context: 'BUSINESS',
        referenceId: purchaseId,
        createdAt: new Date().toISOString()
      });
    }

    // 5. Debt record for due (payable)
    if (dueAmount > 0) {
      const debtId = 'debt_' + Date.now();
      await khataDB.put('debts', {
        id: debtId,
        accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
        personName: suppName,
        amount: dueAmount,
        paidAmount: 0,
        remainingAmount: dueAmount,
        type: 'PAYABLE',
        status: 'UNPAID',
        date,
        description: `ক্রয় চালান #${invoiceNo}-এর পাওনা`,
        referenceId: purchaseId,
        createdAt: new Date().toISOString()
      });
    }

    await khataDB.logAudit('PURCHASE_ENTRY', `ক্রয় চালান #${invoiceNo} সম্পন্ন। মোট: ৳${totalAmount}`);

    UI.closeModal('purchaseModal');
    UI.toast('ক্রয় চালান সফলভাবে সংরক্ষণ ও স্টক আপডেট হয়েছে! ✓', 'success');

    await this.refreshAllData();
    this.renderCurrentView();
  },

  async deletePurchase(purchaseId) {
    const pur = (this.state.purchases || []).find(p => p.id === purchaseId);
    if (!pur) return;

    UI.confirm({
      title: 'ক্রয় চালান মুছে ফেলবেন?',
      message: `আপনি কি নিশ্চিত যে চালান #${pur.invoiceNo || purchaseId} মুছে ফেলতে চান?`,
      confirmText: 'মুছে ফেলুন',
      isDanger: true,
      onConfirm: async () => {
        await khataDB.delete('purchases', purchaseId);
        const allItems = await khataDB.getAll('purchaseItems');
        for (const it of allItems.filter(i => i.purchaseId === purchaseId)) {
          await khataDB.delete('purchaseItems', it.id);
        }
        await this.refreshAllData();
        this.renderCurrentView();
        UI.toast('ক্রয় চালানটি মুছে ফেলা হয়েছে', 'info');
      }
    });
  },

  /**
   * ==========================================
   * 20. CUSTOMERS DIRECTORY & LEDGERS
   * ==========================================
   */
  renderCustomersView() {
    const list = this.state.customers || [];
    const totalDue = list.reduce((s, c) => s + (Number(c.currentDue) || 0), 0);

    const elDue = document.getElementById('customersTotalDue');
    const elBadge = document.getElementById('customersCountBadge');
    const tbody = document.getElementById('customersTableBody');

    if (elDue) elDue.textContent = Utils.formatCurrency(totalDue);
    if (elBadge) elBadge.textContent = `${Utils.toBanglaNumber(list.length)} জন`;

    if (!tbody) return;
    if (list.length === 0) {
      tbody.innerHTML = '<tr><td colspan="8" style="text-align: center; color: #94A3B8; padding: 2.5rem;">কোন কাস্টমার পাওয়া যায়নি। উপরের বাটনে চাপ দিয়ে কাস্টমার যোগ করুন।</td></tr>';
      return;
    }

    tbody.innerHTML = list.map((c, idx) => `
      <tr>
        <td style="text-align: center; font-weight: 600;">${Utils.toBanglaNumber(idx + 1)}</td>
        <td><strong>${Utils.escapeHTML(c.name)}</strong></td>
        <td>${c.phone ? `<a href="tel:${c.phone}" style="color: var(--primary-600);">${c.phone}</a>` : '—'}</td>
        <td>${Utils.escapeHTML(c.address || '—')}</td>
        <td style="text-align: right;">${Utils.formatCurrency(c.totalSales || 0)}</td>
        <td style="text-align: right; color: ${c.currentDue > 0 ? '#DC2626' : '#059669'}; font-weight: 700;">${Utils.formatCurrency(c.currentDue || 0)}</td>
        <td style="text-align: center;">
          <button class="btn btn-success btn-sm" style="padding: 3px 8px; font-size: 0.78rem;" onclick="App.openCustomerPaymentModal('${c.id}')">+ টাকা আদায়</button>
          <button class="btn-action-icon" title="এডিট" onclick="App.openCustomerModal('${c.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
          </button>
          <button class="btn-action-icon btn-delete" title="মুছুন" onclick="App.deleteCustomer('${c.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>
          </button>
        </td>
      </tr>
    `).join('');
  },

  openCustomerModal(customerId = null) {
    const form = document.getElementById('customerForm');
    if (form) form.reset();
    document.getElementById('customerId').value = customerId || '';
    document.getElementById('customerModalTitle').textContent = customerId ? 'কাস্টমার তথ্য সম্পাদনা' : 'নতুন কাস্টমার যোগ করুন';

    if (customerId) {
      const c = (this.state.customers || []).find(cust => cust.id === customerId);
      if (c) {
        document.getElementById('custNameInput').value = c.name || '';
        document.getElementById('custPhoneInput').value = c.phone || '';
        document.getElementById('custAddressInput').value = c.address || '';
        document.getElementById('custOpeningDueInput').value = c.currentDue || 0;
      }
    }
    UI.openModal('customerModal');
  },

  async handleSaveCustomer(e) {
    e.preventDefault();
    const id = document.getElementById('customerId')?.value || ('cust_' + Date.now());
    const name = (document.getElementById('custNameInput')?.value || '').trim();
    const phone = (document.getElementById('custPhoneInput')?.value || '').trim();
    const address = (document.getElementById('custAddressInput')?.value || '').trim();
    const openingDue = parseFloat(document.getElementById('custOpeningDueInput')?.value) || 0;

    if (!name) return;

    const existing = (this.state.customers || []).find(c => c.id === id);
    const customerObj = {
      id,
      accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
      name,
      phone,
      address,
      currentDue: existing ? (existing.currentDue || 0) : openingDue,
      totalSales: existing ? (existing.totalSales || 0) : 0,
      updatedAt: new Date().toISOString()
    };
    if (!existing) customerObj.createdAt = new Date().toISOString();

    await khataDB.put('customers', customerObj);
    UI.closeModal('customerModal');
    UI.toast('কাস্টমার তথ্য সংরক্ষিত হয়েছে ✓', 'success');

    await this.refreshAllData();
    this.renderCurrentView();
  },

  async deleteCustomer(customerId) {
    UI.confirm({
      title: 'কাস্টমার মুছে ফেলবেন?',
      message: 'আপনি কি নিশ্চিত যে এই কাস্টমারকে তালিকা থেকে মুছে ফেলতে চান?',
      confirmText: 'মুছে ফেলুন',
      isDanger: true,
      onConfirm: async () => {
        await khataDB.delete('customers', customerId);
        await this.refreshAllData();
        this.renderCurrentView();
        UI.toast('কাস্টমার মুছে ফেলা হয়েছে', 'info');
      }
    });
  },

  openCustomerPaymentModal(customerId = null) {
    let cust = null;
    if (customerId) {
      cust = (this.state.customers || []).find(c => c.id === customerId);
    }
    this.openAddTransactionModal('INCOME');
    if (cust) {
      const personInput = document.getElementById('txPersonInput');
      if (personInput) personInput.value = cust.name;
      const descInput = document.getElementById('txDescInput');
      if (descInput) descInput.value = `কাস্টমার বাকি আদায় (পূর্বে বাকি: ৳${cust.currentDue || 0})`;
      const catSelect = document.getElementById('txCategorySelect');
      if (catSelect) catSelect.value = 'cat_inc_sales';
    }
  },

  /**
   * ==========================================
   * 21. SUPPLIERS DIRECTORY & LEDGERS
   * ==========================================
   */
  renderSuppliersView() {
    const list = this.state.suppliers || [];
    const totalDue = list.reduce((s, sup) => s + (Number(sup.currentDue) || 0), 0);

    const elDue = document.getElementById('suppliersTotalDue');
    const elBadge = document.getElementById('suppliersCountBadge');
    const tbody = document.getElementById('suppliersTableBody');

    if (elDue) elDue.textContent = Utils.formatCurrency(totalDue);
    if (elBadge) elBadge.textContent = `${Utils.toBanglaNumber(list.length)} জন`;

    if (!tbody) return;
    if (list.length === 0) {
      tbody.innerHTML = '<tr><td colspan="8" style="text-align: center; color: #94A3B8; padding: 2.5rem;">কোন মহাজন বা সাপ্লায়ার পাওয়া যায়নি। উপরের বাটনে চাপ দিয়ে যোগ করুন।</td></tr>';
      return;
    }

    tbody.innerHTML = list.map((s, idx) => `
      <tr>
        <td style="text-align: center; font-weight: 600;">${Utils.toBanglaNumber(idx + 1)}</td>
        <td><strong>${Utils.escapeHTML(s.name)}</strong></td>
        <td>${s.phone ? `<a href="tel:${s.phone}" style="color: var(--primary-600);">${s.phone}</a>` : '—'}</td>
        <td>${Utils.escapeHTML(s.company || s.address || '—')}</td>
        <td style="text-align: right;">${Utils.formatCurrency(s.totalPurchases || 0)}</td>
        <td style="text-align: right; color: ${s.currentDue > 0 ? '#DC2626' : '#059669'}; font-weight: 700;">${Utils.formatCurrency(s.currentDue || 0)}</td>
        <td style="text-align: center;">
          <button class="btn btn-warning btn-sm" style="padding: 3px 8px; font-size: 0.78rem;" onclick="App.openSupplierPaymentModal('${s.id}')">- টাকা পরিশোধ</button>
          <button class="btn-action-icon" title="এডিট" onclick="App.openSupplierModal('${s.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
          </button>
          <button class="btn-action-icon btn-delete" title="মুছুন" onclick="App.deleteSupplier('${s.id}')">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>
          </button>
        </td>
      </tr>
    `).join('');
  },

  openSupplierModal(supplierId = null) {
    const form = document.getElementById('supplierForm');
    if (form) form.reset();
    document.getElementById('supplierId').value = supplierId || '';
    document.getElementById('supplierModalTitle').textContent = supplierId ? 'সাপ্লায়ার তথ্য সম্পাদনা' : 'নতুন মহাজন / সাপ্লায়ার যোগ করুন';

    if (supplierId) {
      const s = (this.state.suppliers || []).find(sup => sup.id === supplierId);
      if (s) {
        document.getElementById('suppNameInput').value = s.name || '';
        document.getElementById('suppPhoneInput').value = s.phone || '';
        document.getElementById('suppAddressInput').value = s.company || s.address || '';
        document.getElementById('suppOpeningDueInput').value = s.currentDue || 0;
      }
    }
    UI.openModal('supplierModal');
  },

  async handleSaveSupplier(e) {
    e.preventDefault();
    const id = document.getElementById('supplierId')?.value || ('supp_' + Date.now());
    const name = (document.getElementById('suppNameInput')?.value || '').trim();
    const phone = (document.getElementById('suppPhoneInput')?.value || '').trim();
    const address = (document.getElementById('suppAddressInput')?.value || '').trim();
    const openingDue = parseFloat(document.getElementById('suppOpeningDueInput')?.value) || 0;

    if (!name) return;

    const existing = (this.state.suppliers || []).find(s => s.id === id);
    const supplierObj = {
      id,
      accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
      name,
      phone,
      company: address,
      address,
      currentDue: existing ? (existing.currentDue || 0) : openingDue,
      totalPurchases: existing ? (existing.totalPurchases || 0) : 0,
      updatedAt: new Date().toISOString()
    };
    if (!existing) supplierObj.createdAt = new Date().toISOString();

    await khataDB.put('suppliers', supplierObj);
    UI.closeModal('supplierModal');
    UI.toast('মহাজন / সাপ্লায়ার তথ্য সংরক্ষিত হয়েছে ✓', 'success');

    await this.refreshAllData();
    this.renderCurrentView();
  },

  async deleteSupplier(supplierId) {
    UI.confirm({
      title: 'সাপ্লায়ার মুছে ফেলবেন?',
      message: 'আপনি কি নিশ্চিত যে এই সাপ্লায়ারকে মুছে ফেলতে চান?',
      confirmText: 'মুছে ফেলুন',
      isDanger: true,
      onConfirm: async () => {
        await khataDB.delete('suppliers', supplierId);
        await this.refreshAllData();
        this.renderCurrentView();
        UI.toast('সাপ্লায়ার মুছে ফেলা হয়েছে', 'info');
      }
    });
  },

  openSupplierPaymentModal(supplierId = null) {
    let sup = null;
    if (supplierId) {
      sup = (this.state.suppliers || []).find(s => s.id === supplierId);
    }
    this.openAddTransactionModal('EXPENSE_SHOP');
    if (sup) {
      const personInput = document.getElementById('txPersonInput');
      if (personInput) personInput.value = sup.name;
      const descInput = document.getElementById('txDescInput');
      if (descInput) descInput.value = `মহাজন পাওনা পরিশোধ (পূর্বে দেনা: ৳${sup.currentDue || 0})`;
      const catSelect = document.getElementById('txCategorySelect');
      if (catSelect) catSelect.value = 'cat_exp_stock';
    }
  },

  openNewDebtModal() {
    this.openAddDebtModal();
  },

  /**
   * ==========================================
   * 22. FINANCIAL ACCOUNTS & FUND TRANSFERS
   * ==========================================
   */
  renderAccountsView() {
    const metrics = Accounting.calculateMetrics(this.state.transactions, this.state.debts, this.state.sales, this.state.saleItems, this.state.products);
    const grid = document.getElementById('accountsViewGrid');
    if (!grid) return;

    const accs = [
      { name: '💵 নগদ ক্যাশ (Cash in Hand)', balance: metrics.accountBalances.Cash || 0, icon: '৳', bg: '#ECFDF5', color: '#059669', type: 'নগদ' },
      { name: '🏛️ ব্যাংক একাউন্ট (Bank Account)', balance: metrics.accountBalances.Bank || 0, icon: '🏛️', bg: '#EFF6FF', color: '#2563EB', type: 'ব্যাংক' },
      { name: '📱 বিকাশ ওয়ালেট (bKash)', balance: metrics.accountBalances.bKash || 0, icon: '📱', bg: '#FDF2F8', color: '#DB2777', type: 'এমএফএস' },
      { name: '📲 নগদ ওয়ালেট (Nagad)', balance: metrics.accountBalances.Nagad || 0, icon: '📲', bg: '#FFFBEB', color: '#D97706', type: 'এমএফএস' },
      { name: '🚀 রকেট ওয়ালেট (Rocket)', balance: metrics.accountBalances.Rocket || 0, icon: '🚀', bg: '#F5F3FF', color: '#7C3AED', type: 'এমএফএস' },
      { name: '💳 সর্বমোট তরল ফান্ড', balance: metrics.totalLiquidBalance || 0, icon: '✓', bg: '#1E293B', color: '#FFFFFF', isTotal: true }
    ];

    grid.innerHTML = accs.map(a => `
      <div class="account-strip-card" style="${a.isTotal ? 'background: linear-gradient(135deg, #1E293B, #0F172A); color: white;' : ''}">
        <div class="account-strip-meta">
          <span class="account-strip-title" style="${a.isTotal ? 'color: rgba(255,255,255,0.85);' : ''}">${a.name}</span>
          <span class="account-strip-amount" style="${a.isTotal ? 'color: #FFFFFF;' : ''}">${Utils.formatCurrency(a.balance)}</span>
        </div>
        <div class="account-strip-icon" style="background: ${a.bg}; color: ${a.color};">${a.icon}</div>
      </div>
    `).join('');

    const tbody = document.getElementById('accountsTransfersTableBody');
    if (!tbody) return;

    const transfers = (this.state.transactions || []).filter(t => t.type === 'TRANSFER');
    if (transfers.length === 0) {
      tbody.innerHTML = '<tr><td colspan="6" style="text-align: center; color: #94A3B8; padding: 2rem;">কোন স্থানান্তর ইতিহাস নেই। উপরের "ফান্ড ট্রান্সফার" বাটনে চাপ দিয়ে স্থানান্তর করুন।</td></tr>';
      return;
    }

    tbody.innerHTML = transfers.map((t, idx) => `
      <tr>
        <td style="text-align: center; font-weight: 600;">${Utils.toBanglaNumber(idx + 1)}</td>
        <td>${Utils.formatDateBengali(t.date)}</td>
        <td><strong>${t.fromAccount || 'ক্যাশ'} ➔ ${t.toAccount || 'ব্যাংক'}</strong></td>
        <td>ফান্ড ট্রান্সফার</td>
        <td style="text-align: right; font-weight: 700; color: #2563EB;">${Utils.formatCurrency(t.amount)}</td>
        <td>${Utils.escapeHTML(t.description || t.notes || '—')}</td>
      </tr>
    `).join('');
  },

  openTransferModal() {
    const form = document.getElementById('transferForm');
    if (form) form.reset();
    document.getElementById('transferDateInput').value = Utils.getTodayDateString();
    UI.openModal('transferModal');
  },

  async handleSaveTransfer(e) {
    e.preventDefault();
    const fromAcc = document.getElementById('transferFromSelect')?.value;
    const toAcc = document.getElementById('transferToSelect')?.value;
    const amountStr = document.getElementById('transferAmountInput')?.value;
    const date = document.getElementById('transferDateInput')?.value || Utils.getTodayDateString();
    const note = (document.getElementById('transferNoteInput')?.value || '').trim();

    const amount = parseFloat(amountStr) || 0;
    if (amount <= 0) {
      UI.toast('সঠিক টাকার পরিমাণ দিন', 'warning');
      return;
    }

    if (fromAcc === toAcc) {
      UI.toast('একই একাউন্টে স্থানান্তর সম্ভব নয়। ভিন্ন একাউন্ট নির্বাচন করুন।', 'warning');
      return;
    }

    const txId = 'tx_' + Date.now();
    const transferTx = {
      id: txId,
      accountId: this.state.activeAccount?.id || 'acc_rafiqul_main',
      type: 'TRANSFER',
      fromAccount: fromAcc,
      toAccount: toAcc,
      paymentMethod: fromAcc,
      amount,
      date,
      description: note || `${fromAcc} হতে ${toAcc}-এ ট্রান্সফার`,
      context: 'TRANSFER',
      createdAt: new Date().toISOString()
    };

    await khataDB.put('transactions', transferTx);
    await khataDB.logAudit('TRANSFER', `${fromAcc} হতে ${toAcc}-এ ৳${amount} ট্রান্সফার`);

    UI.closeModal('transferModal');
    UI.toast(`৳${Utils.toBanglaNumber(amount)} সফলভাবে ট্রান্সফার হয়েছে! ✓`, 'success');

    await this.refreshAllData();
    this.renderCurrentView();
  },

  /**
   * ==========================================
   * 23. FAMILY FINANCE VIEW & CONTROLLER
   * ==========================================
   */
  renderFamilyView() {
    const metrics = Accounting.calculateMetrics(this.state.transactions, this.state.debts, [], [], [], 'FAMILY');

    const elInc = document.getElementById('familyTotalIncome');
    const elExp = document.getElementById('familyTotalExpenses');
    const elSav = document.getElementById('familyNetSavings');

    if (elInc) elInc.textContent = Utils.formatCurrency(metrics.familyIncome);
    if (elExp) elExp.textContent = Utils.formatCurrency(metrics.familyExpenses);
    if (elSav) elSav.textContent = Utils.formatCurrency(metrics.netFamilySavings);

    const familyList = document.getElementById('familyRecentTxList');
    if (familyList) {
      const familyTxs = (this.state.transactions || []).filter(t => 
        t.expenseType === 'EXPENSE_HOUSE' || 
        t.category === 'cat_inc_personal' || 
        (t.category && (t.category.includes('house') || t.category.includes('bazar') || t.category.includes('gas') || t.category.includes('water') || t.category.includes('med') || t.category.includes('edu')))
      ).slice(0, 6);

      if (familyTxs.length === 0) {
        familyList.innerHTML = '<div class="empty-state-box" style="padding: 2rem;">সংসার খরচের কোনো এন্ট্রি পাওয়া যায়নি</div>';
      } else {
        familyList.innerHTML = familyTxs.map(t => this.renderTransactionRowHTML(t)).join('');
      }
    }

    setTimeout(() => {
      const canvas = document.getElementById('familyExpenseDonutChart');
      if (canvas && window.KhataCharts) {
        const catMap = metrics.familyCategories || {};
        const items = Object.values(catMap).map(c => ({ label: c.name, value: c.amount }));
        if (items.length > 0) {
          KhataCharts.drawDonutExpenseChart('familyExpenseDonutChart', items);
        }
      }
    }, 100);
  }
};

// Global Helpers for HTML attributes
window.App = App;
window.navigateView = (v) => App.navigateView(v);
window.toggleSidebar = (open) => {
  const sb = document.getElementById('appSidebar');
  const bd = document.getElementById('sidebarBackdrop');
  if (!sb || !bd) return;
  if (open) {
    sb.classList.add('mobile-open');
    bd.classList.add('show');
  } else {
    sb.classList.remove('mobile-open');
    bd.classList.remove('show');
  }
};

// Initialize App on DOM Ready
document.addEventListener('DOMContentLoaded', () => {
  App.init();
});
